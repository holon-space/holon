generated_by: scripts/pbt_inventory.py extract
cap_universe:
  ref:
  - RefAdvice
  - RefApplyMutationMut
  - RefArrowNav
  - RefBackend
  - RefBlockTree
  - RefBlockTreeMut
  - RefBoot
  - RefBootMut
  - RefClock
  - RefClockMut
  - RefDocuments
  - RefDocumentsMut
  - RefEditor
  - RefEditorMirror
  - RefEditorMirrorMut
  - RefFocusMut
  - RefFocusRoots
  - RefGlobalFocus
  - RefHistoryExpectation
  - RefLayout
  - RefLayoutInteract
  - RefLayoutMutate
  - RefLifecycle
  - RefNavHistory
  - RefNavHistoryMut
  - RefPeers
  - RefPeersMut
  - RefPins
  - RefPinsMut
  - RefRenderExpr
  - RefSqlCardinality
  - RefTaskState
  - RefTaskStateToggle
  - RefToggle
  - RefToggleMut
  - RefViewSelectionMut
  - RefWatchesMut
  - RefWiring
  sut:
  - SutAdviceMatview
  - SutAppLifecycle
  - SutApplyMutation
  - SutBackend
  - SutBlockCreate
  - SutBlockInteract
  - SutBlockTreeWrite
  - SutBootWatches
  - SutClockAdvance
  - SutDriver
  - SutEdgeFieldWrite
  - SutEditorMirror
  - SutEditorMirrorRead
  - SutEditorMirrorWrite
  - SutErrorLog
  - SutFixtureFs
  - SutFocusWrite
  - SutFrontendEmissions
  - SutFrontendEngine
  - SutHistory
  - SutHistoryWrite
  - SutLayout
  - SutLoro
  - SutLoroLog
  - SutLoroTaskState
  - SutMcpEmit
  - SutMetricsLifecycle
  - SutMutate
  - SutNavHistoryDrive
  - SutNavHistoryWrite
  - SutOrgRead
  - SutOrgRender
  - SutQueryResults
  - SutQuiesce
  - SutRender
  - SutRenderer
  - SutSeamMutate
  - SutSqlProjection
  - SutTemplateInstantiate
  - SutTransitionTarget
  - SutViewControl
  - SutWatchRegister
components:
- file: crates/holon-integration-tests/src/mcp_user_driver.rs
  kind: driver
  covers:
  - live-mcp — top (out-of-process) driver rung; unsupported verbs panic-loud (never fake), read-before-refresh
    panics. Exemplary fail-loud.
- file: crates/holon-integration-tests/src/mutation_driver.rs
  kind: module
  provides_caps:
  - SutBlockCreate
  - SutBlockTreeWrite
  - SutTemplateInstantiate
- file: crates/holon-integration-tests/src/pbt/action_actor_state.rs
  kind: ref
  covers:
  - 'action-actor-state — lifecycle flag, `next_doc_id` allocator, last-transition tag (Markov weighting),
    and the undo/redo `BlockState` snapshot stacks. FIDELITY: `next_doc_id`/`next_id` are monotonic across
    undo/redo by design (prod never reuses a burned id, see'
- file: crates/holon-integration-tests/src/pbt/advice_expectation.rs
  kind: ref
  covers:
  - 'advice-weave — INDEPENDENT Rust re-implementation of the advice matview + read-time weave (self-join
    `shared_tag_count`, suppression anti-join, K-cut, sort DESC). Differential against the SUT''s Turso
    IVM SQL path — uncorrelated derivation, NOT a mirror. FIDELITY: hand-encodes the contract pinned in
    `holon-advice` matview_build.rs / synthesis.rs — must track those; the `HasTag`-only narrowing is
    fail-loud (`unreachable!`).'
- file: crates/holon-integration-tests/src/pbt/block_state.rs
  kind: ref
  covers:
  - 'sibling-order-contract — `children_of` is the encoding-free child-id contract crossing to the SUT
    (no `sort_key`/`sequence` strings). FIDELITY: `sorted_children_of` orders by (sequence, id); those
    sequences are stamped by `assign_reference_sequences_canonical`, whose ordering is a hand-mirror of
    the org re-emission order — see the sibling-order finding.'
  - 'determinism — `BTreeMap` (not `HashMap`) is load-bearing: the canonicalizer derives sequence numbers
    from iteration order.'
- file: crates/holon-integration-tests/src/pbt/clock_state.rs
  kind: ref
  covers:
  - journal-count — `visited_days` cardinality predicts the journal-rule output (one block per distinct
    day; same-day re-tick idempotent). Boot day agrees with the SUT `TestClock` by construction via the
    shared `KEYSTONE_CLOCK_BOOT_MS` constant — no read-back side channel.
- file: crates/holon-integration-tests/src/pbt/composed/builder.rs
  kind: infra
  requires_sut_caps:
  - SutAppLifecycle
  - SutBackend
  - SutBlockCreate
  - SutBlockTreeWrite
  - SutEdgeFieldWrite
  - SutEditorMirrorRead
  - SutEditorMirrorWrite
  - SutFixtureFs
  - SutFocusWrite
  - SutHistory
  - SutLoro
  - SutLoroLog
  - SutLoroTaskState
  - SutMutate
  - SutOrgRead
  - SutQueryResults
  - SutRenderer
  - SutSeamMutate
  - SutSqlProjection
  - SutTemplateInstantiate
  - SutViewControl
  - SutWatchRegister
  env_gates:
  - HOLON_SOAK_SETTLE_MS
  covers:
  - compose-sut-builder — the CapMap-by-subsystem production SUT
- file: crates/holon-integration-tests/src/pbt/composed/catalog.rs
  kind: infra
  feature_gates:
  - otel-testing
  covers:
  - invariant-catalog — the shared CapInvariant catalog assembled as
- file: crates/holon-integration-tests/src/pbt/composed/correspondences.rs
  kind: module
  requires_ref_caps:
  - RefBackend
  - RefBlockTree
  - RefEditorMirror
  - RefHistoryExpectation
  - RefLayout
  requires_sut_caps:
  - SutEditorMirrorRead
  - SutOrgRead
  - SutRenderer
  provides_caps:
  - SutHistory
- file: crates/holon-integration-tests/src/pbt/composed/fixtures.rs
  kind: module
  requires_ref_caps:
  - RefTaskState
  provides_caps:
  - SutBackend
  - SutEditorMirrorRead
  - SutErrorLog
  - SutLoroLog
  - SutLoroTaskState
  - SutSqlProjection
  feature_gates:
  - otel-testing
- file: crates/holon-integration-tests/src/pbt/composed/harness.rs
  kind: infra
  requires_sut_caps:
  - SutBackend
  - SutLayout
  - SutLoroLog
  - SutMetricsLifecycle
  feature_gates:
  - otel-testing
  covers:
  - 'composed-harness — generic ComposedSut StateMachineTest:'
- file: crates/holon-integration-tests/src/pbt/composed/invariants/advice_rows_woven.rs
  kind: invariant-wiring
  requires_ref_caps:
  - RefAdvice
  requires_sut_caps:
  - SutRenderer
  declared_needs:
  - RefAdvice
  - SutRenderer
- file: crates/holon-integration-tests/src/pbt/composed/invariants/companion_has_no_child_page_headings.rs
  kind: invariant-wiring
  requires_ref_caps:
  - RefBlockTree
  requires_sut_caps:
  - SutOrgRender
  declared_needs:
  - RefBlockTree
  - SutOrgRender
- file: crates/holon-integration-tests/src/pbt/composed/invariants/display_placement_canonical_inert.rs
  kind: invariant-wiring
  requires_sut_caps:
  - SutBackend
  - SutOrgRender
  - SutRenderer
  declared_needs:
  - SutBackend
  - SutOrgRender
  - SutRenderer
- file: crates/holon-integration-tests/src/pbt/composed/invariants/displayed_text.rs
  kind: invariant-wiring
  requires_ref_caps:
  - RefBlockTree
  - RefEditorMirror
  requires_sut_caps:
  - SutLayout
  - SutRenderer
  declared_needs:
  - RefBlockTree
  - RefEditorMirror
  - SutLayout
  - SutRenderer
- file: crates/holon-integration-tests/src/pbt/composed/invariants/editable_text_has_draggable.rs
  kind: invariant-wiring
  requires_ref_caps:
  - RefLayout
  requires_sut_caps:
  - SutRenderer
  declared_needs:
  - RefLayout
  - SutRenderer
- file: crates/holon-integration-tests/src/pbt/composed/invariants/embedded_page_collapsed_lazy.rs
  kind: invariant-wiring
  requires_ref_caps:
  - RefBlockTree
  - RefLayout
  - RefToggle
  requires_sut_caps:
  - SutRenderer
  declared_needs:
  - RefBlockTree
  - RefFocus
  - RefLayout
  - RefToggle
  - RefViewSelection
  - SutRenderer
- file: crates/holon-integration-tests/src/pbt/composed/invariants/every_page_has_its_own_file.rs
  kind: invariant-wiring
  requires_ref_caps:
  - RefBlockTree
  requires_sut_caps:
  - SutOrgRender
  declared_needs:
  - RefBlockTree
  - SutOrgRender
- file: crates/holon-integration-tests/src/pbt/composed/invariants/focus_matches_ref.rs
  kind: invariant-wiring
  requires_ref_caps:
  - RefEditorMirror
  - RefGlobalFocus
  requires_sut_caps:
  - SutDriver
  declared_needs:
  - RefEditorMirror
  - RefGlobalFocus
  - SutDriver
- file: crates/holon-integration-tests/src/pbt/composed/invariants/focus_roots.rs
  kind: invariant-wiring
  requires_sut_caps:
  - SutBackend
  declared_needs:
  - RefFocus
  - SutBackend
  - SutFocus
- file: crates/holon-integration-tests/src/pbt/composed/invariants/frontend_bounds_rendered.rs
  kind: invariant-wiring
  requires_ref_caps:
  - RefLayout
  requires_sut_caps:
  - SutFrontendEngine
  - SutLayout
  declared_needs:
  - RefLayout
  - SutFrontendEngine
  - SutLayout
- file: crates/holon-integration-tests/src/pbt/composed/invariants/frontend_engine.rs
  kind: invariant-wiring
  requires_sut_caps:
  - SutFrontendEngine
  declared_needs:
  - SutFrontendEngine
- file: crates/holon-integration-tests/src/pbt/composed/invariants/frontend_no_error_widgets.rs
  kind: invariant-wiring
  requires_sut_caps:
  - SutLayout
  declared_needs:
  - SutLayout
  - SutViewSelection
- file: crates/holon-integration-tests/src/pbt/composed/invariants/frontend_root_not_error.rs
  kind: invariant-wiring
  requires_sut_caps:
  - SutFrontendEngine
  declared_needs:
  - SutFrontendEngine
- file: crates/holon-integration-tests/src/pbt/composed/invariants/live_children_match_ref.rs
  kind: invariant-wiring
  requires_ref_caps:
  - RefBlockTree
  requires_sut_caps:
  - SutLoroLog
  - SutSqlProjection
  declared_needs:
  - RefBlockTree
  - SutLoroLog
  - SutSqlProjection
- file: crates/holon-integration-tests/src/pbt/composed/invariants/live_tree_matches_fresh.rs
  kind: invariant-wiring
  requires_sut_caps:
  - SutFrontendEmissions
  declared_needs:
  - SutFrontendEmissions
- file: crates/holon-integration-tests/src/pbt/composed/invariants/loro_children_match_ref.rs
  kind: invariant-wiring
  requires_ref_caps:
  - RefBlockTree
  requires_sut_caps:
  - SutLoroLog
- file: crates/holon-integration-tests/src/pbt/composed/invariants/loro_no_errors.rs
  kind: invariant-wiring
  requires_sut_caps:
  - SutLoroLog
- file: crates/holon-integration-tests/src/pbt/composed/invariants/main_panel_rows_match_focus.rs
  kind: invariant-wiring
  requires_ref_caps:
  - RefLayout
  requires_sut_caps:
  - SutRenderer
  declared_needs:
  - RefFocus
  - RefLayout
  - RefViewSelection
  - SutRenderer
- file: crates/holon-integration-tests/src/pbt/composed/invariants/no_errors.rs
  kind: invariant-wiring
  requires_sut_caps:
  - SutErrorLog
  declared_needs:
  - SutErrorLog
- file: crates/holon-integration-tests/src/pbt/composed/invariants/no_orphan.rs
  kind: invariant-wiring
  requires_sut_caps:
  - SutBackend
  declared_needs:
  - SutBackend
- file: crates/holon-integration-tests/src/pbt/composed/invariants/no_page_under_non_page.rs
  kind: invariant-wiring
  requires_ref_caps:
  - RefBlockTree
  declared_needs:
  - RefBlockTree
- file: crates/holon-integration-tests/src/pbt/composed/invariants/no_parent_cycles.rs
  kind: invariant-wiring
  requires_sut_caps:
  - SutBackend
  declared_needs:
  - SutBackend
- file: crates/holon-integration-tests/src/pbt/composed/invariants/org_render_fixed_point.rs
  kind: invariant-wiring
  requires_sut_caps:
  - SutOrgRender
  declared_needs:
  - SutOrgRender
- file: crates/holon-integration-tests/src/pbt/composed/invariants/sidebar_page_tag_preserved.rs
  kind: invariant-wiring
  requires_ref_caps:
  - RefBlockTree
  requires_sut_caps:
  - SutBackend
  declared_needs:
  - RefBlockTree
  - SutBackend
- file: crates/holon-integration-tests/src/pbt/composed/invariants/source_language.rs
  kind: invariant-wiring
  requires_sut_caps:
  - SutBackend
  declared_needs:
  - SutBackend
- file: crates/holon-integration-tests/src/pbt/composed/invariants/task_state_storage_coherence.rs
  kind: invariant-wiring
  requires_ref_caps:
  - RefTaskState
  requires_sut_caps:
  - SutLoroTaskState
  - SutSqlProjection
  declared_needs:
  - RefTaskState
  - SutLoroTaskState
  - SutSqlProjection
- file: crates/holon-integration-tests/src/pbt/composed/invariants/undo_redo_reference_heal.rs
  kind: invariant-wiring
  requires_ref_caps:
  - RefUndoRedoBurned
  requires_sut_caps:
  - SutBackend
  - SutFocus
  - SutSqlProjection
  declared_needs:
  - RefUndoRedoBurned
  - SutBackend
  - SutFocus
  - SutSqlProjection
- file: crates/holon-integration-tests/src/pbt/composed/invariants/value_fn_provider_arg_variance_13.rs
  kind: invariant-wiring
  requires_ref_caps:
  - RefGlobalFocus
  - RefLayout
  requires_sut_caps:
  - SutFrontendEmissions
  declared_needs:
  - RefGlobalFocus
  - RefLayout
  - SutFrontendEmissions
- file: crates/holon-integration-tests/src/pbt/composed/invariants/value_fn_provider_identity.rs
  kind: invariant-wiring
  requires_ref_caps:
  - RefBlockTree
  - RefTaskState
  requires_sut_caps:
  - SutFrontendEmissions
  declared_needs:
  - RefBlockTree
  - RefTaskState
  - SutFrontendEmissions
- file: crates/holon-integration-tests/src/pbt/composed/invariants/viewmodel_decompiled_rows_match_query.rs
  kind: invariant-wiring
  requires_sut_caps:
  - SutQueryResults
  - SutRenderer
  declared_needs:
  - RefViewSelection
  - SutQueryResults
  - SutRenderer
- file: crates/holon-integration-tests/src/pbt/composed/invariants/viewmodel_editable_text_triggers.rs
  kind: invariant-wiring
  requires_sut_caps:
  - SutRenderer
  declared_needs:
  - SutRenderer
- file: crates/holon-integration-tests/src/pbt/composed/invariants/viewmodel_entity_ids_subset_of_data.rs
  kind: invariant-wiring
  requires_ref_caps:
  - RefLayout
  requires_sut_caps:
  - SutRenderer
  declared_needs:
  - RefLayout
  - RefViewSelection
  - SutRenderer
- file: crates/holon-integration-tests/src/pbt/composed/invariants/viewmodel_root_matches_render_expr.rs
  kind: invariant-wiring
  requires_sut_caps:
  - SutRenderer
  declared_needs:
  - RefViewSelection
  - SutRenderer
- file: crates/holon-integration-tests/src/pbt/composed/invariants/viewmodel_shows_source_when_no_query.rs
  kind: invariant-wiring
  requires_sut_caps:
  - SutQueryResults
  - SutRenderer
  declared_needs:
  - SutQueryResults
  - SutRenderer
- file: crates/holon-integration-tests/src/pbt/composed/invariants/viewmodel_snapshot.rs
  kind: invariant-wiring
  requires_sut_caps:
  - SutRenderer
  declared_needs:
  - SutRenderer
- file: crates/holon-integration-tests/src/pbt/composed/invariants/viewmodel_state_toggle_correct.rs
  kind: invariant-wiring
  requires_ref_caps:
  - RefBlockTree
  - RefTaskState
  requires_sut_caps:
  - SutRenderer
  declared_needs:
  - RefBlockTree
  - RefTaskState
  - SutRenderer
- file: crates/holon-integration-tests/src/pbt/composed/invariants/viewmodel_tree_virtual_slots.rs
  kind: invariant-wiring
  requires_ref_caps:
  - RefBlockTree
  requires_sut_caps:
  - SutRenderer
  declared_needs:
  - RefBlockTree
  - SutRenderer
- file: crates/holon-integration-tests/src/pbt/composed/invariants/window_focus.rs
  kind: invariant-wiring
  requires_sut_caps:
  - SutDriver
  - SutLayout
  declared_needs:
  - SutDriver
  - SutLayout
- file: crates/holon-integration-tests/src/pbt/composed/invariants.rs
  kind: invariant
  feature_gates:
  - otel-testing
- file: crates/holon-integration-tests/src/pbt/composed/live_mcp.rs
  kind: infra
  provides_caps:
  - SutBackend
  - SutBlockTreeWrite
  - SutEditorMirrorWrite
  - SutFocusWrite
  - SutLoroLog
  - SutOrgRead
  - SutOrgRender
  - SutQuiesce
  - SutSqlProjection
  covers:
  - live-mcp-slice — out-of-process twin of the keystone over a live
- file: crates/holon-integration-tests/src/pbt/composed/observed_errors.rs
  kind: module
  invariant_ids:
  - inv-no-observed-errors
- file: crates/holon-integration-tests/src/pbt/composed/reseed_observer.rs
  kind: module
  invariant_ids:
  - inv-no-steady-reseed-leak
  env_gates:
  - HOLON_PBT_RESEED_ORACLE
- file: crates/holon-integration-tests/src/pbt/composed/span_metrics.rs
  kind: module
  provides_caps:
  - SutMetricsLifecycle
- file: crates/holon-integration-tests/src/pbt/composed/subsystem_seed.rs
  kind: module
  requires_ref_caps:
  - RefFocusMut
- file: crates/holon-integration-tests/src/pbt/composed/wide_e2e.rs
  kind: slice
  requires_sut_caps:
  - SutDriver
  - SutFrontendEmissions
  - SutFrontendEngine
  - SutHistoryWrite
  - SutLayout
  - SutLoro
  - SutMetricsLifecycle
  - SutNavHistoryDrive
  - SutNavHistoryWrite
  - SutViewControl
  env_gates:
  - HOLON_FOLDER_COMPANION_SEED
  - HOLON_PBT_FORCE_FULL
  - HOLON_PBT_PIN_WIRING
  feature_gates:
  - otel-testing
  - pbt
  covers:
  - 'wide-e2e-swap — the composed general_e2e slice: production'
- file: crates/holon-integration-tests/src/pbt/composed/windowed.rs
  kind: infra
  covers:
  - windowed-composed-check — single-sourced E4 windowed composition
- file: crates/holon-integration-tests/src/pbt/driver_input.rs
  kind: sut-component
  provides_caps:
  - SutBlockInteract
  - SutDriver
  gen:
  - NOT a generator — no input distribution lives here; every gesture (click/drag/arrow/slash/keystroke)
    forwards verbatim to the production `UserDriver`. Which ids, regions, keys, and step-counts are exercised
    is decided entirely by the block-interaction / arrow-navigate transition generators upstream. `resolver`
    remaps oracle ids to SUT-minted ids (headless composed only) so a gesture never drives a ghost block.
- file: crates/holon-integration-tests/src/pbt/file_adapter_state.rs
  kind: ref
  covers:
  - file-adapter-state — `doc_uri → filename` map (doc identity = its keyset, single source of truth)
    plus pre-boot file/VCS flags. An adapter concern, not domain identity.
- file: crates/holon-integration-tests/src/pbt/fixtures/assert.rs
  kind: module
  requires_sut_caps:
  - SutRenderer
- file: crates/holon-integration-tests/src/pbt/fixtures/mod.rs
  kind: module
  requires_ref_caps:
  - RefLifecycle
  env_gates:
  - HOLON_PBT_FORCE_FAIL_AT_STEP
- file: crates/holon-integration-tests/src/pbt/frontend_slice/block_query_component.rs
  kind: sut-arm
  provides_caps:
  - SutRenderer
- file: crates/holon-integration-tests/src/pbt/frontend_slice/builders.rs
  kind: module
  requires_sut_caps:
  - SutQueryResults
  - SutSqlProjection
- file: crates/holon-integration-tests/src/pbt/frontend_slice/components.rs
  kind: sut-arm
  requires_sut_caps:
  - SutBlockTreeWrite
  - SutFixtureFs
  provides_caps:
  - SutAdviceMatview
  - SutAppLifecycle
  - SutBackend
  - SutBlockCreate
  - SutClockAdvance
  - SutEditorMirrorRead
  - SutEditorMirrorWrite
  - SutErrorLog
  - SutFocusWrite
  - SutHistory
  - SutHistoryWrite
  - SutMcpEmit
  - SutMutate
  - SutNavHistoryDrive
  - SutNavHistoryWrite
  - SutOrgRead
  - SutOrgRender
  - SutQueryResults
  - SutRenderer
  - SutSeamMutate
  - SutSqlProjection
  - SutViewControl
  - SutWatchRegister
  env_gates:
  - HOLON_PBT_DISPLAY_PLACED
  - HOLON_SOAK_SETTLE_MS
  covers:
  - frontend-slice — real headless `FrontendSession` +
- file: crates/holon-integration-tests/src/pbt/frontend_slice/integration_tests.rs
  kind: module
  requires_sut_caps:
  - SutOrgRead
  - SutOrgRender
  - SutRenderer
- file: crates/holon-integration-tests/src/pbt/frontend_slice/navigation_pbt.rs
  kind: module
  requires_sut_caps:
  - SutBackend
  - SutFocusWrite
  - SutHistoryWrite
  - SutMcpEmit
  - SutNavHistoryDrive
  - SutNavHistoryWrite
  - SutOrgRead
  - SutOrgRender
  - SutRenderer
  - SutSqlProjection
  - SutViewControl
  - SutWatchRegister
- file: crates/holon-integration-tests/src/pbt/frontend_slice/structural_pbt.rs
  kind: module
  requires_ref_caps:
  - RefNavHistoryMut
  - RefToggleMut
  requires_sut_caps:
  - SutBackend
  - SutBlockTreeWrite
  - SutEditorMirrorRead
  - SutFocusWrite
  - SutLoro
  - SutLoroLog
  - SutOrgRead
  - SutQueryResults
  - SutRenderer
  - SutSqlProjection
- file: crates/holon-integration-tests/src/pbt/frontend_slice.rs
  kind: slice-mod
  covers:
  - frontend-slice — modules + builders for the headless FE arm.
- file: crates/holon-integration-tests/src/pbt/generators.rs
  kind: generator
  env_gates:
  - HOLON_PBT_EXTENDED_GEN
  - HOLON_PBT_SHARED_TREE_MOUNT
  gen:
  - 'content strategies (content/edit/typing/bulk) mix the multi-byte / empty / whitespace / org-special
    `extended_content_arm` UNCONDITIONALLY at ~40-50% — it is NOT behind HOLON_PBT_EXTENDED_GEN. Only
    three arms are env-gated: file-tree nesting (parent-selector), GQL query language (generate_query_language),
    and the profile-in-no-override file arm. (Prior header claim that byte-offset classes are ASCII-hidden
    by default was stale — those classes ARE in the default gate.)'
  - file-tree is FLAT by default (every heading parents to doc root); the parent-selector arm (heading
    i → sel % (i+1)) only fires under HOLON_PBT_EXTENDED_GEN, so headline-depth / nested-page bug classes
    are NOT reached by the default gate (runtime nesting only via Create/mutation)
  - 'ADVICE_TAG_POOL couples two floors: rule-block presence IS floored (advice_rule_arm_reachable_in_default_mix
    ~20/200) but the tag COLLISION that makes advice_expectation nonempty is NOT — a SetEdgeField weight/pool
    change can silently drop advice invariants to the exp-empty vacuous path'
  covers:
  - content-vocabulary — shared ADVICE_TAG_POOL keeps advice invariants non-vacuous (anchor/candidate
    tag collisions)
- file: crates/holon-integration-tests/src/pbt/invariant_mode_override.rs
  kind: infra
  env_gates:
  - HOLON_PBT_INVARIANTS
  covers:
  - invariant-softening-knob — HOLON_PBT_INVARIANTS env-only
- file: crates/holon-integration-tests/src/pbt/invariants/bodies/advice_rows_woven.rs
  kind: invariant
  invariant_ids:
  - inv-advice-rows-woven
  requires_ref_caps:
  - RefAdvice
  requires_sut_caps:
  - SutRenderer
  slips-if-removed:
  - renderer weaves a suppressed lesson, the wrong top-K, or ascending-score order under an anchor; the
    advice section shows wrong recommendations and nothing else observes it
  oracle:
  - correspondence
  covers:
  - advice-weave — top-K suppression-filtered lessons per anchor, in non-increasing score order with top-K
    boundary dominance, vs the ref expectation
- file: crates/holon-integration-tests/src/pbt/invariants/bodies/block_ids_match_ref.rs
  kind: invariant
  invariant_ids:
  - inv-block-ids-match-ref
  requires_ref_caps:
  - RefBlockTree
  requires_sut_caps:
  - SutSqlProjection
  slips-if-removed:
  - a block created in the ref never reaches SQL (or a ghost row survives a delete); the block is silently
    missing from / extra in every SQL-backed query and panel
  oracle:
  - correspondence
  covers:
  - block-tree drift — SQL projection block-id set vs ref non-seed block ids (coarse set equality)
- file: crates/holon-integration-tests/src/pbt/invariants/bodies/block_tags_references_exist.rs
  kind: invariant
  invariant_ids:
  - inv-block-tags-references-exist
  requires_sut_caps:
  - SutSqlProjection
  slips-if-removed:
  - a block DELETE that leaves its block_tags rows behind (cascade not wired) creates orphan junction
    rows that resurface phantom tags in tag-driven queries
  oracle:
  - internal-consistency
  covers:
  - junction referential integrity — block_tags.block_id orphaned w.r.t. block_raw (no DB-level FK in
    Turso IVM mode)
- file: crates/holon-integration-tests/src/pbt/invariants/bodies/companion_has_no_child_page_headings.rs
  kind: invariant
  invariant_ids:
  - inv-companion-has-no-child-page-headings
  requires_ref_caps:
  - RefBlockTree
  requires_sut_caps:
  - SutOrgRender
  slips-if-removed:
  - companion writeback keeps a child page inlined (or a leftover backlink line); the page is double-represented
    and a store-rebuild-from-disk re-ingests it under the wrong parent
  oracle:
  - correspondence
  covers:
  - writeback de-inline — a folder-companion `.org` retaining a heading for a block the ref models as
    a `Page` doc-root (is_page_block)
- file: crates/holon-integration-tests/src/pbt/invariants/bodies/display_placement_canonical_inert.rs
  kind: invariant
  invariant_ids:
  - inv-display-placement-canonical-inert
  requires_sut_caps:
  - SutBackend
  - SutOrgRender
  - SutRenderer
  env_gates:
  - HOLON_PBT_DISPLAY_PLACED
  slips-if-removed:
  - a placement path that writes to SQL/Loro/org instead of staying render-only corrupts canonical state
    while the row still displays
  oracle:
  - sut-internal — the display-placed injection is a post-snapshot widget-tree node with no reference
    projection, so inertness is proven by SUT canonical self-reads (org fixed-point + placed-id-is-real),
    not a ref
  covers:
  - display-placement inertness — a display-placed widget node must not perturb SQL / Loro / org canonical
    state
- file: crates/holon-integration-tests/src/pbt/invariants/bodies/displayed_text.rs
  kind: invariant
  invariant_ids:
  - inv-displayed-text/viewmodel
  - inv-displayed-text/widget
  requires_ref_caps:
  - RefBlockTree
  - RefEditorMirror
  requires_sut_caps:
  - SutLayout
  - SutRenderer
  slips-if-removed:
  - after split/join the row's data signal fires but the editable_text InputState or VM `content` prop
    skips the update; the user sees stale text on screen
  oracle:
  - correspondence
  covers:
  - displayed-text staleness — text-widget / ViewModel content vs ref block content (or the live editor
    text while editing)
- file: crates/holon-integration-tests/src/pbt/invariants/bodies/editable_text_has_draggable.rs
  kind: invariant
  invariant_ids:
  - inv-editable-text-has-draggable
  requires_ref_caps:
  - RefLayout
  requires_sut_caps:
  - SutRenderer
  slips-if-removed:
  - a render path emits editable_text without its draggable wrapper (row_id None); drag&drop silently
    breaks for those rows
  oracle:
  - internal-consistency — SUT widget-tree pairing check; the ref cap gates a skip only (`has_blocks_profile`),
    it is not compared against
  covers:
  - drag-affordance pairing — every editable_text/rendered_text in a block-profile subtree paired with
    a same-id draggable
- file: crates/holon-integration-tests/src/pbt/invariants/bodies/embedded_page_collapsed_lazy.rs
  kind: invariant
  invariant_ids:
  - inv-embedded-page-collapsed-lazy
  requires_ref_caps:
  - RefBlockTree
  - RefLayout
  - RefToggle
  requires_sut_caps:
  - SutRenderer
  slips-if-removed:
  - a collapsed embedded page leaks its children into the main panel (lazy gate bypassed → unbounded eager
    load) or renders with no expand_toggle affordance
  oracle:
  - correspondence
  covers:
  - lazy embedded-page gating — expand_toggle presence and collapsed→no-descendants-in-panel vs the ref
    expansion state
- file: crates/holon-integration-tests/src/pbt/invariants/bodies/every_page_has_its_own_file.rs
  kind: invariant
  invariant_ids:
  - inv-every-page-has-its-own-file
  provides_caps:
  - RefBlockTree
  - SutOrgRender
  slips-if-removed:
  - a nested page stays store-only (fileless, row 137) and vanishes on store-rebuild-from-disk, or double-homes
    across two files and splits its identity
  oracle:
  - correspondence
  covers:
  - page materialization — every ref `Page` doc-root owns exactly one `#+ID:`-rooted file (not fileless,
    not double-homed)
- file: crates/holon-integration-tests/src/pbt/invariants/bodies/focus_matches_ref.rs
  kind: invariant
  invariant_ids:
  - inv-focus-matches-ref
  requires_ref_caps:
  - RefEditorMirror
  - RefGlobalFocus
  requires_sut_caps:
  - SutDriver
  slips-if-removed:
  - a focus op leaves the engine focused on the wrong block or unfocused (steal-back / lost-focus); keyboard
    input routes to the wrong row
  oracle:
  - correspondence
  covers:
  - global focus tracking — engine `focused_block` vs ref global focus (resolved) after a focus-changing
    transition
- file: crates/holon-integration-tests/src/pbt/invariants/bodies/focus_roots.rs
  kind: invariant
  invariant_ids:
  - inv-focus-roots
  requires_sut_caps:
  - SutBackend
  slips-if-removed:
  - a close-path that fails to set closed_at leaves a stale root in the matview; the region keeps focusing
    the old subtree, the main panel shows the wrong page, and no other invariant notices
  oracle:
  - correspondence
  covers:
  - focus-root-drift — per-region open-focus-root set (nav_history → focus_roots matview) vs ref expected
    roots
- file: crates/holon-integration-tests/src/pbt/invariants/bodies/frontend_bounds_rendered.rs
  kind: invariant
  invariant_ids:
  - inv-frontend-bounds-rendered
  requires_ref_caps:
  - RefLayout
  requires_sut_caps:
  - SutFrontendEngine
  - SutLayout
  slips-if-removed:
  - a render-expr that emits a widget the gpui window never lays out (zero-size / off-screen / silently
    dropped) ships a blank or mis-ordered panel that headless VM-only checks cannot see
  oracle:
  - internal-consistency
  covers:
  - render-layout-coherence — VM-emitted entities actually laid out (sizes, no error widgets, y-order/contiguity,
    not visually empty); RefLayout only gates the content-presence sub-checks
- file: crates/holon-integration-tests/src/pbt/invariants/bodies/frontend_engine.rs
  kind: invariant
  invariant_ids:
  - inv-frontend-engine
  requires_sut_caps:
  - SutFrontendEngine
  slips-if-removed:
  - a matview/CDC/waker stall that leaves the root perpetually `loading` ships a permanently blank window
    that the headless engine, having no live window, never exercises
  oracle:
  - internal-consistency
  covers:
  - render-liveness — the gpui window's ReactiveEngine resolves the root layout to a settled (non-loading)
    ViewModel
- file: crates/holon-integration-tests/src/pbt/invariants/bodies/frontend_no_error_widgets.rs
  kind: invariant
  invariant_ids:
  - inv-frontend-no-error-widgets
  requires_sut_caps:
  - SutLayout
  slips-if-removed:
  - a render expression that evaluates to an error node (bad query, missing field, interp panic) renders
    a red error card in the live window where the user would see it but no assertion fires
  oracle:
  - internal-consistency
  covers:
  - no-error-widgets — no Error widget in the laid-out BoundsRegistry (authoritative) or, absent geometry,
    the ViewModel tree
- file: crates/holon-integration-tests/src/pbt/invariants/bodies/frontend_root_not_error.rs
  kind: invariant
  invariant_ids:
  - inv-frontend-root-not-error
  requires_sut_caps:
  - SutFrontendEngine
  slips-if-removed:
  - a broken root render-expr collapses the entire window to a single error node; the whole app is unusable
    yet no correspondence check runs because there is no valid tree to compare
  oracle:
  - internal-consistency
  covers:
  - root-not-error — the frontend root ViewModel node is not the Error variant
- file: crates/holon-integration-tests/src/pbt/invariants/bodies/live_children_match_ref.rs
  kind: invariant
  invariant_ids:
  - inv-live-children-match-ref
  requires_ref_caps:
  - RefBlockTree
  requires_sut_caps:
  - SutLoroLog
  - SutSqlProjection
  slips-if-removed:
  - org-ingested block whose Loro fractional index never reaches SQL sort_key (stuck at default "A0")
    silently mis-sorts against moved siblings; UI shows wrong order, nothing else observes it
  oracle:
  - correspondence
  covers:
  - sibling-order — SQL projection sort_key order vs ref document order, per parent, non-seed blocks only
- file: crates/holon-integration-tests/src/pbt/invariants/bodies/live_tree_matches_fresh.rs
  kind: invariant
  invariant_ids:
  - inv-live-tree-matches-fresh
  requires_sut_caps:
  - SutFrontendEmissions
  slips-if-removed:
  - the collection driver's set_data fails to push an updated prop (state_toggle checked, editable_text
    value) to an existing child widget; GPUI shows stale content while the fresh tree is correct
  oracle:
  - 'metamorphic — SUT-internal (no ref): the incremental `set_data` result must equal a fresh interpretation
    of the same rows; no ref projection of the live collection-driver path exists, so the fresh rebuild
    IS the oracle for the props children should see'
  covers:
  - set-data-prop-propagation — live vs fresh per-item prop diff
- file: crates/holon-integration-tests/src/pbt/invariants/bodies/main_panel_rows_match_focus.rs
  kind: invariant
  invariant_ids:
  - inv-main-panel-rows-match-focus
  requires_ref_caps:
  - RefLayout
  requires_sut_caps:
  - SutRenderer
  slips-if-removed:
  - a CDC delete that fails to propagate through the focus_roots→main-panel chained matview leaves the
    previous root's rows in the panel after navigation (dioxus w4-web-05, GPUI stale sidebar)
  oracle:
  - correspondence
  covers:
  - stale-rows-on-nav — ref-known main-panel rows must ⊆ Main's current focus-root subtree (ref.expected_visible_content_ids)
- file: crates/holon-integration-tests/src/pbt/invariants/bodies/mod.rs
  kind: invariant
- file: crates/holon-integration-tests/src/pbt/invariants/bodies/no_errors.rs
  kind: invariant
  invariant_ids:
  - inv-no-errors
  requires_sut_caps:
  - SutErrorLog
  slips-if-removed:
  - a DDL/sync race that logs a publish error during initial document sync ships silent data-loss on boot;
    the UI may look fine while a document failed to sync and nothing else asserts on it
  oracle:
  - internal-consistency
  covers:
  - app-error-log-empty — no Flutter/event publish error logged since startup (the app-level counter,
    distinct from widget-tree errors)
- file: crates/holon-integration-tests/src/pbt/invariants/bodies/no_orphan_blocks.rs
  kind: invariant
  invariant_ids:
  - inv-no-orphan-blocks
  requires_sut_caps:
  - SutBackend
  slips-if-removed:
  - the projection drops a parent node while keeping its children; the children become unreachable in
    the tree and silently vanish from every view rooted above the lost node
  oracle:
  - internal-consistency
  covers:
  - no-orphan-blocks — every non-root block's parent_id resolves to a block present in the same matview
    snapshot
- file: crates/holon-integration-tests/src/pbt/invariants/bodies/no_page_under_non_page.rs
  kind: invariant
  invariant_ids:
  - inv-no-page-under-non-page
  provides_caps:
  - RefBlockTree
  slips-if-removed:
  - a generator/seed change reparents a page under a non-page; DocumentManager::name_chain bails deep
    in writeback instead of a clean keystone RED at the ref boundary
  oracle:
  - internal-consistency — ref-side structural tripwire on the generator/seed guarantee (walks the ref
    parent chain; reads no SUT)
  covers:
  - page-hierarchy — a page nested under a non-page block
- file: crates/holon-integration-tests/src/pbt/invariants/bodies/no_parent_cycles.rs
  kind: invariant
  invariant_ids:
  - inv-no-parent-cycles
  requires_sut_caps:
  - SutBackend
  slips-if-removed:
  - a mutation-guard regression lets a block become its own ancestor; projection walks / render recursion
    spin forever and no other oracle observes the cycle
  oracle:
  - internal-consistency — the SUT parent chain terminates at a root without revisiting a node (structural,
    no ref)
  covers:
  - parent-cycle — a write path bypassing BlockMutation::validate / tree.mov admits a cyclic parent_id
    into the projection
- file: crates/holon-integration-tests/src/pbt/invariants/bodies/org_render_fixed_point.rs
  kind: invariant
  invariant_ids:
  - inv-org-render-fixed-point
  requires_sut_caps:
  - SutOrgRender
  slips-if-removed:
  - 'a property-drawer / sibling round-trip asymmetry (e.g. :share-role: mount) diverges render from disk;
    the app spins in an FSEvent echo loop the blocks-match-ref family never sees'
  oracle:
  - roundtrip — render(SQL) == on-disk bytes fixed point (bounded wait for a STABLE fixed point to reject
    transient projection lag)
  covers:
  - org-echo-loop — render != disk that PERSISTS, forcing re_render_all_tracked to keep rewriting the
    file and re-firing FSEvents
- file: crates/holon-integration-tests/src/pbt/invariants/bodies/sidebar_page_tag_preserved.rs
  kind: invariant
  invariant_ids:
  - inv-sidebar-page-tag-preserved
  requires_ref_caps:
  - RefBlockTree
  requires_sut_caps:
  - SutBackend
  slips-if-removed:
  - a folder-companion .org inlines a page-file doc-root as a plain heading, stripping its `Page` tag
    on cold-boot reconcile; the page vanishes from the sidebar and the id-set compare can't see it
  oracle:
  - correspondence — SUT live-projection is_page vs ref is_page_block, over ids present in the SUT snapshot
  covers:
  - page-demotion — a Page doc-root whose `Page` tag is stripped in the SUT projection while its block
    row survives
- file: crates/holon-integration-tests/src/pbt/invariants/bodies/source_language_iff_source.rs
  kind: invariant
  invariant_ids:
  - inv-source-language-iff-source
  requires_sut_caps:
  - SutBackend
  slips-if-removed:
  - an adapter projection corrupts source_language; a Source block renders with no language (or a Text
    block sprouts one) and no differential oracle flags it
  oracle:
  - internal-consistency — source_language present iff content_type is Source, over the SUT write-side
    snapshot (no ref)
  covers:
  - source-lang-projection — a Source row that lost its language or a Text/Image row that grew one
- file: crates/holon-integration-tests/src/pbt/invariants/bodies/sql_budget.rs
  kind: invariant
  invariant_ids:
  - inv-sql-budget
  feature_gates:
  - otel-testing
  slips-if-removed:
  - a change adds an O(N) query-per-row or a full reprojection to a hot transition; latency silently regresses
    past the SLO with no failing test (only fires when HOLON_PERF_BUDGET enforces)
  oracle:
  - budget — per-transition SQL read/write/DDL + wall + RSS counts vs an expected budget (canonical id
    home; body dispatched via composed::span_metrics::InvComposedBudget)
  covers:
  - perf-regression — a transition issuing N+1 / over-budget SQL or blowing the wall / memory ceiling
- file: crates/holon-integration-tests/src/pbt/invariants/bodies/task_state_storage_coherence.rs
  kind: invariant
  invariant_ids:
  - inv-task-state-storage-coherence
  requires_ref_caps:
  - RefTaskState
  requires_sut_caps:
  - SutLoroTaskState
  - SutSqlProjection
  slips-if-removed:
  - a CDC / tag-projection bug lets a store drift from the canonical task_state; a render reading it shows
    a checkbox the model contradicts
  oracle:
  - 'correspondence — each block''s task_state in BOTH SUT stores (SQL `block_raw.properties` and the
    Loro tag projection) is compared to the REFERENCE (`RefTaskState::task_state_of`). Store-to-store
    coherence falls out: if both equal the ref, they equal each other. Re-anchored from the former SUT↔SUT
    comparison (F4) so a shared enrichment/CDC bug writing the same wrong value to BOTH stores can no
    longer stay green.'
  covers:
  - loro-sql-desync — either store disagrees with the ref (and so, transitively, with the other) on presence
    or value of task_state
- file: crates/holon-integration-tests/src/pbt/invariants/bodies/undo_redo_reference_heal.rs
  kind: invariant
  invariant_ids:
  - inv-undo-redo-reference-heal
  requires_ref_caps:
  - RefUndoRedoBurned
  requires_sut_caps:
  - SutBackend
  - SutFocus
  - SutSqlProjection
  gates:
  - HOLON_PBT_UNDO_REDO_HEAL
  slips-if-removed:
  - a redo brings a block back under a new id while every reference to its pre-undo id stays pointed
    at the burned uuid — the reference dangles permanently, and the DIAGNOSIS is lost (inv-focus-roots
    also fires when the site is a pin, but reports only a focus-root set mismatch)
  oracle:
  - roundtrip — on a COMPLETED undo→redo round trip (redo-gated burned-id set non-empty), no base-table
    reference site may still name the burned id. Reference-prediction-free — compares the SUT against
    the harness reconcile's burned record, not against an oracle projection.
  covers:
  - undo-redo-reference-heal — references to a block whose identity a `Redo` re-minted (undo deletes
    the tail, redo re-executes the forward op and mints a FRESH uuid)
- file: crates/holon-integration-tests/src/pbt/invariants/bodies/value_fn_provider_arg_variance_13.rs
  kind: invariant
  invariant_ids:
  - inv-value-fn-provider-arg-variance-13
  requires_ref_caps:
  - RefGlobalFocus
  - RefLayout
  requires_sut_caps:
  - SutFrontendEmissions
  slips-if-removed:
  - a provider-cache regression re-allocates identities each interpret pass (or yields no focus rows under
    a focus_chain expr); the UI re-mounts / flickers streamed widgets with no other signal
  oracle:
  - internal-consistency — structural well-formedness of the SUT ProviderStabilityReport (bottom_dock
    presence, provider rows, cache identity); vfn13 is metamorphic (pass-1 identities reappear in pass-2)
  covers:
  - provider-cache-wiring — the ReactiveEngine / interpret_pure / ProviderCache coupling drops rows, churns
    Arc identity, or flickers
- file: crates/holon-integration-tests/src/pbt/invariants/bodies/value_fn_provider_identity.rs
  kind: invariant
  invariant_ids:
  - inv-value-fn-provider-identity
  requires_ref_caps:
  - RefBlockTree
  - RefTaskState
  requires_sut_caps:
  - SutFrontendEmissions
  slips-if-removed:
  - flatten_properties mishandles a CDC Value::String and a checkbox flashes the wrong state mid-transition;
    the settled frame is correct so a post-settle-only oracle never sees it
  oracle:
  - correspondence — each intermediate ViewModel StateToggle.current vs the ref task_state, over drained
    emission toggles
  covers:
  - cdc-enrichment-glitch — a transient wrong StateToggle in an intermediate emission that a later structural
    re-render masks
- file: crates/holon-integration-tests/src/pbt/invariants/bodies/viewmodel_decompiled_rows_match_query.rs
  kind: invariant
  invariant_ids:
  - inv-viewmodel-decompiled-rows-match-query
  requires_sut_caps:
  - SutRenderer
  slips-if-removed:
  - interpret_pure drops or reorders a data row; the list silently shows fewer rows than the query returned
  oracle:
  - 'sut-internal — SUT decompiled rendered `content` vs SUT query data_rows `content` (ordered equality,
    filtered to ref visible_columns); DOCTRINE-SUSPECT: ref models no interpret_pure display tree, so
    the query result is the closest in-SUT ground truth and no ref render exists'
  covers:
  - row-drop — the interpreter renders an ordered SUBSET of the query rows (a dropped row the old subset-only
    check let pass)
- file: crates/holon-integration-tests/src/pbt/invariants/bodies/viewmodel_editable_text_triggers.rs
  kind: invariant
  invariant_ids:
  - inv-viewmodel-editable-text-triggers
  requires_sut_caps:
  - SutRenderer
  slips-if-removed:
  - an editable_text with delete/set_field ops but empty triggers ships; typing never fires on_text_changed,
    so the slash-menu / wiki-link popups and bound ops are unreachable from input
  oracle:
  - internal-consistency
  covers:
  - 'editable-text-trigger-wiring — render-DSL regression: editor has bound ops but no input triggers'
- file: crates/holon-integration-tests/src/pbt/invariants/bodies/viewmodel_entity_ids_subset_of_data.rs
  kind: invariant
  invariant_ids:
  - inv-viewmodel-entity-ids-subset-of-data
  requires_ref_caps:
  - RefLayout
  requires_sut_caps:
  - SutRenderer
  slips-if-removed:
  - a render bug mints a widget with a fabricated or stale entity id (leftover peer row, mis-resolved
    uri); the UI shows a ghost row pointing at a non-existent block, nothing else observes it
  oracle:
  - correspondence
  covers:
  - phantom-entity — a rendered entity id that is neither a root query-data row nor a ref-known block
- file: crates/holon-integration-tests/src/pbt/invariants/bodies/viewmodel_no_error_widgets.rs
  kind: invariant
  invariant_ids:
  - inv-viewmodel-no-error-widgets
  slips-if-removed:
  - a CDC/interpret failure renders Error placeholders in the user-visible tree; the app looks structurally
    fine but shows error boxes and no oracle flags it
  oracle:
  - internal-consistency
  covers:
  - error-widget-in-tree — render-pipeline fault (matview fault, CDC delivery bug, shadow-interp panic)
    leaves Error nodes in the tree
- file: crates/holon-integration-tests/src/pbt/invariants/bodies/viewmodel_root_matches_render_expr.rs
  kind: invariant
  invariant_ids:
  - inv-viewmodel-root-matches-render-expr
  requires_sut_caps:
  - SutRenderer
  slips-if-removed:
  - the engine renders the wrong widget kind at the root / main panel (content missing or wrong view mode);
    the user sees wrong or absent primary content
  oracle:
  - correspondence
  covers:
  - root-render-misplacement — active content render expr not at the root (layout-less) or inside the
    main panel (3-column)
- file: crates/holon-integration-tests/src/pbt/invariants/bodies/viewmodel_shows_source_when_no_query.rs
  kind: invariant
  invariant_ids:
  - inv-viewmodel-shows-source-when-no-query
  requires_sut_caps:
  - SutRenderer
  slips-if-removed:
  - without a query engine the app attempts a full-mode render of a query block and shows an error/blank
    instead of the source editor; the degraded-mode contract silently breaks
  oracle:
  - sut-internal — ref is cap-blind to the storage backend and would predict the full-mode render, so
    a ref comparison is a guaranteed false Fail; the expected value is fixed by the sut_absent-SutQueryResults
    selection
  covers:
  - degradation-regression — no-Turso wiring fails to degrade a query-source block to the bare `source_editor`
    view (ADR 0004 Phase 9)
- file: crates/holon-integration-tests/src/pbt/invariants/bodies/viewmodel_snapshot.rs
  kind: invariant
  invariant_ids:
  - inv-viewmodel-snapshot
  requires_sut_caps:
  - SutRenderer
  slips-if-removed:
  - a render-pipeline fault makes the whole root layout an error widget; the entire screen is an error
    box, unobserved
  oracle:
  - internal-consistency
  covers:
  - root-error-widget — headless interpret_pure pipeline yields an `error` root layout
- file: crates/holon-integration-tests/src/pbt/invariants/bodies/viewmodel_state_toggle_correct.rs
  kind: invariant
  invariant_ids:
  - inv-viewmodel-state-toggle-correct
  requires_ref_caps:
  - RefBlockTree
  - RefTaskState
  requires_sut_caps:
  - SutRenderer
  slips-if-removed:
  - a task block's toggle shows the wrong state or label, or lacks a task_state-affecting op; clicking
    the checkbox can't cycle state or displays a stale value
  oracle:
  - correspondence
  covers:
  - state-toggle-divergence — rendered StateToggle current/label/field/ops disagree with the ref block's
    task_state
- file: crates/holon-integration-tests/src/pbt/invariants/bodies/viewmodel_tree_virtual_slots.rs
  kind: invariant
  invariant_ids:
  - inv-viewmodel-tree-virtual-slots
  requires_ref_caps:
  - RefBlockTree
  requires_sut_caps:
  - SutRenderer
  slips-if-removed:
  - the "add new block" slot renders above the page title, or the title renders as an editable state_toggle
    row instead of an h1; both dogfood regressions ship (devlog 2026-07-05)
  oracle:
  - internal-consistency — R:RefBlockTree only LOCATES the Main focus roots; nothing from ref is compared
    as an expected value, the assertions are structural (slot order, no state_toggle in title subtree)
  covers:
  - virtual-slot-misorder + page-title-wrong-variant — creation slot not last child; focused page title
    on the default (state_toggle) variant instead of the page_title bare-text variant
- file: crates/holon-integration-tests/src/pbt/invariants/bodies/watch_rows_match_ref.rs
  kind: invariant
  invariant_ids:
  - inv-watch-rows-match-ref
  slips-if-removed:
  - a Turso IVM CDC bug delivers wrong/missing/spurious rows or a wrong parent_id (e.g. block:root-layout)
    to a live watch; the watched query panel shows stale or wrong data
  oracle:
  - correspondence
  covers:
  - cdc-watch-divergence — per-watch CDC-delivered ui_model rows (id set, selected fields, parent_id)
    disagree with ref expected rows
- file: crates/holon-integration-tests/src/pbt/invariants/bodies/window_focus_matches_engine_focus.rs
  kind: invariant
  invariant_ids:
  - inv-window-focus-matches-engine-focus
  requires_sut_caps:
  - SutDriver
  - SutLayout
  slips-if-removed:
  - a stale window-focused editor keeps focus after engine focus moves; keystrokes meant for the newly
    focused block get consumed by the zombie editor
  oracle:
  - sut-internal — compares the engine's focused_block against the per-frame window RenderedElement::focused;
    both are SUT authorities and the ref model has no window-focus projection to compare against
  covers:
  - focus-steal-back / zombie-editor (ADR 0010) — window focus settles on an editor different from engine
    focus
- file: crates/holon-integration-tests/src/pbt/layout_bridge.rs
  kind: module
  requires_sut_caps:
  - SutBlockInteract
- file: crates/holon-integration-tests/src/pbt/loro_slice.rs
  kind: slice-mod
  covers:
  - loro-slice — re-exports the co-located `LoroBackendComponent`.
- file: crates/holon-integration-tests/src/pbt/mcp_server_actor_state.rs
  kind: ref
  covers:
  - mcp-actor-state — the active query-watch set (`query_id → WatchSpec`) the MCP server serves; single-homed
    watch registry.
- file: crates/holon-integration-tests/src/pbt/memory_slice/components.rs
  kind: sut-arm
  provides_caps:
  - SutBackend
  - SutBlockTreeWrite
  - SutEditorMirrorRead
  - SutEditorMirrorWrite
  covers:
  - 'memory-slice — in-memory store arm: no projection, no CDC, no async settle; exists purely for speed
    (microseconds/case)'
- file: crates/holon-integration-tests/src/pbt/memory_slice/integration_tests.rs
  kind: module
  requires_ref_caps:
  - RefBackend
  - RefEditorMirrorMut
  requires_sut_caps:
  - SutBackend
  - SutEditorMirrorWrite
- file: crates/holon-integration-tests/src/pbt/memory_slice.rs
  kind: slice-mod
  covers:
  - memory-slice — component list + builders for the in-memory arm.
- file: crates/holon-integration-tests/src/pbt/op_write_cap.rs
  kind: module
  provides_caps:
  - SutBlockTreeWrite
  - SutEdgeFieldWrite
- file: crates/holon-integration-tests/src/pbt/query.rs
  kind: oracle
  gen:
  - '`QuerySource` has 5 variants but the file generator only mints 3 (AllBlocks, DirectChildren, DescendantsOfAny);
    FocusRootDescendants and PageBlocks are seeded by the default layout / start_app only, never by a
    user-authored index.org override — so a layout-override bug specific to the focus-root or page-blocks
    traversal is not reachable via WriteOrgFile'
  - 'watched-query generator (`generate_test_query`) is fixed: always AllBlocks + the same 6 columns +
    0..=2 preds drawn from a 4-element `generate_predicate` set (Ne/Eq×2/IsNotNull) — no Lt/Gt/Contains,
    no custom-property predicates, no negation; the predicate compiler''s untested surface is the AST
    in query_ast.rs, not this one'
- file: crates/holon-integration-tests/src/pbt/query_ast.rs
  kind: oracle
  gen:
  - 'ORPHANED relative to the keystone: no transition, invariant, or generator constructs a `QueryAst`
    or calls `now_query_ast`. The 7-variant Predicate surface — crucially the compound And/Or/Not nesting
    — is exercised ONLY by this module''s three hand-written unit tests over the ONE canonical now-query.
    The gql/sql compound-predicate compile path is never fuzzed; a nested And(Or(Not(..))) SQL-lowering
    bug would slip. Either wire a QueryAst generator into a watched-query transition or delete as dead.'
- file: crates/holon-integration-tests/src/pbt/ref_caps/advice.rs
  kind: ref
  provides_caps:
  - RefAdvice
  covers:
  - 'advice-weave — delegates to the pure `advice_expectation` module, an INDEPENDENT Rust re-implementation
    of the advice matview SQL (self-join + suppression anti-join) — differential vs the SUT''s Turso IVM
    path, not a mirror. FIDELITY: v1 only models `AnchorSelector::HasTag` (other selectors `unreachable!`
    — fail-loud, not silent).'
- file: crates/holon-integration-tests/src/pbt/ref_caps/blocks.rs
  kind: ref
  provides_caps:
  - RefApplyMutationMut
  - RefBackend
  - RefBlockTree
  - RefBlockTreeMut
  - RefHistoryExpectation
  covers:
  - block-tree-truth — models the canonical block tree (parentage, sibling order, layout/seed/page classification)
    the block-correspondence invariants compare against.
  - org-disk-view — `RefBackend::org_blocks` predicts on-disk org form; REUSES the real `holon_orgmode::parser::split_headline_tags`
    (via `apply_org_headline_tag_split`) fed from ref content — legit reuse, blind only to a bug INSIDE
    that shared parser fn (its own tier), not a
- file: crates/holon-integration-tests/src/pbt/ref_caps/boot.rs
  kind: ref
  provides_caps:
  - RefBoot
  - RefBootMut
  - RefLifecycle
  covers:
  - app-lifecycle — models `app_started`, `is_properly_setup`, `enable_loro` (wiring-derived) and the
    pre-boot on-disk workspace (dirs / org files / git-jj init) a `StartApp` observes.
- file: crates/holon-integration-tests/src/pbt/ref_caps/clock.rs
  kind: ref
  provides_caps:
  - RefClock
  - RefClockMut
  covers:
  - 'journal-count — thin delegate to `ClockState`: current day + `visited_days` cardinality = predicted
    journal-block count (one per distinct day). Boot day agrees with the SUT by construction (shared `KEYSTONE_CLOCK_BOOT_MS`),
    no read-back side channel.'
- file: crates/holon-integration-tests/src/pbt/ref_caps/docs.rs
  kind: ref
  provides_caps:
  - RefDocuments
  - RefDocumentsMut
  covers:
  - doc-identity — file-adapter doc identity + naming (`doc_uri → filename`, page-block creation on `insert_document`).
    Delete cascades through the SAME `Mutation::Delete` + canonical re-sort machinery `apply_content_mutation`
    uses (single applier discipline for deletes).
- file: crates/holon-integration-tests/src/pbt/ref_caps/editor.rs
  kind: ref
  provides_caps:
  - RefEditorMirror
  - RefEditorMirrorMut
  covers:
  - 'editor-mirror — headless mirror of the live GPUI/TUI `InputState` (active editor block + dirty/clean
    buffer text). FIDELITY: the clean-buffer refresh-from-content-cell (dirty buffers keep pending user
    text) is modeled in `apply_content_mutation`, not here.'
- file: crates/holon-integration-tests/src/pbt/ref_caps/focus.rs
  kind: ref
  provides_caps:
  - RefFocusMut
  - RefFocusRoots
  - RefGlobalFocus
  covers:
  - 'navigation-focus — per-region focused entity + cursor and the `focus_roots` set (mirrors `schema/matview_focus_roots.sql`:
    open pins / `navigation_history WHERE closed_at IS NULL`). The matview shape is HAND-modeled in `expected_focus_root_ids`;
    keep it in sync with that SQL.'
- file: crates/holon-integration-tests/src/pbt/ref_caps/layout.rs
  kind: ref
  provides_caps:
  - RefLayout
  - RefLayoutInteract
  - RefLayoutMutate
  - RefRenderExpr
  covers:
  - 'layout-render-config — layout-block classification (headline / query-source / render-source), author-intent
    `RenderExpr` per render source, and interactivity/draggability predictions. FIDELITY: the render-expr↔Rhai
    mapping is a closed vocabulary (`render_expr_from_rhai`); a Rhai string outside it round-trips as
    an untracked render (table() fallback in `get_block_data`, disclosed via warn).'
- file: crates/holon-integration-tests/src/pbt/ref_caps/misc.rs
  kind: ref
  provides_caps:
  - RefSqlCardinality
  - RefViewSelectionMut
  - RefWiring
  covers:
  - view-selection — active view + render-expr name per region, the cap-set gate (`RefWiring`, harness
    config not domain state), and the SQL row-cardinality budget model (`RefSqlCardinality`).
- file: crates/holon-integration-tests/src/pbt/ref_caps/mod.rs
  kind: ref
  requires_ref_caps:
  - RefAdvice
  - RefBackend
  - RefBlockTree
  - RefClock
  - RefEditorMirror
  - RefGlobalFocus
  - RefHistoryExpectation
  - RefLayout
  - RefTaskState
  - RefToggle
  covers:
  - ref-capmap-wiring — `CapProvider::register` exposes the single `ReferenceState` as the ref `CapMap`
    for `run_selected`. Every
- file: crates/holon-integration-tests/src/pbt/ref_caps/nav.rs
  kind: ref
  requires_ref_caps:
  - RefArrowNav
  provides_caps:
  - RefNavHistory
  - RefNavHistoryMut
  - RefPins
  - RefPinsMut
  covers:
  - 'navigation-history — models `navigation_history` rows: open pins per region, the AUTOINCREMENT `id`
    counter (`next_history_id`, bumped on INSERT not UPDATE), close-prior-then-insert focus contract,
    and arrow-nav target order. Row-id semantics are HAND-mirrored from SQLite.'
- file: crates/holon-integration-tests/src/pbt/ref_caps/peers.rs
  kind: ref
  provides_caps:
  - RefPeers
  - RefPeersMut
  covers:
  - loro-peer-merge — orphan-rule delegates to `holon_loro_testing::ref_ext::LoroRefExt`, which REUSES
    the REAL Loro CRDT sync on shadow docs fed from ref intent (not SUT read-back) — the strongest reused-not-under-test-engine
    oracle for concurrent-merge order / text interleaving. The peer→primary block merge + `recanon_and_rebuild`
    fixpoint stay core-side (`merge_peer_blocks_into_primary`).
- file: crates/holon-integration-tests/src/pbt/ref_caps/toggle.rs
  kind: ref
  requires_ref_caps:
  - RefTaskStateToggle
  provides_caps:
  - RefTaskState
  - RefToggle
  - RefToggleMut
  covers:
  - expand-collapse — `is_expanded` (embedded-page lazy-collapse model) + task-state keyword for the ViewModel
    `value_fn_provider` invariants. Untracked toggles default open (see `widget_state`,
- file: crates/holon-integration-tests/src/pbt/ref_caps/watches.rs
  kind: ref
  provides_caps:
  - RefWatchesMut
  covers:
  - active-watches — MCP-server actor's active query watches (`query_id → WatchSpec`) and expected rows
    via the `TestQuery` evaluator (an independent PRQL-subset evaluator, not the SUT's Turso path).
- file: crates/holon-integration-tests/src/pbt/reference_domain_state.rs
  kind: ref
  covers:
  - domain-truth — single-homed Tier-1 domain fragment (block tree, layout/profile classification, author-intent
    render config, seed profile). The one owner of these facts; the actor/UI/loro fragments are read-only
    lenses over it. No datum is double-homed here.
- file: crates/holon-integration-tests/src/pbt/reference_state.rs
  kind: ref
  oracle:
  - 'correspondence — the single `ReferenceState` oracle. Its `BuilderServices::interpret` REUSES the
    production `ShadowInterpreter` (render engine) driven from the ref''s OWN block map (`get_block_data`),
    never SUT read-back: legitimate reused-not-under-test-engine oracle, deliberately blind to render-engine-internal
    bugs (covered by its own tier), sharp on the ref↔SUT projection axis.'
  covers:
  - 'block-tree/sibling-order — the structural mutation helpers (`move_block`/`outdent_block`/`swap_sequence`/`split_block`/`join_block`)
    each independently predict post-op sibling order. FIDELITY DRIFT: `move_block` deliberately SUPPRESSES
    the canonical re-sort (models the production fractional `sort_key`), whereas `outdent`/`swap`/content
    mutations funnel through `recanon_and_rebuild` → `assign_reference_sequences_canonical`, a HAND-mirror
    of the org `process_headlines` re-emission order (Source<Image<Text). A move followed by a same-parent
    content mutation re-canonicalizes and can silently reorder what the move placed — see the REF honesty-drift
    finding.'
- file: crates/holon-integration-tests/src/pbt/sql_loro_slice/builders.rs
  kind: module
  requires_sut_caps:
  - SutLoroTaskState
- file: crates/holon-integration-tests/src/pbt/sql_loro_slice/integration_tests.rs
  kind: module
  requires_sut_caps:
  - SutLoroTaskState
  - SutSqlProjection
- file: crates/holon-integration-tests/src/pbt/sql_loro_slice.rs
  kind: slice-mod
  covers:
  - sql+loro-slice — the ONLY consumer of `inv-task-state-storage- coherence`. NOTE that invariant is
    SUT↔SUT (SQL vs Loro projection), not SUT↔Ref — see audit SUT-3.
- file: crates/holon-integration-tests/src/pbt/sql_slice/builders.rs
  kind: module
  requires_sut_caps:
  - SutBackend
  - SutBlockTreeWrite
- file: crates/holon-integration-tests/src/pbt/sql_slice/components.rs
  kind: sut-arm
  requires_sut_caps:
  - SutBlockTreeWrite
  provides_caps:
  - SutBackend
  - SutSqlProjection
  covers:
  - sql-slice — real Turso `BackendEngine` over `block_raw`, driven through production block ops; no reactive
    engine / frontend / CDC mirror. Storage-layer arm of the future `E2ESut` replacement (§F2 convergence).
- file: crates/holon-integration-tests/src/pbt/sql_slice/integration_tests.rs
  kind: module
  requires_sut_caps:
  - SutBackend
- file: crates/holon-integration-tests/src/pbt/sql_slice.rs
  kind: slice-mod
  covers:
  - sql-slice — component list + builders for the Turso storage arm.
- file: crates/holon-integration-tests/src/pbt/sut_metrics.rs
  kind: sut-arm
  env_gates:
  - HOLON_PERF_BUDGET
  - HOLON_PERF_DETAIL
  feature_gates:
  - otel-testing
  covers:
  - 'all-slices (span-metrics) — non-functional SUT arm:'
- file: crates/holon-integration-tests/src/pbt/sut_row_parsing.rs
  kind: cap-plumbing
  covers:
  - sql-slice/frontend-slice — shared fail-loud SQL-row→Block parser so the SQL invariant path and LiveData
    mirror stay byte-for-byte
- file: crates/holon-integration-tests/src/pbt/transition_dispatch.rs
  kind: transition-dispatch
  requires_ref_caps:
  - RefBlockTree
  requires_sut_caps:
  - SutAppLifecycle
  - SutApplyMutation
  - SutBlockCreate
  - SutBlockInteract
  - SutBlockTreeWrite
  - SutBootWatches
  - SutClockAdvance
  - SutEdgeFieldWrite
  - SutEditorMirrorWrite
  - SutFixtureFs
  - SutFocusWrite
  - SutHistoryWrite
  - SutLoro
  - SutMcpEmit
  - SutMutate
  - SutNavHistoryDrive
  - SutNavHistoryWrite
  - SutSeamMutate
  - SutTemplateInstantiate
  - SutViewControl
  - SutWatchRegister
  provides_caps:
  - RefSqlCardinality
  env_gates:
  - HOLON_PBT_WEIGHTS
  feature_gates:
  - otel-testing
  uses_cap_transition_macro: true
  covers:
  - 'transition-enum-dispatch — declare_e2e_transitions! generated E2ETransition enum: variant union caps,
    hand-rolled trait match, strategy aggregator (RESTRICTED file — structural recommendations in docs/Testing/PBT-Audit-2026-07-16.md,
    not edited here).'
- file: crates/holon-integration-tests/src/pbt/transitions/advance_day.rs
  kind: transition
  requires_ref_caps:
  - RefClock
  - RefClockMut
  - RefLifecycle
  requires_sut_caps:
  - SutClockAdvance
  feature_gates:
  - otel-testing
  uses_cap_transition_macro: true
  covers:
  - clock-day-rollover — reconcile_clock re-fires the journal rule
  rung: dispatch ambient time stimulus — no user gesture exists; `advance_clock_days` advances the injected
    TestClock + runs the production `reconcile_clock` (ADR 0024 §6), the faithful floor (no higher rung
    to descend from).
- file: crates/holon-integration-tests/src/pbt/transitions/apply_mutation.rs
  kind: transition
  requires_ref_caps:
  - RefApplyMutationMut
  - RefBlockTree
  - RefBlockTreeMut
  - RefDocuments
  - RefFocusMut
  - RefLayout
  - RefLayoutInteract
  - RefLifecycle
  - RefPeers
  - RefPeersMut
  - RefSqlCardinality
  - RefWiring
  requires_sut_caps:
  - SutLoro
  - SutSeamMutate
  provides_caps:
  - SutApplyMutation
  feature_gates:
  - otel-testing
  uses_cap_transition_macro: true
  covers:
  - mutation-source-differential — org vs Loro ingress the shrinker
  rung: 'external routed on the composed CapMap by MutationSource: External -> org-file rewrite via SutSeamMutate
    (FileSyncController re-ingest); LoroPeer -> SutLoro peer apply. No in-process op dispatch.'
- file: crates/holon-integration-tests/src/pbt/transitions/arrow_navigate.rs
  kind: transition
  requires_ref_caps:
  - RefArrowNav
  - RefLifecycle
  feature_gates:
  - otel-testing
  uses_cap_transition_macro: true
  covers:
  - arrow-key-nav — keyboard arrow -> click-intent -> focus reducer
  rung: input-pipeline `apply_arrow_navigate` drives `send_raw_keystroke_until_handled` (real arrow keys)
    through the installed UserDriver -> intent -> focus move.
- file: crates/holon-integration-tests/src/pbt/transitions/bulk_external_add.rs
  kind: transition
  requires_ref_caps:
  - RefDocuments
  - RefLayoutInteract
  - RefLayoutMutate
  - RefLifecycle
  requires_sut_caps:
  - SutSeamMutate
  feature_gates:
  - otel-testing
  uses_cap_transition_macro: true
  covers:
  - bulk-org-ingest — multi-block org write -> parser -> block_raw
  rung: external `bulk_external_add` writes an org file and lets the production FileSyncController re-ingest
    it.
- file: crates/holon-integration-tests/src/pbt/transitions/click_block.rs
  kind: transition
  requires_ref_caps:
  - RefBlockTree
  - RefLayoutMutate
  - RefLifecycle
  - RefNavHistory
  requires_sut_caps:
  - SutBlockInteract
  - SutDriver
  - SutLayout
  feature_gates:
  - otel-testing
  uses_cap_transition_macro: true
  covers:
  - click-to-focus — pointer click -> find_click_intent -> focus
  rung: 'input-pipeline `apply_click_block_to_sut`: wait_for_bounds + click_entity + wait_for_engine_focus
    through the production UserDriver.'
- file: crates/holon-integration-tests/src/pbt/transitions/concurrent_schema_init.rs
  kind: transition
  requires_ref_caps:
  - RefLayout
  - RefLifecycle
  requires_sut_caps:
  - SutAppLifecycle
  feature_gates:
  - otel-testing
  uses_cap_transition_macro: true
  covers:
  - concurrent-schema-init-race — parallel matview/schema setup
  rung: dispatch `concurrent_schema_init` is a storage-level race probe with no user gesture (faithful
    floor).
- file: crates/holon-integration-tests/src/pbt/transitions/create_block_under_focus.rs
  kind: transition
  requires_ref_caps:
  - RefFocusRoots
  - RefLayout
  - RefLayoutInteract
  - RefLayoutMutate
  - RefLifecycle
  requires_sut_caps:
  - SutBlockCreate
  feature_gates:
  - otel-testing
  uses_cap_transition_macro: true
  rung: input-pipeline no-id arm drives the production creation-slot gesture
- file: crates/holon-integration-tests/src/pbt/transitions/create_directory.rs
  kind: transition
  requires_ref_caps:
  - RefBootMut
  - RefLifecycle
  requires_sut_caps:
  - SutFixtureFs
  feature_gates:
  - otel-testing
  uses_cap_transition_macro: true
  covers:
  - workspace-dir-create — fs directory creation in the temp
  rung: external
- file: crates/holon-integration-tests/src/pbt/transitions/create_document.rs
  kind: transition
  requires_ref_caps:
  - RefDocumentsMut
  - RefLifecycle
  requires_sut_caps:
  - SutAppLifecycle
  feature_gates:
  - otel-testing
  uses_cap_transition_macro: true
  covers:
  - doc-create-ingest — new-doc file -> watcher -> block_raw page
  rung: external writes an empty org file into the watched org_root; the production FileSyncController
    ingests it and mints the page block.
- file: crates/holon-integration-tests/src/pbt/transitions/delete_backward.rs
  kind: transition
  requires_ref_caps:
  - RefBlockTreeMut
  - RefEditorMirror
  - RefEditorMirrorMut
  - RefFocusMut
  - RefLifecycle
  requires_sut_caps:
  - SutEditorMirrorWrite
  feature_gates:
  - otel-testing
  uses_cap_transition_macro: true
  covers:
  - editor-backspace — backspace keystroke -> MutableText edit
  rung: input-pipeline `apply_delete_backward` drives editor backspace keystrokes through the production
    ReactiveEngineDriver -> HeadlessEditorMirror.
- file: crates/holon-integration-tests/src/pbt/transitions/delete_document.rs
  kind: transition
  requires_ref_caps:
  - RefDocuments
  - RefDocumentsMut
  - RefLifecycle
  requires_sut_caps:
  - SutAppLifecycle
  feature_gates:
  - otel-testing
  uses_cap_transition_macro: true
  covers:
  - doc-delete — fs delete -> watcher removal -> block_raw page drop
  rung: external
- file: crates/holon-integration-tests/src/pbt/transitions/deliver_block_content.rs
  kind: transition
  covers:
  - deferred-live-block-delivery — async content into a live_block
  rung: 'dispatch BACKEND-PBT-ONLY: `apply_to_sut` panics in the composed runner (the backend PBT rejects
    it). Never dispatched into any composed alphabet; a deferred-live-block delivery probe for the pure
    backend slice only.'
- file: crates/holon-integration-tests/src/pbt/transitions/drag_drop_block.rs
  kind: transition
  requires_ref_caps:
  - RefBlockTree
  - RefBlockTreeMut
  - RefFocusRoots
  - RefLayout
  - RefLayoutInteract
  - RefLifecycle
  requires_sut_caps:
  - SutBlockInteract
  feature_gates:
  - otel-testing
  uses_cap_transition_macro: true
  covers:
  - drag-reorder — pointer drag -> reparent under target
  rung: input-pipeline `drag_drop_block` drives a real pointer drag (geometry) through the production
    UserDriver.
- file: crates/holon-integration-tests/src/pbt/transitions/emit_mcp_data.rs
  kind: transition
  requires_ref_caps:
  - RefLifecycle
  requires_sut_caps:
  - SutMcpEmit
  feature_gates:
  - otel-testing
  uses_cap_transition_macro: true
  covers:
  - mcp-ivm-reemission — CDC re-eval to detect duplicate MCP
  rung: mcp purpose is an MCP re-emission; but the headless frontend has no PbtMcpIntegration attached
    -> `emit_mcp_data` is a faithful no-op AND no invariant observes an emission on this path (see audit
    finding TR-OBS).
- file: crates/holon-integration-tests/src/pbt/transitions/epoch_flip_rejected.rs
  kind: transition
  requires_ref_caps:
  - RefLifecycle
  requires_sut_caps:
  - SutAppLifecycle
  feature_gates:
  - otel-testing
  uses_cap_transition_macro: true
  covers:
  - consolidator-epoch-flip-reject — stale epoch flip must be
  rung: dispatch `assert_epoch_flip_rejected` is a consolidator-level assertion probe.
- file: crates/holon-integration-tests/src/pbt/transitions/expand_toggle.rs
  kind: transition
  requires_ref_caps:
  - RefBlockTree
  - RefLifecycle
  - RefRenderExpr
  - RefToggleMut
  requires_sut_caps:
  - SutBlockInteract
  feature_gates:
  - otel-testing
  uses_cap_transition_macro: true
  covers:
  - expand-chevron — chevron gesture -> collapsed-gate flip
  rung: input-pipeline `expand_toggle` drives `UserDriver::set_block_expanded` (real chevron flip) ->
    view-local gate mutable.
- file: crates/holon-integration-tests/src/pbt/transitions/focus_editable_text.rs
  kind: transition
  requires_ref_caps:
  - RefBlockTree
  - RefBlockTreeMut
  - RefEditorMirror
  - RefEditorMirrorMut
  - RefFocusMut
  - RefFocusRoots
  - RefLayout
  - RefLifecycle
  requires_sut_caps:
  - SutDriver
  - SutFocusWrite
  - SutLayout
  feature_gates:
  - otel-testing
  uses_cap_transition_macro: true
  covers:
  - focus-editor-click — main-panel click -> editor focus
  rung: 'input-pipeline `apply_focus_editable_text_to_sut`: click_entity(main) through the production
    driver (editable_text binds no intent -> falls through to in-memory set_focus, ADR 0010).'
- file: crates/holon-integration-tests/src/pbt/transitions/git_init.rs
  kind: transition
  requires_ref_caps:
  - RefBootMut
  - RefLifecycle
  requires_sut_caps:
  - SutFixtureFs
  feature_gates:
  - otel-testing
  uses_cap_transition_macro: true
  covers:
  - git-init — git repository initialization
  rung: external
- file: crates/holon-integration-tests/src/pbt/transitions/indent.rs
  kind: transition
  requires_ref_caps:
  - RefBlockTree
  - RefBlockTreeMut
  - RefEditorMirrorMut
  - RefFocusMut
  - RefLifecycle
  requires_sut_caps:
  - SutBlockTreeWrite
  feature_gates:
  - otel-testing
  uses_cap_transition_macro: true
  covers:
  - indent-chord — indent chord -> bubble_input -> structural
  rung: 'input-pipeline KEYSTONE: KeystrokeBlockTreeWriter drives the bound chord (Tab) through the production
    chord-resolution path. FIXED-ID lib slices (no resolver) fall back to OpDispatchWriter raw op dispatch
    (dispatch floor).'
- file: crates/holon-integration-tests/src/pbt/transitions/instantiate_template.rs
  kind: transition
  requires_ref_caps:
  - RefBlockTree
  - RefLayoutMutate
  - RefLifecycle
  requires_sut_caps:
  - SutTemplateInstantiate
  feature_gates:
  - otel-testing
  uses_cap_transition_macro: true
  covers:
  - template-instantiate — canned inline template -> block.create
  rung: dispatch `instantiate_template` mints template blocks via `block.create` dispatch; storage-only
    pin uses the op-floor SutTemplateInstantiate. No template gesture verified (see UNCERTAIN).
- file: crates/holon-integration-tests/src/pbt/transitions/jj_git_init.rs
  kind: transition
  requires_ref_caps:
  - RefBootMut
  - RefLifecycle
  requires_sut_caps:
  - SutFixtureFs
  feature_gates:
  - otel-testing
  uses_cap_transition_macro: true
  covers:
  - jj-init — jj repository initialization
  rung: external
- file: crates/holon-integration-tests/src/pbt/transitions/join_block.rs
  kind: transition
  requires_ref_caps:
  - RefBlockTree
  - RefBlockTreeMut
  - RefFocusMut
  - RefLifecycle
  requires_sut_caps:
  - SutBlockTreeWrite
  feature_gates:
  - otel-testing
  uses_cap_transition_macro: true
  covers:
  - join-backspace — backspace-at-start -> join into previous
  rung: 'input-pipeline KEYSTONE: KeystrokeBlockTreeWriter drives Backspace-at-start via the editor keystroke
    path; fixed-id slices fall back to OpDispatchWriter (dispatch floor).'
- file: crates/holon-integration-tests/src/pbt/transitions/mod.rs
  kind: transition
  requires_ref_caps:
  - RefBlockTree
  - RefBlockTreeMut
  - RefEditorMirrorMut
  - RefFocusMut
  requires_sut_caps:
  - SutAppLifecycle
  - SutBlockInteract
  - SutBlockTreeWrite
  - SutEditorMirrorWrite
  - SutFixtureFs
  - SutFocusWrite
  - SutHistoryWrite
  - SutLoro
  - SutMcpEmit
  - SutMutate
  - SutNavHistoryDrive
  - SutNavHistoryWrite
  - SutSeamMutate
  - SutViewControl
  - SutWatchRegister
  uses_cap_transition_macro: true
- file: crates/holon-integration-tests/src/pbt/transitions/move_cursor.rs
  kind: transition
  requires_ref_caps:
  - RefEditorMirror
  - RefEditorMirrorMut
  - RefLifecycle
  requires_sut_caps:
  - SutEditorMirrorWrite
  feature_gates:
  - otel-testing
  uses_cap_transition_macro: true
  covers:
  - editor-caret-move — caret keystroke -> InputState cursor
  rung: input-pipeline `apply_move_cursor` drives caret keystrokes through the editor pipeline.
- file: crates/holon-integration-tests/src/pbt/transitions/move_down.rs
  kind: transition
  requires_ref_caps:
  - RefBlockTree
  - RefBlockTreeMut
  - RefEditorMirrorMut
  - RefFocusMut
  - RefLifecycle
  requires_sut_caps:
  - SutBlockTreeWrite
  feature_gates:
  - otel-testing
  uses_cap_transition_macro: true
  covers:
  - reorder-chord-down — Alt+Down chord -> move_down reducer
  rung: 'input-pipeline KEYSTONE: send_block_chord resolves the bound Alt+Down chord from the live registry
    -> bubble_input -> ExecuteOperation; fixed-id slices fall back to OpDispatchWriter (dispatch floor).'
- file: crates/holon-integration-tests/src/pbt/transitions/move_up.rs
  kind: transition
  requires_ref_caps:
  - RefBlockTree
  - RefBlockTreeMut
  - RefEditorMirrorMut
  - RefFocusMut
  - RefLifecycle
  requires_sut_caps:
  - SutBlockTreeWrite
  feature_gates:
  - otel-testing
  uses_cap_transition_macro: true
  covers:
  - reorder-chord-up — Alt+Up chord -> move_up reducer
  rung: 'input-pipeline KEYSTONE: send_block_chord resolves the bound Alt+Up chord; fixed-id slices fall
    back to OpDispatchWriter (dispatch floor).'
- file: crates/holon-integration-tests/src/pbt/transitions/navigate_back.rs
  kind: transition
  requires_ref_caps:
  - RefLifecycle
  - RefNavHistoryMut
  requires_sut_caps:
  - SutNavHistoryDrive
  feature_gates:
  - otel-testing
  uses_cap_transition_macro: true
  rung: 'dispatch UNFAITHFUL SHORTCUT (audit TR-NAV): `navigate_back` dispatches the `navigation.go_back`
    provider op directly through the session, bypassing the leader-chord path a real user (and E2ESut''s
    synthetic_dispatch) takes. A headless chord path demonstrably exists (see move_up/down'
- file: crates/holon-integration-tests/src/pbt/transitions/navigate_focus.rs
  kind: transition
  requires_ref_caps:
  - RefBlockTree
  - RefFocusRoots
  - RefLayout
  - RefLifecycle
  - RefNavHistoryMut
  requires_sut_caps:
  - SutFocusWrite
  feature_gates:
  - otel-testing
  uses_cap_transition_macro: true
  covers:
  - sidebar-nav-focus — sidebar click -> navigation.focus intent +
  rung: 'input-pipeline `apply_navigate_focus_via`: sidebar click_entity(left_sidebar) -> the entry''s
    bound navigation.focus intent (find_click_intent -> dispatch).'
- file: crates/holon-integration-tests/src/pbt/transitions/navigate_forward.rs
  kind: transition
  requires_ref_caps:
  - RefLifecycle
  - RefNavHistoryMut
  requires_sut_caps:
  - SutNavHistoryDrive
  feature_gates:
  - otel-testing
  uses_cap_transition_macro: true
  covers:
  - nav-forward — per-region navigation history forward (op-level
  rung: 'dispatch UNFAITHFUL SHORTCUT (audit TR-NAV): dispatches `navigation.go_forward` directly, bypassing
    the leader-chord path.'
- file: crates/holon-integration-tests/src/pbt/transitions/navigate_home.rs
  kind: transition
  requires_ref_caps:
  - RefLifecycle
  - RefNavHistoryMut
  requires_sut_caps:
  - SutNavHistoryWrite
  feature_gates:
  - otel-testing
  uses_cap_transition_macro: true
  covers:
  - nav-home — return-to-root in a region (op-level only)
  rung: 'dispatch UNFAITHFUL SHORTCUT (audit TR-NAV): `apply_navigate_home` dispatches `navigation.go_home`
    directly, bypassing the leader-h chord path (the same op the GPUI/CLI leader-h dispatches).'
- file: crates/holon-integration-tests/src/pbt/transitions/nothing.rs
  kind: transition
  requires_ref_caps:
  - RefSqlCardinality
  feature_gates:
  - otel-testing
  uses_cap_transition_macro: true
  covers:
  - search-space-exploration — no-op interleaving for schedule
  rung: 'dispatch no-op: `apply_to_sut` does nothing (search-space filler, no SUT effect).'
- file: crates/holon-integration-tests/src/pbt/transitions/outdent.rs
  kind: transition
  requires_ref_caps:
  - RefBlockTree
  - RefBlockTreeMut
  - RefEditorMirrorMut
  - RefFocusMut
  - RefLifecycle
  requires_sut_caps:
  - SutBlockTreeWrite
  feature_gates:
  - otel-testing
  uses_cap_transition_macro: true
  covers:
  - outdent-chord — outdent chord -> structural reducer
  rung: 'input-pipeline KEYSTONE: chord (Shift+Tab) via KeystrokeBlockTreeWriter; fixed-id slices fall
    back to OpDispatchWriter (dispatch floor).'
- file: crates/holon-integration-tests/src/pbt/transitions/pin_block.rs
  kind: transition
  requires_ref_caps:
  - RefBlockTree
  - RefLifecycle
  - RefPinsMut
  requires_sut_caps:
  - SutNavHistoryDrive
  feature_gates:
  - otel-testing
  uses_cap_transition_macro: true
  covers:
  - pin-sidebar — pin block to sidebar (op-level only)
  rung: 'dispatch UNFAITHFUL SHORTCUT (audit TR-NAV): title is a shift+click gesture, but `pin_block`
    dispatches `navigation.focus_pin` directly; the shift+click click-intent path is untested headless.'
- file: crates/holon-integration-tests/src/pbt/transitions/press_key.rs
  kind: transition
  requires_ref_caps:
  - RefBlockTree
  - RefBlockTreeMut
  - RefEditorMirror
  - RefEditorMirrorMut
  - RefFocusMut
  - RefLifecycle
  requires_sut_caps:
  - SutBlockInteract
  feature_gates:
  - otel-testing
  uses_cap_transition_macro: true
  covers:
  - structural-chord — raw key chord -> bubble_input resolution
  rung: input-pipeline `press_key` drives `send_raw_keystroke` for each chord key through the production
    UserDriver.
- file: crates/holon-integration-tests/src/pbt/transitions/redo.rs
  kind: transition
  requires_ref_caps:
  - RefBlockTreeMut
  - RefLifecycle
  requires_sut_caps:
  - SutHistoryWrite
  feature_gates:
  - otel-testing
  uses_cap_transition_macro: true
  covers:
  - redo-stack — engine redo restores the last undone mutation
  rung: dispatch `redo` calls `engine.redo()` directly; no redo keybinding is bound in production (undo-ruling),
    so no higher rung exists yet to exercise.
- file: crates/holon-integration-tests/src/pbt/transitions/remove_watch.rs
  kind: transition
  requires_ref_caps:
  - RefLifecycle
  - RefWatchesMut
  requires_sut_caps:
  - SutWatchRegister
  feature_gates:
  - otel-testing
  uses_cap_transition_macro: true
  covers:
  - watch-teardown — watch removal + guard drop
  rung: 'dispatch `unregister_watch` drops the tracked WatchGuard (Drop releases the query watcher). OBSERVABILITY
    HOLE (audit TR-OBS): no invariant checks a torn-down watch actually stopped emitting / released resources.'
- file: crates/holon-integration-tests/src/pbt/transitions/set_edge_field.rs
  kind: transition
  requires_ref_caps:
  - RefBlockTree
  - RefLayout
  - RefLayoutInteract
  - RefLayoutMutate
  - RefLifecycle
  - RefWiring
  requires_sut_caps:
  - SutEdgeFieldWrite
  feature_gates:
  - otel-testing
  uses_cap_transition_macro: true
  covers:
  - edge-field-set — tags/requires edge write -> project() -> SQL
  rung: dispatch `apply_set_edge_field` routes a `set_field` op through EdgeFieldWriter over the real
    engine (Loro-authority mode, journaled for undo). No edge- field UI gesture exists.
- file: crates/holon-integration-tests/src/pbt/transitions/setup_watch.rs
  kind: transition
  requires_ref_caps:
  - RefLifecycle
  - RefWatchesMut
  requires_sut_caps:
  - SutWatchRegister
  feature_gates:
  - otel-testing
  uses_cap_transition_macro: true
  covers:
  - watch-register — query watch registration + CDC subscription
  rung: dispatch `register_watch` compiles + registers the query watcher directly (no UI gesture for registering
    a watch).
- file: crates/holon-integration-tests/src/pbt/transitions/simulate_restart.rs
  kind: transition
  requires_ref_caps:
  - RefLayout
  - RefLifecycle
  requires_sut_caps:
  - SutAppLifecycle
  feature_gates:
  - otel-testing
  uses_cap_transition_macro: true
  covers:
  - restart-reingest — org re-parse on file touch
  rung: external file-touch re-trigger of the production FileSyncController (NOT a true reboot — faithful
    to E2ESut::simulate_restart).
- file: crates/holon-integration-tests/src/pbt/transitions/split_block.rs
  kind: transition
  requires_ref_caps:
  - RefBlockTree
  - RefBlockTreeMut
  - RefEditorMirrorMut
  - RefFocusMut
  - RefLifecycle
  - RefSqlCardinality
  requires_sut_caps:
  - SutBlockTreeWrite
  - SutDriver
  - SutLayout
  feature_gates:
  - otel-testing
  uses_cap_transition_macro: true
  gen:
  - position weighted toward content byte boundaries of the focused block; shrinks toward position 0 via
    proptest default
  covers:
  - editor-split — Enter-at-cursor split via the live InputState capture_action path (editor_view.rs),
    not the chord resolver
  rung: input-pipeline
- file: crates/holon-integration-tests/src/pbt/transitions/start_app.rs
  kind: transition
  requires_ref_caps:
  - RefBoot
  - RefBootMut
  - RefLifecycle
  - RefSqlCardinality
  requires_sut_caps:
  - SutAppLifecycle
  - SutWatchRegister
  provides_caps:
  - SutBootWatches
  feature_gates:
  - otel-testing
  uses_cap_transition_macro: true
  covers:
  - app-boot — application startup + seeded sidebar watch
  rung: 'external app boot/lifecycle stimulus. NOTE: unimplemented on the headless frontend component
    (panics) and NOT in any composed alphabet yet — lifecycle is a later increment.'
- file: crates/holon-integration-tests/src/pbt/transitions/switch_view.rs
  kind: transition
  requires_ref_caps:
  - RefLifecycle
  - RefViewSelectionMut
  requires_sut_caps:
  - SutViewControl
  feature_gates:
  - otel-testing
  uses_cap_transition_macro: true
  covers:
  - view-switch — current-view selection (harness mirror only; no
  rung: 'dispatch VACUOUS (audit TR-VAC): `switch_view` is a pure harness field write (`*current_view.lock()
    = name`) that no production view-switch path drives — the oracle reads back the same field. Tests
    nothing of prod.'
- file: crates/holon-integration-tests/src/pbt/transitions/switch_view_mode.rs
  kind: transition
  requires_sut_caps:
  - SutBlockInteract
  rung: 'input-pipeline RESIDUAL (concrete ReferenceState, audit TR-RESID): drives'
- file: crates/holon-integration-tests/src/pbt/transitions/toggle_collapse.rs
  kind: transition
  requires_ref_caps:
  - RefToggleMut
  requires_sut_caps:
  - SutBlockInteract
  covers:
  - collapse-click — expand_toggle click -> collapse
  rung: 'input-pipeline RESIDUAL (concrete ReferenceState, audit TR-RESID): SutBlockInteract click via
    LayoutSut bridge. Blocked by the concrete LayoutRef bridge.'
- file: crates/holon-integration-tests/src/pbt/transitions/toggle_drawer.rs
  kind: transition
  requires_ref_caps:
  - RefToggleMut
  requires_sut_caps:
  - SutBlockInteract
  covers:
  - drawer-toggle-click — drawer open/close gesture
  rung: 'input-pipeline RESIDUAL (concrete ReferenceState, audit TR-RESID): SutBlockInteract click via
    LayoutSut bridge. Blocked by the concrete LayoutRef bridge.'
- file: crates/holon-integration-tests/src/pbt/transitions/toggle_state.rs
  kind: transition
  requires_ref_caps:
  - RefBlockTree
  - RefFocusRoots
  - RefLayout
  - RefLifecycle
  - RefTaskState
  - RefTaskStateToggle
  requires_sut_caps:
  - SutDriver
  - SutLayout
  - SutMutate
  feature_gates:
  - otel-testing
  uses_cap_transition_macro: true
  covers:
  - task-state-cycle — state_toggle click -> task-state cycle
  rung: 'input-pipeline `apply_toggle_state_to_sut`: wait_for_widget_kind(state_toggle) + click_entity
    through the production driver.'
- file: crates/holon-integration-tests/src/pbt/transitions/trigger_slash_command.rs
  kind: transition
  requires_ref_caps:
  - RefBlockTree
  - RefFocusRoots
  - RefLayout
  - RefLayoutInteract
  - RefLayoutMutate
  - RefLifecycle
  requires_sut_caps:
  - SutBlockInteract
  - SutDriver
  - SutLayout
  feature_gates:
  - otel-testing
  uses_cap_transition_macro: true
  covers:
  - slash-command — slash-menu keystroke sequence -> command
  rung: 'input-pipeline `apply_trigger_slash_command_to_sut`: click + type ''/'' + command chars + Enter,
    every step a real UserDriver gesture.'
- file: crates/holon-integration-tests/src/pbt/transitions/type_chars.rs
  kind: transition
  requires_ref_caps:
  - RefBlockTreeMut
  - RefEditorMirror
  - RefEditorMirrorMut
  - RefLifecycle
  requires_sut_caps:
  - SutEditorMirrorWrite
  feature_gates:
  - otel-testing
  uses_cap_transition_macro: true
  covers:
  - editor-typing — character keystrokes -> MutableText edit
  rung: input-pipeline `apply_type_chars` drives editor keystrokes through the production ReactiveEngineDriver
    -> HeadlessEditorMirror.
- file: crates/holon-integration-tests/src/pbt/transitions/undo_last_mutation.rs
  kind: transition
  requires_ref_caps:
  - RefBlockTreeMut
  - RefLifecycle
  requires_sut_caps:
  - SutHistoryWrite
  feature_gates:
  - otel-testing
  uses_cap_transition_macro: true
  covers:
  - undo-stack — engine undo restores the pre-mutation block tree
  rung: 'dispatch `undo_last_mutation` calls `engine.undo()` directly; cmd+z is unbound in production
    (undo-ruling), so no higher rung exists yet. OBSERVABILITY OK: the ref does a full block_state snapshot/restore,
    so block-tree correspondence observes wrong-content restores (cursor is only reset).'
- file: crates/holon-integration-tests/src/pbt/transitions/unpin_block.rs
  kind: transition
  requires_ref_caps:
  - RefLifecycle
  - RefPinsMut
  requires_sut_caps:
  - SutNavHistoryDrive
  feature_gates:
  - otel-testing
  uses_cap_transition_macro: true
  covers:
  - unpin — remove pinned block (op-level only)
  rung: 'dispatch UNFAITHFUL SHORTCUT (audit TR-NAV): title is an X-button gesture, but `unpin_block`
    dispatches `navigation.close` directly.'
- file: crates/holon-integration-tests/src/pbt/transitions/write_org_file.rs
  kind: transition
  requires_ref_caps:
  - RefDocumentsMut
  requires_sut_caps:
  - SutFixtureFs
  env_gates:
  - HOLON_PBT_LAYOUT_OVERRIDE
  feature_gates:
  - otel-testing
  uses_cap_transition_macro: true
  rung: external writes an org file to the watched temp dir -> FileSyncController
- file: crates/holon-integration-tests/src/pbt/ui_actor_state.rs
  kind: ref
  covers:
  - ui-actor-state — Tier-3 UI actor fragment (nav history, focus, cursor, widget open-state, pins, active
    editor). Read-only lens over the domain fragment; owns only UI-actor-private facts. Several fields
    HAND-mirror SQLite tables — `next_history_id` (AUTOINCREMENT), `drawer_open` (`widget_open` table),
    `open_pins` (`focus_roots` source rows); keep them in sync with those schemas.
- file: crates/holon-integration-tests/src/pbt/ui_harness.rs
  kind: harness
  env_gates:
  - HOLON_LORO_PEER_ID
  covers:
  - window-slice/live-mcp — env/config scaffolding + geometry-ready gate + embedded-MCP boot for the cross-thread
    windowed runners.
- file: crates/holon-integration-tests/src/pbt/value_fn_invariants.rs
  kind: module
  slips-if-removed:
  - a ProviderCache regression mints a fresh Arc per re-interpret (identity flicker), forcing needless
    row rebuilds and UI flicker; a flatten_properties CDC glitch emits a wrong StateToggle visible for
    one frame before a structural re-render masks it — both invisible once these consumers lose their
    walker
  oracle:
  - sut-internal (arg-variance-13) — provider cache-identity stability + bottom_dock structural presence
    over the SUT ReactiveViewModel; no ref projection of Arc identity exists. Its vfn13 sub-check is metamorphic
    (identities stable across two interpret passes); vfn11 is a ref-focus-gated existence check (weak
    correspondence)
  - correspondence (provider-identity) — drained VM StateToggle `current` vs the reference `task_state_of`,
    per tracked block
  - budget (sql-budget) — per-transition SQL/wall/RSS count vs a frozen expected budget (does not use
    this walker)
  covers:
  - provider-cache-coupling — ReactiveEngine/interpret_pure/ ProviderCache Arc churn, CDC-enrichment StateToggle
    glitches
- file: crates/holon-integration-tests/src/pbt/window_slice/builders.rs
  kind: module
  requires_sut_caps:
  - SutFrontendEmissions
  - SutFrontendEngine
- file: crates/holon-integration-tests/src/pbt/window_slice/components.rs
  kind: sut-arm
  provides_caps:
  - SutFrontendEmissions
  - SutFrontendEngine
  - SutLayout
  - SutQueryResults
  - SutRenderer
  covers:
  - window-slice — live gpui window `BoundsRegistry` read through
- file: crates/holon-integration-tests/src/pbt/window_slice.rs
  kind: slice-mod
  covers:
  - window-slice — component + builders + seed for the windowed arm.
- file: crates/holon-integration-tests/src/ui_driver.rs
  kind: driver
  feature_gates:
  - pbt
  covers:
  - window-slice — geometry driver-ladder rungs (FFI fallback ⊏ geometry-resolved) for driving a real
    frontend during windowed PBT.
- file: crates/holon-integration-tests/tests/advice_live_mcp_gate.rs
  kind: harness
  covers:
  - advice-live-mcp-gate — advice weave proven over a LIVE
- file: crates/holon-integration-tests/tests/advice_step4_red.rs
  kind: harness
  requires_ref_caps:
  - RefAdvice
  covers:
  - advice-weave-green-proof — deterministic composed sequence
- file: crates/holon-integration-tests/tests/bidirectional_sync.rs
  kind: harness
  covers:
  - orgmode-backend-sync — full OrgMode<->Backend bidirectional sync
- file: crates/holon-integration-tests/tests/bisection_pbt.rs
  kind: harness
  env_gates:
  - HOLON_BISECT_CAPTURE
  - HOLON_BISECT_CEILING
  - HOLON_BISECT_PROBE
  - HOLON_BISECT_REPEAT
  - HOLON_BISECT_SIGNATURE
  - HOLON_BISECT_SLICE
  feature_gates:
  - pbt
  covers:
  - replay-portability-bisection — ADR 0009 cross-ComponentSet
- file: crates/holon-integration-tests/tests/boot_scan_bad_file_survives.rs
  kind: harness
  covers:
  - boot-scan-bad-file — boot survives a malformed vault file
- file: crates/holon-integration-tests/tests/cross_medium_caps.rs
  kind: harness
  provides_caps:
  - SutBlockTreeWrite
  - SutEditorMirrorWrite
  feature_gates:
  - pbt
  covers:
  - cross-medium-cap-reuse(compile-time) — Stage2/B2 compile-time
- file: crates/holon-integration-tests/tests/doc_id_writeback_stability.rs
  kind: harness
  covers:
  - 'doc-id-writeback-stability — doc #+ID re-mint data-loss pin'
- file: crates/holon-integration-tests/tests/editor_pure_pbt.rs
  kind: harness
  provides_caps:
  - RefBlockTree
  - RefBlockTreeMut
  - RefEditorMirror
  - RefEditorMirrorMut
  - RefFocusMut
  - RefLifecycle
  feature_gates:
  - pbt
  covers:
  - editor-transition-sharing — proves migrated editor transitions
- file: crates/holon-integration-tests/tests/forward_edge_ingest_regression.rs
  kind: harness
  covers:
  - forward-edge-ingest — forward-edge ingest abort regression
- file: crates/holon-integration-tests/tests/fts_query_block_e2e.rs
  kind: harness
  feature_gates:
  - test-infra
  covers:
  - fts-query-block — fts_match over block content through the real
- file: crates/holon-integration-tests/tests/general_e2e_composed_pbt.rs
  kind: keystone
  env_gates:
  - HOLON_PBT_LIVE_MCP
  covers:
  - the-one-composed-convergence-PBT — random valid wiring over
- file: crates/holon-integration-tests/tests/gpui_mcp_replay.rs
  kind: harness
  covers:
  - mcp-driver-rung — drives a real running app through embedded MCP
- file: crates/holon-integration-tests/tests/journals_restart_survival.rs
  kind: harness
  covers:
  - restart-persistence(journals) — Journals view definition
- file: crates/holon-integration-tests/tests/layout_bridge_smoke.rs
  kind: harness
  requires_sut_caps:
  - SutBlockInteract
  feature_gates:
  - pbt
  covers:
  - layout-bridge-typelevel — type-level proof shared
- file: crates/holon-integration-tests/tests/local_ui_state_precedence.rs
  kind: harness
  covers:
  - local-ui-state-precedence — C8 LocalStateStore
- file: crates/holon-integration-tests/tests/logseq_org_vault_ingest.rs
  kind: harness
  covers:
  - logseq-vault-ingest — real LogSeq :org vault ingest
- file: crates/holon-integration-tests/tests/loro_memory_start_app.rs
  kind: harness
  covers:
  - backend-selection-di — a2 backend selection at start_app via DI
- file: crates/holon-integration-tests/tests/loro_projection_atomic_advance.rs
  kind: harness
  covers:
  - loro-projection-atomic — atomic base-advance fault injection for
- file: crates/holon-integration-tests/tests/loro_restart_unseeded_vault.rs
  kind: harness
  covers:
  - restart-persistence(loro-unseeded) — true-restart twin,
- file: crates/holon-integration-tests/tests/loro_sync_controller_pbt.rs
  kind: harness
  covers:
  - loro-multipeer-bridge — LoroSyncController over multi_peer
- file: crates/holon-integration-tests/tests/loro_unseeded_vault_split.rs
  kind: harness
  covers:
  - loro-unseeded-split — Loro-over-unseeded-vault split failure
- file: crates/holon-integration-tests/tests/matview_duplicate_row_repro.rs
  kind: harness
  covers:
  - matview-dup-forced-weights — duplicate matview row surfaced by
- file: crates/holon-integration-tests/tests/matview_reboot_duplicate_repro.rs
  kind: harness
  covers:
  - reboot-matview-dup — duplicate matview rows after reboot
- file: crates/holon-integration-tests/tests/multiline_serializer_test.rs
  kind: harness
  covers:
  - multiline-serializer — multiline org serializer unit check
- file: crates/holon-integration-tests/tests/org_roundtrip_pbt.rs
  kind: harness
  feature_gates:
  - pbt
  covers:
  - org-parse-render-fixedpoint — pure parser+renderer round-trip
- file: crates/holon-integration-tests/tests/perspective_slot_resolution.rs
  kind: harness
  covers:
  - perspective-slot-resolution — C8 root display slot resolves via
- file: crates/holon-integration-tests/tests/phantom_loro_exists_repro.rs
  kind: harness
  covers:
  - phantom-loro-exists — phantom-Loro-exists race (Phase 3.7 gate)
- file: crates/holon-integration-tests/tests/probe_link_marks_roundtrip.rs
  kind: harness
  covers:
  - link-marks-roundtrip — inline [[link]] marks round-trip through
- file: crates/holon-integration-tests/tests/query_equivalence_pbt.rs
  kind: harness
  feature_gates:
  - pbt
  covers:
  - testquery-equivalence — TestQuery vs production query
- file: crates/holon-integration-tests/tests/region_writeback_loss.rs
  kind: harness
  covers:
  - region-writeback-loss — region-writeback data-loss P0 repro
- file: crates/holon-integration-tests/tests/split_block_content_pbt.rs
  kind: harness
  feature_gates:
  - pbt
  covers:
  - split-content-routing — SplitBlock content routing, driven
- file: crates/holon-integration-tests/tests/tour_spike.rs
  kind: harness
  covers:
  - onboarding-tour-spike — onboarding-tour vertical-slice spike
- file: crates/holon-integration-tests/tests/turso_ivm_index_bug.rs
  kind: harness
  covers:
  - turso-ivm-index-corruption — Turso IVM
- file: crates/holon-integration-tests/tests/watch_guard_raii.rs
  kind: harness
  covers:
  - watcher-refcount-raii — H1 watcher refcount RAII on read-only
- file: crates/holon-integration-tests/tests/watch_ui.rs
  kind: harness
  covers:
  - uievent-stream-lifecycle — UiEvent stream happy-path +
- file: crates/holon-pbt-core/src/budget.rs
  kind: module
  requires_ref_caps:
  - RefSqlCardinality
  feature_gates:
  - otel-testing
- file: crates/holon-pbt-core/src/caching_proxy.rs
  kind: cap-plumbing
  provides_caps:
  - SutBackend
  - SutErrorLog
  - SutFrontendEmissions
  - SutFrontendEngine
  - SutLayout
  - SutLoroLog
  - SutOrgRead
  - SutOrgRender
  - SutRenderer
  - SutSqlProjection
  covers:
  - all-slices — memoises ONE SUT's own reads within a single tick
- file: crates/holon-pbt-core/src/cap_transition.rs
  kind: module
  requires_ref_caps:
  - RefSqlCardinality
  feature_gates:
  - otel-testing
  uses_cap_transition_macro: true
- file: crates/holon-pbt-core/src/capabilities.rs
  kind: cap-surface
  requires_ref_caps:
  - RefAdvice
  - RefApplyMutationMut
  - RefArrowNav
  - RefBackend
  - RefBlockTree
  - RefBlockTreeMut
  - RefBoot
  - RefBootMut
  - RefClock
  - RefClockMut
  - RefDocuments
  - RefDocumentsMut
  - RefEditorMirror
  - RefEditorMirrorMut
  - RefFocusMut
  - RefFocusRoots
  - RefGlobalFocus
  - RefHistoryExpectation
  - RefLayout
  - RefLayoutInteract
  - RefLayoutMutate
  - RefLifecycle
  - RefNavHistory
  - RefNavHistoryMut
  - RefPeers
  - RefPeersMut
  - RefPins
  - RefPinsMut
  - RefRenderExpr
  - RefSqlCardinality
  - RefTaskState
  - RefTaskStateToggle
  - RefToggle
  - RefToggleMut
  - RefViewSelectionMut
  - RefWatchesMut
  - RefWiring
  requires_sut_caps:
  - SutAdviceMatview
  - SutAppLifecycle
  - SutBackend
  - SutBlockCreate
  - SutBlockInteract
  - SutBlockTreeWrite
  - SutClockAdvance
  - SutDriver
  - SutEdgeFieldWrite
  - SutEditorMirrorRead
  - SutErrorLog
  - SutFixtureFs
  - SutFocusWrite
  - SutFrontendEmissions
  - SutFrontendEngine
  - SutHistory
  - SutHistoryWrite
  - SutLayout
  - SutLoro
  - SutLoroLog
  - SutLoroTaskState
  - SutMcpEmit
  - SutMutate
  - SutNavHistoryDrive
  - SutNavHistoryWrite
  - SutOrgRead
  - SutOrgRender
  - SutQueryResults
  - SutQuiesce
  - SutRenderer
  - SutSeamMutate
  - SutSqlProjection
  - SutTemplateInstantiate
  - SutViewControl
  - SutWatchRegister
  provides_caps:
  - SutEditorMirrorWrite
  - SutTransitionTarget
  covers:
  - all-slices — the component-oriented capability trait vocabulary every SUT arm and reference implements;
    selection is by presence of these.
- file: crates/holon-pbt-core/src/component_set.rs
  kind: cap-plumbing
  covers:
  - all-slices — the subsystem lattice (Wiring + projections) the bisection shrinker walks; membership
    only, providers live in the CapMap.
- file: crates/holon-pbt-core/src/composition.rs
  kind: cap-plumbing
  provides_caps:
  - RefBlockTree
  - RefEditor
  - SutBackend
  - SutBlockTreeWrite
  - SutEditorMirror
  - SutQueryResults
  - SutRender
  covers:
  - all-slices — CapMap typemap (insert fail-loud, expect
- file: crates/holon-pbt-core/src/content_generators.rs
  kind: generator
  gen:
  - extended arms (multi-byte, empty, whitespace-only, org-special prefixes, mid-word underscores) are
    the ONLY producers of those classes; every default consumer mixes them at ~4/10 weight EXCEPT the
    org-special spelling sub-arm and the empty/whitespace sub-arms, which are 1/N INSIDE extended_content_arm
    — so empty content and `:PROPERTIES:`-prefix
- file: crates/holon-pbt-core/src/fixture.rs
  kind: fixture
  gen:
  - 'value-level (not seed-level) corpus: a stored `Vec<T>` survives any generator/strategy edit, so a
    regression pinned here stays reproducible even after the distribution that first found it is retuned
    — the durable backstop for every gated-arm coverage hole below. `CaptureEnvironment` records the wiring
    + env flags a capture ran under; a replay under a different HOLON_PBT_* gate silently changes the
    transition alphabet, so `mismatch_report` must be consulted before trusting a green replay.'
- file: crates/holon-pbt-core/src/observables.rs
  kind: module
  requires_ref_caps:
  - RefBackend
- file: crates/holon-pbt-core/src/retry.rs
  kind: cap-plumbing
  covers:
  - all-slices — the eventual-consistency timing primitive; fails LOUD on deadline (returns the last Err),
    never a Skip/mask of divergence.
- file: crates/holon-pbt-core/src/wiring.rs
  kind: cap-plumbing
  env_gates:
  - HOLON_PBT_WIRING_AXES
  covers:
  - all-slices — storage/sync/actor manifest + validity rules + shrinkable `any_valid_wiring` strategy;
    fail-loud parse of env overrides.
- file: crates/holon-loro-testing/src/component.rs
  kind: sut-arm
  provides_caps:
  - SutBackend
  - SutLoroLog
  - SutLoroTaskState
  covers:
  - loro-slice — real `LoroBackend` CRDT read directly through `CoreOperations`; no SQL projection/CDC,
    so live view IS the store.
- file: crates/holon-loro-testing/src/invariants/loro_blocks_match_ref.rs
  kind: module
  requires_ref_caps:
  - RefBackend
  requires_sut_caps:
  - SutLoroLog
  slips-if-removed:
  - the SQL→Loro mirror drops or corrupts a block field (e.g. marks or parent) on outdent/split; the SQL
    arm still matches ref so the Loro-only divergence — the tree the CRDT-backed UI reads — ships silently
  oracle:
  - correspondence
  covers:
  - loro-blocks-field-match — Loro store block-field divergence from the reference model (content/parent/marks
    drift in the SQL→Loro mirror), non-seed blocks only
- file: crates/holon-loro-testing/src/invariants/loro_children_match_ref.rs
  kind: module
  invariant_ids:
  - inv-loro-children-match-ref
  requires_ref_caps:
  - RefBlockTree
  requires_sut_caps:
  - SutLoroLog
  slips-if-removed:
  - a CRDT move/indent reassigns a fractional index non-monotonically, leaving Loro siblings out of order
    while SQL sort_key stays correct; a UI driven from the Loro tree shows the wrong order and no other
    observer catches it
  oracle:
  - correspondence
  covers:
  - loro-sibling-order — Loro fractional-index child order vs the reference document order, per parent,
    non-seed blocks only
- file: crates/holon-loro-testing/src/invariants/loro_no_errors.rs
  kind: module
  invariant_ids:
  - inv-loro-no-errors
  requires_sut_caps:
  - SutLoroLog
  slips-if-removed:
  - an outdent/indent/split whose new parent is not yet a TreeID makes the controller log `Cannot resolve
    parent URI` and silently drop the event; the Loro tree diverges from SQL with no failing check to
    surface it
  oracle:
  - internal-consistency
  covers:
  - loro-sync-error — LoroSyncController dropped/failed-to-apply events (a gap in the SQL→Loro mirror),
    checked on the SUT alone
- file: crates/holon-loro-testing/src/invariants.rs
  kind: invariant
- file: crates/holon-loro-testing/src/peer_ops.rs
  kind: sut-driver
  gen:
  - NOT a generator — these are the SUT-side write primitives a PeerEdit transition drives; peer content
    distribution lives in `holon_pbt_core::content_generators::peer_content_strategy`.
- file: crates/holon-loro-testing/src/quiescence.rs
  kind: cap-plumbing
  covers:
  - loro-slice (full) — settle barrier before a post-merge read. Fails LOUD on timeout (returns `Err`
    + `tracing::error!`), never a silent return — a never-quiescing sync is surfaced to the caller (F7).
- file: crates/holon-loro-testing/src/ref_ext.rs
  kind: ref
  oracle:
  - 'correspondence — the peer-merge oracle REUSES the REAL Loro CRDT sync on shadow docs, driven from
    ref intent (primary block data passed in by parameter, peer edits applied to owned replicas), never
    SUT read-back. Strongest reused-not-under-test-engine oracle for concurrent-merge sibling order +
    text interleaving. Independence rests on the two Clone seams: `clock_feed` SHARES its cell (harness
    Lamport side-channel, not model state); `shadow_mesh` DEEP-FORKS every doc per proptest step/case.'
  covers:
  - peer-replicas — each `PeerRefState` owns its own block replica; this is CRDT modeling, not double-homing
    of the single-homed primary (which lives in the domain fragment and is passed in read-only).
- file: crates/holon-loro-testing/src/shadow_mesh.rs
  kind: oracle
  covers:
  - 'loro-slice (peer-mesh) — reference-side CRDT convergence predictor; runs REAL Loro merges fed from
    ref intent (see audit caveat: same engine as SUT ⇒ a pure Loro merge-ordering bug is in the blind
    spot).'
- file: crates/holon-loro-testing/src/sut_loro.rs
  kind: sut-arm
  provides_caps:
  - SutLoro
  covers:
  - loro-slice (full/peer-mesh) — owns the multi-peer Loro sync surface (AddPeer/PeerEdit/Sync/Merge)
    and reads the live LoroTree back.
- file: crates/holon-loro-testing/src/transitions/add_peer.rs
  kind: transition
  requires_ref_caps:
  - RefLifecycle
  - RefPeers
  - RefPeersMut
  requires_sut_caps:
  - SutLoro
  feature_gates:
  - otel-testing
  uses_cap_transition_macro: true
  covers:
  - loro-peer-add — new Loro-only peer instance
  rung: external CRDT peer add (out-of-process sync stimulus, no UI path).
- file: crates/holon-loro-testing/src/transitions/create_stale_loro.rs
  kind: transition
  requires_ref_caps:
  - RefDocuments
  - RefLifecycle
  requires_sut_caps:
  - SutFixtureFs
  feature_gates:
  - otel-testing
  uses_cap_transition_macro: true
  covers:
  - stale-loro-recovery — corrupt CRDT file at startup
  rung: external writes a corrupt .loro file before boot (fs stimulus).
- file: crates/holon-loro-testing/src/transitions/merge_from_peer.rs
  kind: transition
  requires_ref_caps:
  - RefLifecycle
  - RefPeers
  - RefPeersMut
  requires_sut_caps:
  - SutLoro
  feature_gates:
  - otel-testing
  uses_cap_transition_macro: true
  covers:
  - loro-merge-oneway — one-way peer delta import
  rung: external one-directional CRDT merge (peer -> primary), no UI path.
- file: crates/holon-loro-testing/src/transitions/peer_char_edit.rs
  kind: transition
  requires_ref_caps:
  - RefLifecycle
  - RefPeers
  requires_sut_caps:
  - SutLoro
  feature_gates:
  - otel-testing
  uses_cap_transition_macro: true
  covers:
  - loro-char-merge — char-level concurrent LoroText edit (NEVER
  rung: 'external DEAD TRANSITION (audit TR-DEAD): `weighted_generator` returns None unconditionally ->
    never emitted by any strategy. Character-level concurrent LoroText merges are UNTESTED (coverage hole).'
- file: crates/holon-loro-testing/src/transitions/peer_edit.rs
  kind: transition
  requires_ref_caps:
  - RefLifecycle
  - RefPeers
  - RefPeersMut
  requires_sut_caps:
  - SutLoro
  feature_gates:
  - otel-testing
  uses_cap_transition_macro: true
  covers:
  - loro-peer-edit — peer-side block mutation on LoroDoc
  rung: external edits a block on a peer's LoroDoc directly (CRDT stimulus).
- file: crates/holon-loro-testing/src/transitions/sync_with_peer.rs
  kind: transition
  requires_ref_caps:
  - RefLifecycle
  - RefPeers
  - RefPeersMut
  requires_sut_caps:
  - SutLoro
  feature_gates:
  - otel-testing
  uses_cap_transition_macro: true
  covers:
  - loro-bidi-sync — two-way LoroDoc convergence
  rung: external bidirectional CRDT sync between primary and peer.
- file: crates/holon-turso-testing/src/correspondences.rs
  kind: module
  requires_ref_caps:
  - RefAdvice
  - RefBackend
  - RefBlockTree
  requires_sut_caps:
  - SutAdviceMatview
  - SutBackend
  - SutSqlProjection
