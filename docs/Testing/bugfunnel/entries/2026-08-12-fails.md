---
id: 2026-08-12-fails
date: 2026-08-12
gap: ENVIRONMENT
secondary: null
status: OPEN
summary: >-
  `cargo check -p holon-frontend --target wasm32-unknown-unknown` fails on
  `getrandom`
source_line: 717
---

## Bug

(task-#22 registry/wasm triage lane; originally found by the
deterministic-scheduling lane on an untouched base, reproduced
byte-identically here; no gated test produced it) **`cargo check -p
holon-frontend --target wasm32-unknown-unknown` fails on `getrandom`**:
`error: The wasm32-unknown-unknown targets are not supported by default; you
may need to enable the "wasm_js" configuration flag. Note that enabling the
wasm_js feature flag alone is insufficient`
(`getrandom-0.3.4/src/backends.rs:194`). getrandom 0.3 requires BOTH the
`wasm_js` cargo feature AND `--cfg getrandom_backend="wasm_js"`; the feature
is set (root `Cargo.toml:176`, `frontends/dioxus-web/Cargo.toml:69-70`) but
the cfg exists NOWHERE — no `[target.wasm32-unknown-unknown]` rustflags in
`.cargo/config.toml`, `rg getrandom_backend` empty. Both halves proven
independently by the discovering lane (no cfg → this error; cfg forced →
"the `wasm_js` backend requires the `wasm_js` feature"). Lockfile carries
0.2.17/0.3.4/0.4.3, so a fix must target the 0.3 instance. Second defect at
the same site: `frontends/dioxus-web/Cargo.toml:41-44` asserts "we verified
`cargo check -p holon-frontend --target wasm32-unknown-unknown` is clean" —
a rotted claim that now misleads.

## Root cause

task-#22 registry/wasm triage lane, found by RUNNING A GATE NO LANE RUNS
(originally by the deterministic-scheduling lane on an untouched base;
reproduced byte-identically here) — no gated test verdict produced it:
**`cargo check -p holon-frontend --target wasm32-unknown-unknown` fails
outright on `getrandom`.** `error: The wasm32-unknown-unknown targets are
not supported by default; you may need to enable the "wasm_js" configuration
flag. Note that enabling the wasm_js feature flag alone is insufficient`
(`getrandom-0.3.4/src/backends.rs:194`). getrandom 0.3 needs BOTH the
`wasm_js` cargo feature AND `--cfg getrandom_backend="wasm_js"`; the FEATURE
is set (root `Cargo.toml:176`, `frontends/dioxus-web/Cargo.toml:69-70`) but
the CFG exists nowhere in the repo — no `[target.wasm32-unknown-unknown]`
rustflags in `.cargo/config.toml`, and `rg getrandom_backend` over the tree
is empty. Both halves proven independently by the discovering lane: without
the cfg → the error above (`00-baseline-wasm.log:371`); with the cfg forced
→ `The "wasm_js" backend requires the `wasm_js` feature for `getrandom``
(`01-baseline-wasm-flagged.log:47`). The lockfile carries getrandom 0.2.17 /
0.3.4 / 0.4.3, so a fix must target the 0.3 instance specifically. SECOND
DEFECT AT THE SAME SITE, and the reason this rotted unseen:
`frontends/dioxus-web/Cargo.toml:41-44` asserts in a comment that "we
verified `cargo check -p holon-frontend --target wasm32-unknown-unknown` is
clean" — the exact command in that comment now fails, so the comment
actively misleads (CLAUDE.md: comments describe the CURRENT state). NOT
FIXED — triage lane; diagnosed only.)

## Missing piece

NO gate builds this target at all — `rg wasm32-unknown-unknown` over
`justfile` and `.github/workflows/*.yml` returns ZERO hits, which is exactly
why the in-comment verification claim could rot unnoticed. Missing piece = a
recipe that builds the wasm frontend target (or deletion of the claim, if
the target is not supported).

## Remedy

OPEN 2026-08-12 — diagnosed, NOT fixed (triage-only lane). Fix direction:
add the `getrandom_backend` cfg for this target in `.cargo/config.toml`,
confirm feature unification reaches the 0.3 instance in `holon-frontend`'s
graph, then gate the command or drop the comment. Evidence:
`lane-logs/wasm-frontend-getrandom.log`; discovering lane
`lane-det-sched/lane-logs/00-baseline-wasm.log:371`,
`01-baseline-wasm-flagged.log:47`.
