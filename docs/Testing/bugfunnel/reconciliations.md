# Counter reconciliations (historical)

The legacy funnel kept its totals by hand, so the log carried
correction lines. Totals are computed from the entries now;
these are preserved as history and count for nothing.

- (+1 ENVIRONMENT 2026-08-16) D25.a lane-driver-fold, found by a fresh-context VERIFIER probing the branch the lane rewrote — no test verdict: **the headless driver's mounted-gate expand-toggle branch was DEAD for every real node** — `user_driver.rs:905` stripped the `block:` scheme before `find_expand_toggle` while the shadow builder stores the SCHEMED row id, so the lookup never matched and every drive fell through to the embedded-page branch; the store's own normalization comment NAMED the divergence and papered over it. HARNESS-ONLY: no production frontend calls the branch (GPUI synthesizes chevron clicks; the worker writes only the store). FIXED: schemed id threaded through, docs corrected, convention-bearing pins added (`find_expand_toggle_requires_the_schemed_id`); residual disclosed — no headless topology MOUNTS an expand_toggle node, so the end-to-end guard is `#[ignore]`d pending a mounting fixture (not a windowed port). COVERAGE secondary: keystone never draws ExpandToggle at all (task #23). See the ledger row of the same date for the full reconciliation.)
- (+3 COVERAGE 2026-08-09) reconciliation — 3 post-split COVERAGE rows were counted in the ledger table without matching increment lines (before this row header 98 and log-sum 61 both trailed the 101 COVERAGE ledger rows present); re-summing to the ledger ground truth, same recount pattern as the 2026-08-01 and 2026-08-04 reconciliations.)
- (+1 ENVIRONMENT 2026-08-04) reconciliation — the Ledger TABLE ROWS are ground truth and were re-counted mechanically (`scripts/bugfunnel-check.sh`, which now counts the rows itself); the header and the log had drifted from them because rows were appended without a matching increment line. Measured against the file as it stood BEFORE the same-day dogfood-sweep lines above: ENVIRONMENT 131 · COVERAGE 81 · PERCEPTION 59 · ORACLE 46 rows, against a header of 130 · 84 · 56 · 44. This line and the three below carry the log to those counts; no earlier line is rewritten. Counting rule for the recount: one table row = one counted bug, `A→B` in the Primary-gap cell counts as the RE-TRIAGED class B, and the table continues BELOW the `## Deferred perf` heading that interrupts it — a naive scan that stops at that heading sees only 123 of the rows)
- (-1 COVERAGE 2026-08-04) reconciliation, same recount — header 84 and log-sum 82 both exceeded the 81 COVERAGE rows actually present)
- (+3 PERCEPTION 2026-08-04) reconciliation, same recount — 59 PERCEPTION rows present against a header of 56; includes the 2026-07-16 `holon_rule` row re-triaged COVERAGE→PERCEPTION, which the header still counted as COVERAGE)
- (+2 ORACLE 2026-08-04) reconciliation, same recount — 46 ORACLE rows present against a header of 44)
- (+13 ENVIRONMENT 2026-08-01) reconciliation — rows counted in the header without increment lines; see task #24)
- (+8 COVERAGE 2026-08-01) reconciliation — rows counted in the header without increment lines; see task #24)
- (+9 PERCEPTION 2026-08-01) reconciliation — rows counted in the header without increment lines; see task #24)
- (+8 ORACLE 2026-08-01) reconciliation — rows counted in the header without increment lines; see task #24)
- (+  2026-07-21) dogfood-sweep batch (8 new rows below; recount = +3 ENV, +3 COV, +1 PERC, +1 ORACLE): GQL varlen unanchored-CTE wedge FIXED (COVERAGE, ENV secondary — only bites at vault scale), unregistered-GQL-edge silent-empty fail-loud (ORACLE, COV secondary), sidebar blank-row + depth-inversion cluster now root-caused to the convert/writeback chain below (PERCEPTION render-no-title-fallback, COV secondary — SUPERSEDES the earlier Directory-purge suspicion), slash-menu missing Delete / no `MenuExposure::Listed` (COVERAGE, PERC secondary — mirrors the convert_block_to_page COVERAGE row), MCP `execute_operation` block-create reports success-without-write (ENVIRONMENT — MCP drive-layer, not the keystone's driver path; ORACLE/fail-loud secondary), MCP screenshot phantom-500x500-window picker (ENVIRONMENT), editor stale-buffer clobber on a mid-focus agent write = 'Enter duplicates block' — new manifestation of the row-80 buffer-authority class the focus-gain `converge_on_render` fix does not cover, parity remedy = EditorBufferOwnership-2026-07-20.md (ENVIRONMENT, ORACLE secondary), convert_block_to_page/delete org-writeback round-trip defect chain (COVERAGE — titles never written to disk, `/` unsanitized, delete orphans the file → reingest = empty Page; the root producer of the sidebar cluster))
- (+  2026-07-21) entity-identity batch (3 new rows below, all ENVIRONMENT primary): boot panic on a spaced vault folder — the write-only `Directory` entity minted path-shaped `block:` ids via `EntityUri::from_raw`'s unschemed-string fallback, fatal under `panic="abort"`; classified ENVIRONMENT not COVERAGE because the keystone wiring never constructs `OrgModeSyncProvider`/`Directory` at all, so no alphabet widening could have reached it (FIXED by deleting the entity); plus the required-features blindspot that hid it — `-p holon-orgmode` runs zero PBT tests without `--features di` and reports success. Third row: the MutableTree/row-set desync the Directory fix UNMASKED — `MutableTree::remove` cascaded to descendants without disclosing them, so a later update to a survivor panicked at boot (FIXED by returning evicted ids and reinstating survivors).
- (+  2026-07-21) cycle-2: dead-red round-trip proptests COVERAGE, virtual-slot blank-panel PERCEPTION, recipient org-writeback-stuck ENVIRONMENT; retriages: B2 = environment artifact + covered, menu-layer dedup landed, V1-V3 live-confirmed)
- (+  2026-07-21) overnight sharing-track batch (12 new rows below; recount = +3 ENV, +5 PERC, +4 ORACLE): dogfood-round3 visual/behavioral findings V1-V4+B2-B4, structural-op double-advertisement follow-up, W5 F-undo inverse-crossing loss, W7 latent journal-date-page tag asymmetry, W4 acceptor-gate-not-wired; plus status updates: fork_at/N3/N4/N1-CRUD/mark-clamp FIXED+WOVEN, N6 CLOSED-AS-DESIGNED (stays in the ORACLE escape count — the funnel counts discovered escapes, later closure does not un-count them). + 2026-07-20 mobile soft-keyboard no-show (iOS+Android release, editable blocks + quick-open search): ENVIRONMENT primary (platform IME raise invisible to headless keystone AND windowed GPUI/CI — no rung sees `show_keyboard` fire) / COVERAGE secondary (no test asserts a focused text input claims a keyboard generation); FIXED for the search box by joining the `editor_focus_gained/lost` generation protocol in `search_ui.rs`, editor-only tap path unchanged/unreproducible as code — needs on-device confirmation. + 2026-07-20 slash-menu fix lane (GPUI dogfood 2026-07-20): bug a "Turn into page" silent no-op = ENVIRONMENT (FIXED — planner `for_page_under` + sync strip-then-dispatch + awaitable/toast), bug b menu-collapsed-to-one = ORACLE (FIXED — matcher id-exclusion + non-defaultable `MenuExposure` registry↔menu correspondence lock), keystone `block_to_page` oracle mirrored prod's content-as-path = ORACLE (FIXED — model-first `for_page_under`), sidebar-tree-empty-on-default-view = PERCEPTION (FIXED 2026-07-20 — `column.rs` renders a stacked scrollable collection at CONTENT height (eager rows from `children_snapshot`, not the `size_full` virtualized list) so it no longer collapses to 0 height inside the `view_mode_switcher`'s absolute wrapper; windowed detection test in `panel_scroll_spike.rs`; live-confirmed), jsonplaceholder poll-resync datatype mismatch = ENVIRONMENT (OPEN), MCP drive-layer trio = ENVIRONMENT (sidebar-click FIXED 2026-07-20 as a consequence of the same column content-height fix — the 0-height list committed no bounds; coord-click-focus + nav-repaint still OPEN). + 2026-07-19 engine-synthetic `convert_block_to_page` absent from every op-driven UI surface (slash menu, op-button toolbar, keyboard) — COVERAGE: the UI op source `create_profile_resolver.entity_operations` was built only from `dispatcher.operations()`, while the synthetic descriptors were appended solely in `OperationEngine::available_operations` (MCP/discovery path) which the profile path never calls; no test asserted synthetic ops reach `resolve_profile(row).operations`. + 2026-07-19 Android dogfood: persisted `ui.theme` ignored at boot — mobile boot built `HolonConfig::default()` and never read the saved `holon.toml`, so restart always booted `holonLight` (ENVIRONMENT; desktop `load_config` path unaffected). + 2026-07-19 GPUI dogfood-explorer batch (chain tip incl. block-to-page/templates/integrations-sidebar lanes): undo of a link-bearing block leaves inconsistent content+marks (ORACLE), fresh config dir without holon.toml fails to boot (ENVIRONMENT), boot floods 869+217 disclosed computed-field/profile degrades (ENVIRONMENT), toolbar ships dev Widget-Gallery+Inspector with no user search (PERCEPTION), empty-block placeholder overlaps typed content (PERCEPTION), daily-note bullets land as siblings-not-children + no back-nav (PERCEPTION). + 2026-07-18 template org :ID: copied into instances destroyed the template file on writeback (COVERAGE); 147 ledger rows total, recounted 2026-07-18 from the primary-gap column of the ledger table — supersedes two conflicting parallel-session distribution lines that had drifted below the true row count. + 2026-07-18 on-device/live dogfood batch: Android toolbar/menu icons render as tofu (icon font unbundled in the APK), automation-rule card overflows the narrow Android display, MCP raw-coordinate click into empty space wedged the request + `/health` >120s, keystone LeftSidebar CDC-settle 5s deadline flaked once under machine load. Prior narrative retained: + dioxus-web renders NO links at all — its `rendered_text` snapshot builder ignored `node.entity`'s `marks` and drew every block as plain text; ENVIRONMENT because the keystone never renders through the dioxus-web builders (a whole frontend uncovered), the shared marks model was correct and GPUI rendered it fine; post Android black-screen fix: gpui-mobile `finish_launching` runs the root-layout pre-warm `block_on` on the event-loop/present thread → re-entrancy wedge, first frame never presented; ENVIRONMENT because the keystone never instantiates the Android platform stack; + a second ENVIRONMENT escape found on-device the same run: boot PANIC reconciling the block-matview chain over a persisted DB (`Cannot drop system table __turso_internal_dbsp_state_*`); as of 2026-07-17, post PBT-audit F1: un-blinding the vacuous `inv-viewmodel-entity-ids-subset-of-data` exposed a real phantom-row-after-split/join (w4-web-05 family) that the standard keystone double-masks; + post dogfood #6: fresh-DB boot crash, Projects.org 6,245-line writeback destruction, dead org-writeback feed, dismiss_advice unwired, claude-history integration killed by auto-discovered entity, page-under-non-page via move_block, vault-scale SLO breach 23s p95, + 11 more; + 2026-07-20 GPUI dogfood-explorer batch (SqlOnly desktop, seeded colleague/link/url/journal/trailing-space vault): split_block destroys link marks — retained block keeps stale out-of-bounds marks + split-off block loses marks, both links gone on disk (ORACLE); navigate SLO breach 500-685ms at ~6-page scale, live banner fired (ORACLE); inline `[[link]]` breaks onto its own line in rendered/unfocused blocks (PERCEPTION); org link writeback emits `[[block:uuid][label]]` scheme prefix vs documented bare-id (ENVIRONMENT); `Journals/` vault dir spawns a duplicate empty "Journals" page beside the built-in shell (ENVIRONMENT); swallowed PANIC `row_origin.rs:285` on a multi-root journal rowset during nav (ENVIRONMENT); trailing whitespace trimmed from `content` projection while editor buffer keeps it (PERCEPTION); MCP `click_entity` "bounds never committed" on main-panel blocks → persistent focus wedge blocking agent editing (ENVIRONMENT))
- (+  2026-07-21) dogfood-explorer phase-1/2 batch (8 new rows below, F1–F8; recount = +2 ENV, +3 ORACLE, +1 PERC, +2 COV): F1 blank sidebar rows persist post-heal (ENVIRONMENT — heal is filewatch-only, no boot/unchanged-file sweep), F2 array-field (tags/requires) duplicates (ORACLE, ENV secondary — phase-2: current main reingest is idempotent, the 5×/4× is FROZEN LEGACY DB cruft, remedy reframed to a one-time dedup migration), F3 inline wiki-link renders block on 3 stacked lines (PERCEPTION), F4 backlinks "Linked references" invisible on existing vaults (ENVIRONMENT — seeded only into fresh index.org, no migration; phase-2 validated it renders on a fresh vault), F5 duplicate folder pages "Projects"×2/"Holon"×2 (COVERAGE, ENV secondary — phase-2 confirmed reproducible on current main, not just legacy cruft), F6 malformed on-disk org link (scheme-prefixed + unclosed ]]) written by link writeback (ORACLE, ENV secondary), F7 journals feed ignores declared ORDER BY DESC / sortkey (ORACLE), F8 boot stack-overflow abort on doc #+ID == heading :ID: self-parent cycle (COVERAGE, ORACLE secondary — fix lane in flight 2026-07-21 night))
- (+  2026-07-21) night: render-layer tree-indent INVERSION row below (+1 PERC, COV secondary) — distinct from the computed-depth rows; FIXED same night in the ui-indent-bullet lane)
