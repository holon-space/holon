# Keystone known reds — the full-depth registry

The per-weave land gate is `just keystone-smoke` (ONE proptest case). The
full-depth sweep (`just pbt general`, default 64 cases) reaches sequences the
smoke never draws, and today it fails roughly half its runs from the
pre-existing signatures below. That is why full depth runs as a NIGHTLY tier
(`just keystone-nightly`) instead of per-weave, and why the nightly is judged
against this registry rather than against "zero failures".

**The discipline:**

- A failure whose signature matches a row below is a **pass-with-note**: the
  nightly prints it as a `WARN` line and still exits 0.
- **ANY other signature is a regression to triage** — the nightly exits
  non-zero and prints the novel signature verbatim. Do not add a row to silence
  it; triage it first (`bug-gap-triage`), and only register it here if Martin
  ratifies it as a known red.
- A row is **removed** when its fix lands with a green soak (repeated
  full-depth runs no longer producing that signature). Rows are not archived
  in place — the registry describes what is red NOW.
- Between the fix landing and the soak confirming it, a row sits at status
  `fixed-pending-soak`. Only `known-red` rows classify, so from that moment a
  recurrence of the signature is reported as NOVEL — the fix is treated as
  believed, not proven, and the tier stops absorbing that signature silently.
  Delete the row once the soak is green; revert it to `known-red` (with the new
  payload decoded) if the soak brings it back.
- **The target end state is a MINIMAL registry of OWNED residuals, not
  necessarily an empty one** (Martin, 2026-08-04). The known-reds fix program
  closes when every family is either FIXED or is a low-frequency residual
  (order of one lifetime observation) carrying all three of: a corrected
  evidence trail decoded from a real payload, a NAMED owner, and a resurfacing
  trigger — a registered `Match pattern` that flips it back to open the moment
  it recurs. A residual without an owner and a trigger is not a residual, it is
  an unclosed family. Absence from a soak never closes a family with fewer than
  ~5 lifetime observations; those need a deterministic repro instead.

**How the matching works:** `scripts/keystone-known-reds.sh` parses THIS FILE.
The `Match pattern` column is the single source of truth for classification —
an extended-regex (`grep -E`) applied to each extracted failure signature line.
Editing a pattern here changes the nightly's verdict; there is no second copy
in the script or the justfile. A pattern may not contain `|` — that is the
markdown table separator; use character classes instead of alternation.

## End-state audit — known-reds fix program, 2026-08-05

Status of Martin's queue item *"fix ALL known-red families"*: **NOT yet
discharged.** Of the six families this program touched: four are FIXED pending
soak (`sidebar-focus-bind` joined them 2026-08-07), one is half-fixed
(`org-blocks-ref-diverge` cause A fixed, cause B `::img::0` OPEN — found by this
program's own sweeps), and one remains open
(`split-id-no-pairing`/`syn-real-mint`). Six families remain open in the registry
overall, including the four untouched Loro/caret/pinblock rows and the UNOWNED
`page-without-own-file` singleton.

**FIXED, pending soak** (fix landed with a red-first proof; row deletes once a
full-depth soak is green — soak absence alone is not the bar for any of them):

| Family | Cause | Lock |
|---|---|---|
| `org-render-echo-loop` | renderer emitted an unconditional blank line after a body | unit `render_of_parsed_disk_text_is_byte_identical` |
| `watch-rows-cdc-parent` | oracle parented template DEFINITION blocks at `no_parent` to win seed exclusion; the watch invariant applies no seed filter | keystone `watch-rows-template-child-parent` |
| `state-toggle-row-absent` | oracle decided panel visibility with a bare ancestor walk, ignoring the page stop and depth cap | unit `main_panel_visibility_stops_at_a_non_root_page` |
| `sidebar-focus-bind` | the sidebar barrier's 5s budget was paying for the layout's COLD BOOT, not for the row's `selectable` | the barrier's own two-phase split + probe `sidebar_bind_latency_probe.rs` |

**Half-fixed — row stays open because a SECOND cause is live:**

- `org-blocks-ref-diverge` — cause A (undo over-reverts file-ingested content
  blocks) FIXED, locked by keystone
  `undo-over-reverts-file-ingested-content-blocks`. Cause B (`::img::0` sub-block
  id remap; non-empty `only_in_ref`) OPEN, attribution pending a base-rev A/B.

**OPEN** (owner and resurfacing trigger per item; one is still UNOWNED) (the residual policy above):

- `sidebar-focus-bind` — ROOT-CAUSED and FIXED 2026-08-07 (ORACLE bug: the
  barrier's budget, not prod). Moved out of OPEN; see the registry row. Its
  `Match pattern` was CORRECTED earlier in this program (risk R10 materialised:
  an intervening landing rewrote the assertion, so the classifier was reporting
  a registered known red as a NOVEL regression and the nightly would have failed
  the tier for it).
- `split-id-no-pairing` / `syn-real-mint` — unfixed and NOT REPRODUCED at this
  base. The plan's lead cause (an N-bounded per-tick reconcile, "the 4th of 4
  splits") is REFUTED: decoding the full shrink shows the unpaired id stays
  `split-3` as earlier splits are removed, and the fully minimized panic is
  `split-0` with `Mapped: []` — one split, nothing paired. `block::split-N` is
  minted off `block_state.next_id`, which several transitions bump, so N does not
  index splits. Green control `four-splits-one-tick-all-pair` locks the shape on
  both the default and the corpus wiring.
- `page-without-own-file` — 1 lifetime observation, **UNOWNED**: it does not yet
  satisfy the residual policy and needs a triage owner.
- `loro-frontier-height`, `loro-stable-id-missing`,
  `pinblock-unrendered-target` — untouched by this program; owners in the table.
- `editor-caret-mirror` — FIXED 2026-08-22 (stale armed caret seed, see the row);
  both known shapes now pinned in `hand-authored-regressions/keystone.jsonl`.

**Standing caveat:** `editor-caret-mirror` and its text-half sibling
`editor-text-mirror` (one family, two rows — the caret row is listed first so
co-fired lines keep classifying under it) cannot be judged by any soak until
mirror-bearing wirings can be pinned — its engagement is `0/N` in Loro-only
draws, so a green run is evidence of DESELECTION, not of a fix.

## Registry

| Key | Status | Match pattern | Signature | Evidence | Task | Owner |
| --- | --- | --- | --- | --- | --- | --- |
| `syn-real-mint` | known-red | `per-tick reconcile: one synthetic per minted real id` | `harness.rs:757` assertion. **This is a BLOCK-LOSS detector, not a minting defect — the direction recorded here until 2026-08-10 was backwards and had been steering investigation the wrong way.** It fires when a real id the SUT ALREADY HELD disappears from `block_raw`: the `retired` rule (`harness.rs:685-697`, written for the undo→redo id burn) un-maps any pair whose synthetic is still live in the oracle but whose real id is now dead in the SUT, the un-mapped synthetic falls back into `synthetic`, and no fresh real exists to pair it with. Observed shape is `syn=[…], real=[]` — the OPPOSITE of the `syn=[], real=[...]` this row used to quote. Captured instance, with the diagnostic context added to the message (retired pairs + vanished ids): `per-tick reconcile: one synthetic per minted real id (syn=[block::split-8], real=[]); this tick RETIRED [(block::split-8, block:0fe3a4ba-9218-42d0-b434-e30248be41f9)] and the SUT LOST [block:0fe3a4ba-9218-42d0-b434-e30248be41f9] from block_raw`. Classify as a PROD-BUG CANDIDATE in the block-loss direction (the SUT lost data the oracle still holds), NOT as an id-reconcile defect. **SECOND SIGNATURE VARIANT — DIRECTION REVERSED (observed 2026-08-14):** the same assertion also fires as `syn=[], real=[EntityUri("block:9b48f876-…")]); this tick RETIRED [] and the SUT LOST []` — nothing retired, nothing lost, and the SUT holds a block the oracle NEVER ALLOCATED. That is the opposite direction from the block-loss shape above and cannot be produced by the `retired` rule (which needs a live synthetic to un-map). Treat it as a DISTINCT defect sharing one assertion: block-loss reads `syn=[…], real=[]`, unexplained-mint reads `syn=[], real=[…]`; a fix for one is not evidence for the other. Captures: `lane-logs/pump-redesign/inc2-serial-RED.log` (7 occurrences, armed `serial` schedule) and one occurrence in `lane-logs/pump-redesign/RED-inv-schedule-coverage.log`, which ran BEFORE schedule points existed — so the variant is not schedule-induced. In both, `inv-blocks-match-ref/org` was the FIRST panic and these were later ones. | Pre-existing since ≤2026-07-25; also fires in the windowed (GPUI) keystone, so it is not headless-only. **RE-CHARACTERISED 2026-08-10 (task #71), 2 captures, both with full context.** (a) `lane-logs/71/runs/ns-1786359363-2544-2.log:1803` — the message above; the lost row was the tail minted by the IMMEDIATELY PRECEDING `SplitBlock`, and it vanished during the next tick's `Indent`. (b) `lane-logs/71/runs/hunt-1786357478-76118-4.log:276` — same shape, two pairs at once, `syn=[block::create-5, block::split-4], real=[]`. Both drew `storage={Org, Turso}` (2/2): the org write-back → disk → re-ingest loop is the only path in those draws that can remove a `block_raw` row no op asked to delete, so instrument the org arm (log every store DELETE whose origin is the file-sync re-ingest), not the harness. **DO NOT TRUST ANY SHRINK ATTRIBUTED TO THIS FAMILY.** The composed harness panics inside `apply`, so a shrink candidate that panics for ANY reason still counts as failing; with several probabilistic panics live at once the reported minimal input routinely belongs to a different family. Proven in `hunt-1786357478-76118-4.log`: original panic line 276 = this assert, every shrink replay (lines 1356-2207) panics at `frontend_slice/components.rs:2088` (`SutOrgRender: read org file: NotFound …structural-page.org`), and the reported `minimal failing input` belongs to THAT panic. Hunt with `PROPTEST_MAX_SHRINK_ITERS=0` and read the FIRST panic only. **Gate context measured the same day:** `just keystone-smoke` (`PROPTEST_CASES=1`) is 77 green / 5 red over 82 runs = **6.1 % red across FIVE distinct signatures**, none of them this one (two `inv-sql-budget`, `inv-editor-caret/mirror`, `inv-main-panel-rows-match-focus`+INGEST DATA LOSS, `OpenTabViaModifierClick`); two further probabilistic panics appear in deeper hunts. So a green smoke streak is NOT evidence that this family is fixed, and "N consecutive green smoke runs" cannot certify any single family until the other four are closed. Full evidence + method: `lane-report-71.md`, logs under `lane-logs/71/`. | #62 | keystone id-reconcile (task #62) |
| `org-render-echo-loop` | fixed-pending-soak | `diverged from the oracle: .*"inv-org-render-fixed-point"` | `[inv-org-render-fixed-point] render != disk PERSISTED for <budget> — … a real echo-loop / oscillation`. The decoded instance is a ONE-BYTE difference: disk 277 bytes vs rendered-from-SQL 278, identical except a blank line the renderer emits between a multi-line block body and the next heading, which the parser does not write back. | Decoded 2026-08-04 from `fixture-logs-2026-07-31/keystone-nightly-20260731-083505-run2.log.zst` (85 panics). SUPERSEDES the prior "reproduces around empty-title headings" attribution — that was never true of any captured payload. All 85 corpus panics are the same shape: render−disk = exactly +1 byte, all on `forward-edge-page.org`. **FIXED 2026-08-04**: `render_headline_block` (`crates/holon-org-format/src/models.rs`) pushed a second newline after every non-empty body; it is now emitted only when the body's last line starts a list item (`body_needs_list_terminator`). The unconditional REMOVAL was tried first and refuted by verification — that blank line is load-bearing org syntax after a list-like line, and without it a following `#+BEGIN_SRC` child is swallowed into the list and LOST on write-back → ingest (real data loss on the `FileFormatAdapter` seam). Red-first proof: `holon-orgmode` `render_of_parsed_disk_text_is_byte_identical` (the 277-byte corpus fixture) RED at 278 bytes → GREEN; `blank_line_closing_a_list_body_survives_round_trip` locks the other half. Closed gate gap: `org_block_round_trip_pbt` — the only binary driving the adapter seam — was in NO `just` recipe, which is why the refuted attempt passed every gate; `just pbt orgmode` now runs it. Status stays `fixed-pending-soak` until a full-depth soak confirms the signature is gone; the classifier already treats a recurrence as NOVEL. The regression lock is that unit test, NOT a keystone JSONL case: write-back and the `render_org` tool share one `WritebackRenderer`, so an ordinary edit converges disk and render by construction — three probe shapes (multi-line body + following sibling, on the seeded forward-edge page and on journals) all replayed GREEN with the defect deliberately re-added. Reproducing it composed needs a second, unrelated ingredient (a write-back that does not land — veto/quarantine or an incremental splice), so a keystone case would lock that conjunction and go red for another family's reason once this one is fixed; disclosed deviation, ruled 2026-08-04. | #66 | org round-trip / holon-org-format (task #66) |
| `org-blocks-ref-diverge` | known-red | `diverged from the oracle: .*"inv-blocks-match-ref/[a-z_]+".*fields diverge from reference` | `[inv-blocks-match-ref/org]` reports `fields diverge from reference`. Decoded instance: SUT-org 33 blocks vs reference 31, `only_in_ref` EMPTY, the two extras (`block:7-cn4c9-9js-vi026hjesuj2-3`, `block:b-5-fx-g-k3x54-7v-8x`) both parented to `block:ref-doc-2` — a set-membership excess on the SUT side confined to one externally-ingested ref-doc, NOT a field diff. | Decoded 2026-08-04 from `fixture-logs-2026-07-31/keystone-nightly-20260731-191108-run1.log.zst` (123 panics — the dominant family). SUPERSEDES the prior JOURNALS-PROJECTION attribution: no journals block appears in the divergence. **VERDICT 2026-08-04: ORACLE bug — the reference's undo over-reverts file-ingested CONTENT blocks.** Two facts the first decode missed, both re-derived from the archived payload: (a) the SAME two ids are simultaneously spurious in `inv-blocks-match-ref/block_raw` AND `/matview`, so this is neither an org-projection defect nor the `!b.is_page()` ref-vs-SUT filter asymmetry the plan suspected — the reference simply does not hold the blocks, on every store; (b) proptest shrank the counterexample to THREE transitions, `CreateBlockUnderFocus → WriteOrgFile → UndoLastMutation`. Mechanism: `CreateBlockUnderFocus` is User-origin and pushes oracle undo snapshot S0 taken BEFORE the file exists; `WriteOrgFile`'s watcher re-ingest is INGEST-origin in prod (`operation_engine.rs:1220` — only User-origin ops push undo entries) so `engine.undo()` leaves its blocks alone, while the oracle's `seed_org_file` writes them INTO the snapshotted `block_state`; `pop_undo_to_redo` restores S0 and drops them, hence `only_in_ref` EMPTY and zero field deltas. This is the CONTENT-BLOCK sibling of the already-fixed doc-root case: `rematerialize_file_ingested_docs` fixed exactly this class for the file's ROOT page and explicitly left "any USER-origin children remain undone" — but a file's parsed content blocks are INGEST-origin, not user-origin, so that half stayed open. **FIXED 2026-08-04**: `files.ingest_origin_blocks` (a `BTreeSet` in `FileAdapterState`, outside the undo snapshot like `documents`/`next_doc_id`) records every block an ingest materialises (`insert_document`, `seed_org_file`, `bulk_add_blocks`; cleared on file rewrite and on `remove_document`), and `rematerialize_file_ingested` restores each such block VERBATIM from the pre-undo state when the snapshot restore dropped it. Restoring from the pre-undo state rather than from the ingest is what makes it prod-faithful: a block the user genuinely deleted is absent there and stays gone, and one a later user op edited was re-snapshotted by that op so it never reaches the path. NOT a weakening — no tolerance, wildcard or skip was added; if prod wrongly drops a file-ingested block on undo the invariant now reports it as `only_in_ref`. Red-first proof: hand-authored `undo-over-reverts-file-ingested-content-blocks` RED with `only in inv-blocks-match-ref/org (2): [block:ext-a, block:ext-b]` / `only in reference (0): []` / `field deltas (0)` on org + loro + block_raw + matview → GREEN; `ref-doc-0-undo-over-reverts-file-ingested-doc` (the doc-root half) stays green. Gate: `just hand-authored` 32/32 x3 consecutive; `just keystone-smoke` x3 substantive (`inv-blocks-match-ref/org` engagement 20/20, 31/31, 18/18). **THE FAMILY HAS TWO CAUSES; only cause A is fixed, so the row stays `known-red`.** Cause A (undo over-reverts file-ingested content blocks) is fixed and locked by the hand-authored case, so a regression of A is caught by `just hand-authored`, not by this row. **Cause B, OPEN, first seen 2026-08-05** in the second 64-case sweep: `inv-blocks-match-ref/org` with EQUAL counts on both sides (18 vs 18, 12 vs 12, 19, 21) and ONE id on each side — `only_in_actual` `block:<uuid>::img::0`, `only_in_ref` `block:<slug>::img::0`. That is the same logical image sub-block under two ids: the PARENT was remapped synthetic→real but the derived `::img::0` child id was not. It is the id-space-mangling cause the plan's F2 dossier refuted for cause A's payload (`only_in_ref` was empty there) and explicitly told us to keep in mind for other instances — non-empty `only_in_ref` is its signature. ATTRIBUTION OPEN: `::img::0` appears in NONE of the archived 2026-07-31 corpus logs, and the FIRST 64-case sweep of 2026-08-05 (same code) did not show it either, so it is intermittent and is either newly unmasked by cause A's fix (runs now get further) or newly landed by an intervening change. Needs a base-rev A/B before it is attributed — do NOT assume it is a regression of this lane's work, and do not assume it is not. Because the `Match pattern` is shape-blind (it cannot use alternation), this one row necessarily covers both causes. **Cause C, OPEN, first seen 2026-08-11** (marks-triage lane, 16-case soak at `70880807` with an unmodified tree): a FIELD DELTA on `parent_id`, not a set-membership difference — 12 blocks on both sides, `only_in` EMPTY both ways, `block:01f3548b-…: parent_id: sut=EntityUri("block:structural-page") ref=EntityUri("block:09b11a36-…")`. The SUT parents a SPLIT PRODUCT to the page while the reference parents it to a sibling block. It fires in `inv-blocks-match-ref/org` AND `/matview` AND `inv-block-parent/block_raw` simultaneously, so it is a store-side disagreement, not an org-projection artifact. Three consecutive shrink candidates showed the same shape with different uuids — a stable family, not a one-off. Neither cause A (set membership) nor cause B (`::img::0`): it is the FIRST cause in this family with an actual field delta. **Treat cause C as a REAL PROD-BUG CANDIDATE, not an oracle artifact — and note the open question is WHICH PARENT IS RIGHT.** Causes A and B were both adjudicated oracle-side, so the reflex here is to assume the reference is wrong again; nothing in the evidence supports that. The delta reaches `block_raw`, so if the SUT is the one in error a split product is being re-homed to the page in the STORE, which is data corruption, not a projection wrinkle. It needs its own investigation deciding the correct parent for a split product from the split semantics first, before either side is "fixed" to agree with the other. Evidence: `lane-report-triage-marks.md` §3, `lane-logs/triage-marks-*.log`. **A FOURTH shape, NOT reproduced, do not treat as confirmed:** a field delta on `marks` (`sut=None ref=Some([MarkSpan 0..3 Italic])`) seen once in a non-minimal shrink candidate of the same 2026-08-11 sweep; it did not survive shrinking and a 16-case soak produced ZERO `marks: sut=` occurrences. The mechanism the log itself states is that `holon-org-format` cannot express Italic with the available delimiters and degrades to `RenderFidelity::StylingDropped`, upstream of which `canonicalize_marks_against` WARNs that a mark span exceeds the content length and clamps it (4..14 over 9 chars, then 4..9 over 5, then 0..5 over 3) — i.e. marks surviving successive SplitBlocks WITHOUT being re-sliced to the shortened content, which is the likelier root than the render drop. The lock that would make it deterministic is known and owed: `InstantiateTemplate` (which carries `assets/default/Templates.org:102`, `** TODO Process each Signal/WhatsApp/Telegram *chat*`, whose `/WhatsApp/` the parser reads as an Italic span) → `SplitBlock` INSIDE that italic span → assert the mark is re-sliced, not clamped. | #76 | keystone oracle — ref undo model (task #76) |
| `split-id-no-pairing` | known-red | `resolve_sut_id: oracle-only id .* has no SUT pairing` | `types.rs` `resolve_sut_id: oracle-only id block::split-N has no SUT pairing … Mapped: [block::split-0, block::split-1, block::split-2]` — the first three editor splits reconciled, the fourth did not. Related to `syn-real-mint` but NOT its mirror (the mirror claim recorded here until 2026-08-10 was backwards): both signatures present as a synthetic with no paired real — this one at `resolve_sut_id` lookup time, `syn-real-mint` at the per-tick reconcile after a retire un-maps the pair. See the corrected `syn-real-mint` row: that assert is a BLOCK-LOSS detector (a real id the SUT already held vanished from `block_raw`). | 52 panics in `fixture-logs-2026-07-31/keystone-nightly-20260731-193535-run1.log.zst`. Registered 2026-08-04 — it had been firing unregistered, so the nightly could not issue a verdict. | #62 | keystone id-reconcile (task #62) |
| `watch-rows-cdc-parent` | fixed-pending-soak | `diverged from the oracle: .*"inv-watch-rows-match-ref".*CDC parent_id mismatch` | `CDC parent_id mismatch for block:tpl-c1 in watch '<id>': actual_ui_model=Some("block:tpl") expected=Some("__document_root__")` — a template child whose CDC-delivered `ui_model` parent is the template block while the reference still holds the document-root sentinel. | 23 panics in `fixture-logs-2026-07-31/keystone-nightly-20260731-193535-run1.log.zst`. Registered 2026-08-04. **VERDICT 2026-08-05: ORACLE bug — CONFIRMED, mechanism exact.** Reproduced live at this base (a 16-case full-depth run) and minimized by proptest to just `[InstantiateTemplate, SetupWatch]`. `block:tpl-c1` is not an instantiated child at all — it is the template DEFINITION child that `InstantiateTemplate`'s driver seeds UNDER `block:tpl`. The oracle parented BOTH definition blocks at `no_parent`, a deliberate documented shortcut whose only purpose was to make `seed_block_ids` classify them as SEED and drop them from the block-comparison invariants. But `inv-watch-rows-match-ref` applies NO seed filter — an `AllBlocks` watch returns every block — so the shortcut leaked out as `expected=__document_root__` (the `normalize_parent` image of `no_parent`) against prod's structurally-correct `block:tpl`. Prod was right throughout. **FIXED 2026-08-05 per Martin's ruling (resolve REAL parents, do NOT wildcard the sentinel):** new cap `seed_template_definition` seeds the definition child under the definition root so `parent_id` is truthful everywhere it is observed, and forces ONLY that block's `block_documents` entry to `no_parent` so the seed classification is unchanged. Parent truthfulness and seed classification are independent axes; the old code conflated them. The rejected alternative — treating `__document_root__` as a wildcard in `normalize_parent` — would also have hidden a genuinely wrong parent, which is exactly what this invariant exists to catch. Red-first proof: hand-authored `watch-rows-template-child-parent` RED with the registry signature verbatim (`CDC parent_id mismatch for block:tpl-c1 in watch 'query-tplparent': actual_ui_model=Some("block:tpl") expected=Some("__document_root__")`) under a mutation probe restoring the old `no_parent` seeding → GREEN with the fix. Gate: `just hand-authored` 33/33 x3 consecutive; the depth-16 run that produced the repro is green after, `inv-watch-rows-match-ref` engagement 9/9 and 40/40 (the invariant is trivially `Ok` with no watch registered, so the fixture registers a `parent_id`-carrying watch to exercise the parent arm). Status stays `fixed-pending-soak` until a full-depth soak confirms it. | #76 | keystone oracle — ref watch model (task #76) |
| `state-toggle-row-absent` | known-red | `could not resolve the state_toggle cycle intent` | `components.rs` `[toggle_state] click #1 failed for <block>: … in region main within 2s. <block> renders NO node in region main — the panel is not showing this block.` The reference believes a block is present and toggleable in the main panel; the panel does not render it. Same defect surface as `inv-main-panel-rows-match-focus`'s dropped-row arm, hit by the driver before the invariant can report it. | 23 panics in `fixture-logs-2026-07-31/keystone-nightly-20260731-191108-run2.log.zst`. Registered 2026-08-04. **VERDICT 2026-08-05: ORACLE bug — and the TIMING ARM DOES NOT APPLY, so nothing files to the latency track.** Martin's ruling conditioned the latency filing on "if the missing row appears at 10s". It never appears, at any deadline: the corpus payload shrank to `[SplitBlock, SplitBlock, Indent, BlockToPage, ToggleState]`, and `BlockToPage` mints a NEW page and re-homes the origin's children under it. The compiled main-panel query STOPS descending at any non-root page, so prod is RIGHT to render no row — waiting longer cannot change that. Only 8 entities were rendered, which also rules out the depth-20 cap of ledger entry 14 (that needs 20+ nesting). The defect is that the oracle decided main-panel visibility with a bare parent-chain walk (`is_descendant_of_any`) that honours neither the page stop nor the depth cap, so it offered a click target no user could click — the driver then burned its full 2s poll and dispatched nothing. **FIXED 2026-08-05** by one predicate, `RefBlockTree::main_panel_renders`, implemented via the panel query's own traversal (`descendant_within_stopping_at_pages` + the new single-sourced `query::MAIN_PANEL_MAX_DEPTH`), used by BOTH `main_editable_descendants` (the generator candidate set AND `inv-main-panel-rows-match-focus`'s required set) and `ToggleState`'s visibility precondition, which previously duplicated the wrong walk and said so in its own comment. This closes BOTH arms of the F5 cluster the plan predicted were one family. It also required fixing a real off-by-one in `descendant_within_stopping_at_pages` itself: it tested whether the CURRENT node is a page, but the CTE guard is on the node being descended FROM (the parent) — so the page stop never fired when the page was a DIRECT CHILD of the root, which is exactly the shape `BlockToPage` creates. DISCLOSED: this narrows an invariant's required set, which is a weakening in form; it is a correction in substance because the panel query's traversal IS the specification of what the panel renders, and demanding rows outside it asserted something false. The depth half was already the sanctioned action in ledger entry 14 ("un-comment once the candidate set honours the same depth cap"); this lane did NOT touch any pin, ceiling, or the quarantined entry-14 fixture — that stays with the read-budget lane. Regression lock is the unit test `main_panel_visibility_stops_at_a_non_root_page`, NOT a keystone JSONL case: the fix is a PRECONDITION/candidate-set narrowing and the hand-authored runner replays its sequence verbatim without evaluating preconditions, so a composed case naming the unrenderable target reproduces the signature (verified — it did, verbatim) but can never go green. Disclosed deviation, same shape as `org-render-echo-loop`'s. **SOAK BROUGHT IT BACK 2026-08-31 — reverted to `known-red` per the registry's own rule for a recurred `fixed-pending-soak` signature; the 2026-08-05 fix is NOT proven.** New payload decoded from the webpbt lane's workspace run (`lane-logs/webpbt-workspace-tests.log:18207`, minimal input at :20077): transitions `[BulkExternalAdd(doc=the day page), NavigateBack, ToggleState(block:bulk-1-0 -> Todo)]`. The bulk blocks are added UNDER the journal day page, then `NavigateBack` moves focus off it, and the toggle then names a block the panel no longer renders — it renders 4 entities (the day page, its virtual twin, the main panel, journals). This is the same candidate-set-vs-panel-rendered-set family, but the distinguishing element is the FOCUS CHANGE: `main_panel_renders` decides visibility against a focus that `NavigateBack` has already moved, so the 2026-08-05 page-stop/depth-cap predicate is evaluated against the wrong root. Recurrence is NOT attributable to that lane's diff — the signature is red with and without it (A/B: `lane-logs/webpbt-workspace-tests-Abase.log` vs `-tests.log`), and five runs across the lane and its verifier produced four distinct signatures plus one green, i.e. stochastic. Triaged as `docs/Testing/bugfunnel/entries/2026-08-31-state-toggle-row-absent-recurs-after-its-soak.md`. | #77 | main-panel projection (task #77) |
| `page-without-own-file` | known-red | `diverged from the oracle: .*"inv-every-page-has-its-own-file".*owns NO file` | `[inv-every-page-has-its-own-file] 1 page(s) not homed to exactly one own file: ["page <uuid> owns NO file (fileless — content lives only in th…"]` | 1 panic across the 2026-07-31 corpus. Registered 2026-08-04 for verdict completeness; a single lifetime observation, so soak absence alone must not close it. **PATTERN NARROWED 2026-08-07 (task #25).** The invariant emits TWO sub-signatures and the old shape-blind pattern absorbed both, so the DOUBLE-HOMED variant (`page <uuid> is DOUBLE-HOMED across 2 files [...]`) would have classified as this registered red instead of as novel. That variant is now FIXED — `materialize_page_identity_file` sourced a renamed page's previous on-disk home ONLY from `dyn AliasRegistrar`, which the composition root wires solely inside the `loro_enabled` arm, so in the shipped SqlOnly default the rename cleanup was unreachable and every renamed page kept its old file. `FileSyncController::doc_home` now records the file the controller itself wrote/ingested for each document, so the cleanup runs in every mode (`crates/holon-orgmode/tests/page_rename_retires_old_file.rs` locks both wirings). The pattern therefore stops at `owns NO file`: a DOUBLE-HOMED recurrence must classify as NOVEL. This row keeps ONLY the fileless sub-signature, which is untouched and still unowned. | — | UNOWNED — needs a triage owner before it can count as a residual (fileless sub-signature only) |
| `loro-stable-id-missing` | fixed-pending-soak | `missing STABLE_ID metadata` | `Node TreeID { peer: …, counter: … } missing STABLE_ID metadata`, panicking a `tokio-rt-worker` at `loro_backend.rs:495` (`block_uri_from_meta`) and surfacing as `inv-no-observed-errors` + `inv-org-render-fixed-point` (the killed worker owned the org-writeback fold, so disk stayed at the 15-byte `#+ID:` identity header while the store held the full document). The prior "shallow-snapshot / history-trimming, same class as `loro-frontier-height`" attribution was WRONG — it is a read/write race, unrelated to history. | **ROOT-CAUSED + FIXED 2026-08-06.** Observed rate at main=09914aad: 2 aborts in 8 `just hand-authored` runs (25%); failing runs abort in ~10–55s, passing runs take ~210s, so the abort is cheap to mistake for a fast green. Always the same case (the 5-transition `echo`/journals case) and always during BOOT, before transition 1. **Cause:** `LoroDocument::with_write` / `with_read` (`loro_document.rs:120-133`) take NO lock — they just call `f(&self.doc)` — while a block create is two separate doc-state steps (`tree.create()` then `meta.insert(STABLE_ID, …)`, `write_new_node` and `create_blocks`). A reader on another tokio worker therefore observes a live child node that is half-born. **[Present tense: the "take NO lock" clause describes the tree as it was when this red was adjudicated. `DocLock` (`crates/holon-loro/src/doc_lock.rs`) has since landed and `LoroDocument` routes `with_read` / `with_write_origin` through it. The remaining way out from under the guard is `LoroDocument::doc()`, whose blessed call sites are pinned per file by `loro_doc_escapes_match_the_allow_list` in `crates/holon-architecture-tests/tests/architecture_rules.rs`.]** `list_children` (`loro_backend.rs`) was the one reader that fed such a child straight into `block_uri_from_meta`, which panics by contract; the caller chain is `home_authority::children_of` → `sql_block_operations::children` → `block_cell_registry::live_children` → `list_children` (captured backtrace). The window itself is already RULED legitimate and handled next door by `snapshot_blocks_from_doc_settled`, which withholds such nodes and marks the snapshot unsettled — `list_children` had simply never been given that treatment. **Causal proof (not inference):** an env-gated 5ms sleep inserted between `tree.create()` and the `STABLE_ID` insert took the rate from 2/8 to **3/3 runs failing** (4/4/2 panics each), with every backtrace on the `list_children` line and no other reader implicated. **Fix:** `list_children` reads `read_stable_id` and withholds a child whose id has not landed, disclosed via `warn_half_born` (WARN, so `inv-no-observed-errors` stays honest). Withholding — not `Err` — is the required shape: `file_sync_controller.rs:3448` polls `ordering.children` on a 10ms/2s loop until the ids it expects appear and `?`-bails on `Err`, so absence is recoverable and an error would abort ingest. **Red-first:** `holon-loro` `list_children_withholds_a_child_whose_stable_id_has_not_landed` RED with the verbatim panic at `loro_backend.rs:495` → GREEN ×3. **Teeth, both directions:** same 5ms-widened race after the fix = 3/3 PASS with 40/57/53 half-born children withheld per run — the race fired ~50×/run and was survived every time. Gates below. Status stays `fixed-pending-soak` until a full-depth soak confirms; the classifier treats a recurrence as NOVEL. Residual (NOT fixed, no evidence of firing): the same panic contract still guards `get_all_blocks`, `get_blocks`, `get_block`, `LoroTreeView::build`, and `read_block_from_tree`'s PARENT read — the widened-race experiment implicated none of them, so they were left alone rather than guessing skip-semantics for each. | #78 | Loro backend (task #78) |
| `editor-caret-mirror` | fixed-pending-soak | `diverged from the oracle: .*"inv-editor-caret/mirror".*Caret mismatch` | `[inv-editor-caret/mirror] Caret mismatch on <block>: reference model cursor_byte=…, SUT tracked caret=…`. Only reachable in wirings that mirror the editor — engagement is 0 in Loro-only draws, so absence in a green run is not evidence of a fix. | Task #66 family, NEW 10. **A DETERMINISTIC 2-TRANSITION REPRODUCER EXISTS as of 2026-08-11 — this family previously had none.** Found by the budget-oracle verifier round as a side effect (it is the shape that blocks pinning `delete-backward-merge-budget`'s two-merge arm). Replayed by the fix owner out of tree, byte-identical, the SOLE violation: `reconciled composed sequence diverged from the oracle: [("inv-editor-caret/mirror", "[inv-editor-caret/mirror] Caret mismatch on block:parent: reference model cursor_byte=1, SUT tracked caret=2")]`. Case: `{"initial_state": {"wiring": {"storage_adapters": ["Turso"], "sync_adapters": [], "actors": []}}, "transitions": [{"SplitBlock": {"block_id": "block:parent", "position": 2}}, {"DeleteBackward": {"count": 2}}]}` — two backspaces at caret 0 merge the split tail back and then merge again, and the reference lands one byte behind the SUT on the second join boundary. The Turso-only wiring is load-bearing: the mirror is DESELECTED in Loro-only draws, and a Loro draw cannot run this case at all (no `SutBlockTreeWrite` in the CapMap). NOT pinned in `hand-authored` — it fires this family's red, so committing it would turn the gate red for a signature this row already owns; it is recorded here for the family owner to pin RED-FIRST when the fix is taken up. Probe log: `lane-logs/round2-h3-probe.log`. **FIXED 2026-08-22 (LogSeq-parity outliner lane).** Root cause: `HeadlessEditorMirror::route_keystroke`'s first-keystroke mount branch (`crates/holon-frontend/src/headless_editor_mirror.rs:578-595`) PEEKED the armed caret seed to initialise the tracked cursor but never called `consume_caret_seed`, so the seed stayed armed; the next `converge_editors` settle (`crates/holon-frontend/src/user_driver.rs:576`) then ran `adopt_armed_caret_seed`, which NOW found a tracked cursor and re-seated it to the stale seed — throwing away every caret move the keystroke run had made. The seed is documented as strictly single-use (`reactive.rs:5300`); the mount path simply was not honouring that. Fix = consume the seed in that branch, one call. **A/B evidence on the registered reproducer above:** with the fix reverted it reds byte-identically (`Caret mismatch on block:parent: reference model cursor_byte=1, SUT tracked caret=2`), with it applied it is green — `gap-outliner-run10-probe66-prefix.log` vs `gap-outliner-run9-probe66.log`. BOTH shapes are now PINNED in `hand-authored-regressions/keystone.jsonl`: `split-then-two-backspaces-caret-follows-second-join` (this row's reproducer, which the previous text deliberately left unpinned for the family owner) and `split-then-type-caret-follows-typed-text` (a NEW, simpler shape found by the parity lane: Enter at end of a block then typing into the new sibling leaves the caret at 0 while the text lands correctly). Full hand-authored corpus green 9/9 after the fix. Status is `fixed-pending-soak`, not `fixed`: the standing caveat below still holds — engagement is `0/N` in Loro-only draws, so a green soak is only evidence once mirror-bearing wirings are pinned, and the text-half sibling `editor-text-mirror` is a DIFFERENT defect and is untouched by this fix. | #66 | editor mirror wiring (task #66) |
| `editor-text-mirror` | known-red | `diverged from the oracle: .*"inv-editor-text/mirror".*Live editor text mismatch` | `[inv-editor-text/mirror] Live editor text mismatch on <block>:` followed by `reference: <text>` / `SUT MutableText: <text>` (the emitter's `\n`s are inside a `{:?}` Debug payload, so the whole thing stays on ONE extracted signature line — `correspondences.rs:126`, re-read against the live format string 2026-08-12). The TEXT half of the editor-mirror family whose CARET half is the `editor-caret-mirror` row above. It fires BOTH co-fired with the caret half (text printed first) AND — the case this row exists for — entirely ALONE. **TWO decoded payload shapes, one invariant; do not narrow this row to either.** (a) TASK-KEYWORD STRIPPING, the dominant shape and the only one with a captured log: `reference: "TODO 1"` / `SUT MutableText: "1"`, likewise `"DONE"`/`""`, `"DONE 1"`/`"1"`, `"DOING 1"`/`"1"` — the reference keeps the task keyword the editor view model strips out of its own buffer during live promotion (the mechanism of BugFunnel's 2026-08-10 task-#64 row). (b) LINK-SYNTAX STRIPPING: `reference: "[[qi]]"` / `SUT MutableText: "qi"` — the reference keeps raw wiki-link markup where the SUT holds the rendered label, the ORACLE gap of BugFunnel's 2026-07-31 row (the reference does not run the org lens on its create path). Both are reference-side lag on the same observable, NOT new defects. Same engagement caveat as the caret row: `0/N` in Loro-only draws, so a green run is evidence of DESELECTION, not of a fix. | REGISTERING AN ALREADY-KNOWN RED, not silencing a novel one. The family's caret half has been registered since 2026-08-04 and its `Match pattern` (`.*"inv-editor-caret/mirror".*Caret mismatch`) anchors on the CARET name, so a text-only failure matched nothing and the tier failed for a family that is registered — the same risk-R10 shape the `sidebar-focus-bind` row records. **CAPTURED 2026-08-12 by the deterministic-scheduling lane (task #20) on an UNTOUCHED base, and independently re-confirmed by verifier `verify-det-sched`:** `.claude/worktrees/lane-det-sched/lane-logs/20-prepush-run2-known-reds.log` classifies **12 novel panics, 0 known-red**, every one of them this signature firing ALONE, e.g. verbatim `reconciled composed sequence diverged from the oracle: [("inv-editor-text/mirror", "[inv-editor-text/mirror] Live editor text mismatch on block:<uuid>:\n  reference: \"TODO 1\"\n  SUT MutableText: \"1\"")]` (source run `19-prepush-run2.log`). A second occurrence the same day (LAT-floor verifier, `d-keystone.log`, workspace since purged) showed shape (b) co-fired AHEAD of `inv-editor-caret/mirror`. Corroborating in-repo mechanism notes: `docs/Testing/BugFunnel.md`'s 2026-07-31 row (OPEN, ORACLE, `see [[Some Page][lbl]]` vs `see lbl`) and `crates/holon-integration-tests/src/pbt/reference_state.rs:1741-1744`. VERIFIED against the captured log, not only reconstructed: re-running the classifier over `19-prepush-run2.log` with this row present turns all 12 novel panics into `known-red [editor-text-mirror]`, 0 novel. This row adds NO new hits to the 2026-07-31 fixture corpus (`just known-reds-fixture` unchanged), and it is placed AFTER the caret row deliberately: the classifier stops at the first matching key, so co-fired lines keep counting as `editor-caret-mirror` and the archived per-key counts are untouched. | #66 | editor mirror wiring (task #66) |
| `sidebar-focus-bind` | fixed-pending-soak | `LeftSidebar never bound a navigation.focus` | `components.rs` `[await_sidebar_intent] LeftSidebar never bound a navigation.focus (the row's ``action:``) click-intent for <block> with modifiers ClickModifiers { … } within 5s` — the sidebar's nested live_block watch fails to stream the target's selectable; latent arrival-order sensitivity, amplified (not created) by the reverted ORDER-BY snapshot change. | Fired 3× in a 64-case run at main-based tree 2026-07-31, and again 2026-08-05 (64-case run, `block:structural-page`, primary click). **PATTERN CORRECTED 2026-08-05 — risk R10 materialised.** An intervening landing rewrote the assertion to interpolate which wiring was awaited (`navigation.focus (the row's ``action:``)` vs `navigation.open_tab (…)`), and moved it from `SutFocusWrite::apply_navigate_focus` to `await_sidebar_intent` (`components.rs:1337`). The old pattern quoted `navigation.focus click-intent` as ADJACENT words, so it stopped matching and the classifier reported this known red as a NOVEL regression — i.e. the nightly would have failed the tier for a family that is registered. The pattern now stops at `navigation.focus`; it deliberately does NOT cover the `navigation.open_tab` variant, which is a different failure mode and correctly classifies as novel until triaged. **VERDICT 2026-08-07: ORACLE bug — the barrier's 5s budget was paying for the layout's COLD BOOT, not for the row's `selectable`. Prod is not implicated.** NOT REPRODUCED as a rate at this base: three consecutive `just hand-authored` runs were 34/34 + 9/9 (`lane-logs/repro-{1,2,3}.log`), refuting the "fires ~2 of 3 runs" premise — it is load-sensitive, which is why it shows up in 64-case sweeps and on busy machines. The prior ARRIVAL-ORDER attribution ("the nested live_block watch fails to stream the target's selectable") is REFUTED by direct measurement: `tests/sidebar_bind_latency_probe.rs` creates 12 pages back to back and finds the sidebar watch's row set EXACTLY equal to the sidebar's own SQL result, zero `[gen-drop]` events, and a steady-state bind of 9-12ms. There is no lost update. **Mechanism, proven by instrumenting the barrier:** the headless component never subscribes to the root layout (every real frontend's shell does on start), so the FIRST `snapshot_resolved` happens inside `await_sidebar_intent` and cold-boots the whole chain there — root slot query -> the three region `live_block`s -> the sidebar's own SQL -> its first CDC batch. Forcing the barrier's budget to 1ms reproduced the registry signature verbatim on `block:structural-page` with the new diagnostics attached: `MISS REASON: region left_sidebar renders no panel in the resolved tree at all` and `watch holds 0 row(s) []; the sidebar's own SQL returns 4 row(s)` (`lane-logs/forced-timeout.log`). The first bind of a process costs ~0.5-0.8s idle against 12ms steady state — under 4-way build contention that 6x headroom is what the family was spending. **FIX:** the barrier now waits in TWO separately-budgeted phases — `await_layout_rendered` (region panel present AND the sidebar page list has streamed its first rows, `LAYOUT_BOOT_BUDGET` 30s, its own loud message) and then the unchanged 5s wait for THIS row's wiring. Deliberately NOT done at boot: warming the watchers in `new_impl` makes the sidebar watch permanently live, which moves `watches=0` and every pinned per-transition SQL read ceiling with it (measured: `echo_loop_block_to_page_child_render_leak_parked` went red on `ToggleState.sql_reads` 68 dedup vs 63). Waiting inside the barrier starts no watcher the barrier did not start anyway, so NO budget moves. NOT a weakening: the 5s per-row teeth are untouched, and a row that truly binds no `selectable` still fails — it now fails with a `MISS REASON` naming which of the three states it was in. **Red-first proof, both directions, at a 1ms per-row budget:** RED before the fix (2 failed, the signature verbatim on `block:structural-page`, `lane-logs/proof-1ms-after-fix.log` for the intermediate panel-only predicate that was too weak), GREEN after (34/34 cases + 9/9 tests at a 1ms per-row budget, `lane-logs/proof-green-1ms.log`) — i.e. what remains in the per-row budget is ~0ms where it used to be the entire cold boot. Also added: `focus_path::click_intent_miss_reason` (the `state_toggle_miss_reason` twin) and a sidebar watch-vs-SQL row-set dump, so the next occurrence is self-diagnosing instead of blind. Status `fixed-pending-soak` until a full-depth soak confirms; the classifier treats a recurrence as NOVEL. | #77 | sidebar click-intent binding (task #77) |
| `pinblock-unrendered-target` | known-red | `PinBlock.sql_reads: [0-9]+ exceeds expected 17` | `[inv-sql-budget PINNED] PinBlock.sql_reads: N exceeds expected 17 + tolerance 1 = 18 (watches=0, docs=3) [PINNED ceilings gate RAW reads; dedup was M]`, N ≈ 90–141, wall ≈ 2.2s, spans ≈ 520–820. **N is the RAW read count.** When the budget gate moved to deduplicated reads (2026-08-06) the PINNED arm was deliberately kept on raw: this row's defect IS identical-binding redundancy (the 41 re-snapshots below), so a dedup comparison would subtract precisely what the pin exists to catch. The `(watches=…)`/`[PINNED …]` tail is appended AFTER the matched prefix so the pattern keeps matching. Co-fires with `inv-focus-roots` (right_sidebar) and `inv-main-panel-rows-match-focus`. | Ledger entry 14, RESOLVED 2026-08-04 by the measurement lane (+ verifier round): NOT a symptom of the focus-roots red and NOT a cost-model gap — all three reds share ONE cause, a pin target whose NESTING DEPTH exceeds the main-panel query's depth-20 recursion cap (`WHERE _vl2.depth < 20 … AND _vl2.depth <= 20`, documented at `crates/holon/tests/turso_storage_repros/tabs_main_panel_delivery.rs:130`). Past the cap the panel renders no row for it, so `click_entity_with_modifiers` (`user_driver.rs:719`) spins its 2s poll — 41 redundant re-snapshots of two `watch_view` SELECTs — and the pin never dispatches. Depth 12 → 17 reads; 21 → 89; 22 → 101. Width is irrelevant: a 40-block FLAT panel renders all 40 rows and pins the 40th for 17 reads. Oracle side, `main_editable_descendants()` applies no depth filter, so the generator offers targets the panel query truncates. Do NOT widen the ceiling. | #7 | read-budget measurement lane (task #7) |
| `history-ingest-create-unrecorded` | known-red | `inv-history-records-all-creates.*MISSED HISTORY` | `[inv-history-records-all-creates/block_history] MISSED HISTORY: block_history has N distinct op_group(s) but the oracle drove M UI create(s) that each must record ≥1 — K create(s) went unrecorded`. Verbatim instance: `reconciled composed sequence diverged from the oracle: [("inv-history-records-all-creates/block_history", "[inv-history-records-all-creates/block_history] MISSED HISTORY: block_history has 6 distinct op_group(s) but the oracle drove 7 UI create(s) that each must record ≥1 — 1 create(s) went unrecorded")]`. The pattern deliberately anchors only on the invariant name and the two stable words `MISSED HISTORY`, never on the counts (they track the draw) and never on a long adjacent word run (risk R10 above). | The DOCUMENTED, deliberately deferred residual split out of the R3 creation-slot fix — see `docs/Testing/BugFunnel.md` line 782 ("DISTINCT RESIDUAL GAP (split out, backlog)"), which is the authority. Watcher-ingested doc-page creates (`CreateDocument` → org file → `FileSyncController` ingest → the Loro→SQL consolidator `command_bus` as `OriginTaggedWrites`, `loro_module.rs:159`) do NOT pass through `DispatchingOperationEngine::execute_operation`, so `record_history` never fires and they record no op_group, while `harness.rs:300` counts each `ref-doc-N→uuid` remap as a UI create demanding ≥1. The invariant is a LOWER BOUND, so mixed sequences mask it; it surfaces when the draw is dominated by doc-ingest creates. Observed in the smoke log `lane-logs/general24.log` (six occurrences, all 6-vs-7 with `CreateDocument` in the sequence's budget trail). Deferred fix direction: route ingest doc creates through the history-recording engine seam, or record the consolidator's projected creates. | — | history recording seam — ingest doc creates (backlog per BugFunnel.md:782) |
| `history-join-phantom-row` | known-red | `inv-history-no-phantom-rows/block_history.*PHANTOM HISTORY` | `[inv-history-no-phantom-rows/block_history] PHANTOM HISTORY: 1 block id(s) recorded in block_history are unknown to the reference (never created/known): [EntityUri("block:parent")]` with `history ids: 33 recorded` / `ref universe (live ∪ ever-created): 38 known`. **The MIRROR DIRECTION of the registered `history-ingest-create-unrecorded` row above**: that one fires when the SUT records FEWER op_groups than the oracle drove creates (history missing), this one when `block_history` carries an id the reference's universe does not acknowledge (history in excess). Note the universe is the LARGER set here (38 vs 33 recorded), so this is not a count shortfall — one SPECIFIC seeded id, `block:parent`, is absent from the reference's `live ∪ ever-created` while the append-only history still holds its rows. Pattern anchors on the invariant name plus the two stable words `PHANTOM HISTORY` and never on the counts or the id (both track the draw) — the risk-R10 discipline the sibling row states. | **CAPTURED 2026-08-12 by the deterministic-scheduling lane (task #20) on an UNTOUCHED base — pre-existing, NOT that lane's work — and independently re-confirmed by verifier `verify-det-sched`.** Classified NOVEL at the time because no row covered it: `.claude/worktrees/lane-det-sched/lane-logs/18-prepush-known-reds.log` reports `FAIL: 1 novel panic(s), 29 known-red panic(s)`, the novel one being this signature (source run `17-prepush.log:968`). Draw context read from the authority line immediately below the panic: `authority: block-CRUD=Sql(SqlOperationProvider); projection-sinks=Sql(block_raw,matview); org-writeback=ON`, wiring `storage={Org, Turso} sync={} actors={MCPServer, ActionEngine}` — so the org write-back → disk → re-ingest loop is live in the failing draw, which is the same arm the `syn-real-mint` row names as the only path that removes `block_raw` rows no op asked to delete. Bisect verdict in the same log: `lowest MEASURED failing layer: matview/SQL … NOT proven first-divergent`. **NOT ATTRIBUTED — do not assume ORACLE by analogy.** The two prior members of this family (BugFunnel:743, :745, both 2026-07-17) were both reference-universe completeness gaps and both FIXED, which makes "the oracle is wrong again" the reflex; nothing in this payload supports it yet, and `block:parent` is a SEEDED id rather than a synthetic one, so neither prior mechanism (`seed_boot_journal` omission, `block:gen-` not recognised by `is_composed_minted_synthetic_id`) applies as written. A third recorded instance with a different payload — `EntityUri("block:c2")` after a backspace-JOIN — sits at `crates/holon-integration-tests/tests/fixtures/dogfood-recorded/task_keyword_join_caret.feature:31-40`, where the producing scenario is deliberately COMMENTED OUT so the suite stays green; whether it is the same cause is open. ZERO occurrences across all eight archived 2026-07-31 corpus logs, so this row adds no fixture hits. | — | UNOWNED — needs a triage owner (mirror of the `history-ingest-create-unrecorded` seam) |
| `delete-backward-merge-budget` | fixed-pending-soak | `DeleteBackward\.sql_reads: [0-9]+ dedup .* exceeds expected 5` | `inv-sql-budget` as the SOLE violation: `DeleteBackward.sql_reads: 22 dedup (raw 28, 6 redundant re-executions) exceeds expected 5 + tolerance 5 = 10 (watches=0, docs=3)`, telemetry `reads=28 (dedup 22)/5 writes=5/0 ddl=0/0 tol=5 … state=b27/d3/w0/r4`, and the block count drops by one across the transition. The `expected 5` in the pattern IS the defect — it is `REACTIVE_BASE`, the state-blind keystroke constant — so after the fix a merge can no longer produce this string (it expects 24 at this shape) and a recurrence means the branch was mis-recorded. | **ORACLE bug, not a regression — the number was never calibrated against the merge path.** Attributed 2026-08-11 by the marks-triage lane: deterministic hand-authored replay of the keystone's own minimal counterexample (`SplitBlock{block:parent, 2}` → `DeleteBackward{1}`, wiring `storage={Turso}`), byte-identical assertion 4/4 runs across THREE revisions (`70880807` pre-B2, `cdcf59bf` main+B2+ingest, `91d3d0f6` C1) and independent of the drawn wiring, with the liveness smoke green in every tree. Mechanism: `SplitBlock` leaves the editor on the NEW tail with the caret at 0, so the backspace is not an in-buffer delete but a JOIN into the predecessor — `crates/holon-integration-tests/src/pbt/transitions/delete_backward.rs` ignored BOTH closure arguments and modelled a pure keystroke (`reads: REACTIVE_BASE, writes: 0`), which no merge can fit. **FIXED 2026-08-11** by making the budget state-aware: `RefEditorMirrorMut::note_backspace_joins` records how many merges the backspace run performed (recorded at apply time, like `last_open_tab_activated`, because `expected_sql` only ever sees the POST-apply reference where a merge and a mid-line delete are indistinguishable), `RefSqlCardinality::last_backspace_joins` exposes it, and the budget charges `joins ×` the `JoinBlock` budget on top of the one reactive base. The join cost is SINGLE-SOURCED as `join_block::join_expected_sql` and shared with `JoinBlock` itself — DeleteBackward-at-0 IS that operation, and two budgets for one op drift by construction. At `joins = 0` the formula reduces EXACTLY to the old constant, so the pure-keystroke arm is unchanged: the old expectation is now visible as the zero-merge case of the general one. Red-first proof: hand-authored `delete-backward-merges-previous-block-budget` RED with the signature above verbatim → GREEN. **The 6 redundant re-executions inside the merge are NOT absorbed** — the ceiling is on dedup reads (22 measured vs 24 expected), the raw 28 stays in the telemetry line, and the four identical-binding `SELECT content FROM block_raw WHERE id = 'block:parent'` + three `SELECT * FROM "block" WHERE "id" = $id LIMIT 1` re-executions belong to the task-#15 roster. DISCLOSED SLACK: the inherited `JoinBlock` tolerance is `2 × cdc_tolerance` = 70 at this shape, far above the 2-read headroom the expectation alone already has; tightening it is a JoinBlock-wide measurement campaign, not this fix. Status `fixed-pending-soak` until a full-depth soak confirms; the classifier treats a recurrence as NOVEL. Evidence: `lane-report-triage-marks.md` §1, `lane-report-budget-oracle.md`, `lane-logs/budget-oracle-{red,green}.log`. **RESIDUALS — the branch selection is right, the NUMBER is carried by inherited tolerance (verifier round, 2026-08-11; none of these are fixed here, tightening is a measurement campaign for the budget/#15 track):** (a) the tolerance is applied to the WRITE and DDL ceilings as well as reads (`transition_budgets.rs:264/326/342`), so at `joins=1` the ceilings are `reads 99 / writes 81 / ddl 75` against measured `22 / 5 / 0` — a `DeleteBackward` that started issuing DDL would pass SILENTLY, which is the sharpest edge here; (b) the slack SCALES with `count`: `tolerance = 5 + joins × 70`, and `count` may reach 4, so an all-joins run tolerates 285 (~366 reads / 309 writes / 285 ddl) — at that shape it is not meaningfully a budget; (c) MEASURED under-count, not hypothetical — the two-merge shape (`SplitBlock{block:parent,2}` → `DeleteBackward{count:2}`) reads `reads=39 (dedup 24)/24 writes=7/6`, i.e. writes are already OVER the expectation on the first non-pinned shape and pass only on the slack, because the formula charges each join but charges NOTHING for the ordinary keystrokes in the same run (under the old 5-tolerance regime that shape would have been a violation); (d) the `joins ≥ 2` arm and `join_expected_sql` AS CALLED FROM `JoinBlock` are both unexercised by any regression case — across all 60 hand-authored cases there is exactly ONE `DeleteBackward` budget line (the pin) and ZERO `JoinBlock` lines, and the two-merge shape CANNOT be pinned green today (see the `editor-caret-mirror` row: that exact case is a deterministic red for THAT family); (e) the sibling path keeps the same defect — `press_key.rs:252-264` performs the identical caret-at-0 join under an unconditional `Update + Create` constant, which fails to red only because `Create` is dearer than `Delete` (28 vs 24 reads), i.e. state-blind in the LOOSE direction where `DeleteBackward` was state-blind in the tight one. NAMED FOLLOW-UP, deliberately not fixed in this lane. | #15 | `inv-sql-budget` / redundant-read roster (task #15) |
| `deletebackward-sql-reads-budget` | known-red | `DeleteBackward\.sql_reads: [0-9]+ dedup \(raw [0-9]+, [0-9]+ redundant re-executions\) exceeds expected 5 \+ tolerance 5 = 10 \(watches=0, docs=[0-9]+\)` | `inv-sql-budget` as the SOLE violation: `DeleteBackward.sql_reads: 16 dedup (raw 21, 5 redundant re-executions) exceeds expected 5 + tolerance 5 = 10 (watches=0, docs=4)`. Shape: NO SplitBlock and no merge in the sequence — `FocusEditableText(block:c1)` then `DeleteBackward{count:1}`, wiring `storage={Turso} sync={} actors={}`, docs=4; a second capture is the identical sequence with `actors={MCPServer}`. Pattern anchors on the structure and the state-blind `expected 5 + tolerance 5 = 10` ceiling, never on the measured counts (they track the draw). SAME ASSERTION as the `delete-backward-merge-budget` row above, DIFFERENT shape: that row's fix charges `note_backspace_joins` merges on top of the zero-join constant, and at THIS shape the run still expects the zero-join constant (10) while measuring a join-sized read set (16 dedup) — the recurrence its own text names ("a recurrence means the branch was mis-recorded"), consistent with its residual (e): the join detection does not engage on this focus-then-backspace path. That row stays `fixed-pending-soak` (its A/B history is untouched); only this `known-red` row classifies. | **Pre-existing at base 83aa988a — A/B by deterministic probe replay 2026-08-25 (trio lane = NOT-CAUSED).** The shrunk probe replayed against a clean `git archive` extraction of 83aa988a (no trio changes) reds BYTE-IDENTICALLY with the signature above: tip log `trio-lane` scratchpad `budget-attrib/tip-run1.log:188`, base log `budget-attrib/base-run5-orch.log:1010` (session scratchpad `/private/tmp/claude-501/-Users-martin-Workspaces-pkm-holon/068962eb-df7b-4d8e-b6ac-92217e6545ec/scratchpad/`). Deterministic replay, so the single-run A/B is decisive. Reproduction, hand-authored sidecar form (NOT pinned in `hand-authored` — each fires this row's red, so committing them would turn the gate red for a signature this row owns; recorded for the family owner to pin RED-FIRST when the fix is taken up): `{"name": "trio-attrib-min-turso-nomcp", "initial_state": {"wiring": {"storage_adapters": ["Turso"], "sync_adapters": [], "actors": []}}, "transitions": [{"FocusEditableText": {"block_id": "block:c1"}}, {"DeleteBackward": {"count": 1}}]}` and `{"name": "trio-attrib-min-turso-mcp", "initial_state": {"wiring": {"storage_adapters": ["Turso"], "sync_adapters": [], "actors": ["MCPServer"]}}, "transitions": [{"FocusEditableText": {"block_id": "block:c1"}}, {"DeleteBackward": {"count": 1}}]}` (both also in `budget-attrib/probes.jsonl`). | #15 | `inv-sql-budget` / redundant-read roster (task #15) |
| `typechars-sql-reads-budget` | known-red | `TypeChars\.sql_reads: [0-9]+ dedup \(raw [0-9]+, [0-9]+ redundant re-executions\) exceeds expected [0-9]+ \+ tolerance 5 = [0-9]+ \(watches=0, docs=[0-9]+\)` | `inv-sql-budget` as the SOLE violation, on `TypeChars`: e.g. `TypeChars.sql_reads: 16 dedup (raw 21, 5 redundant re-executions) exceeds expected 7 + tolerance 5 = 12 (watches=0, docs=4)`. SAME FAMILY as `deletebackward-sql-reads-budget` above — a state-blind keystroke ceiling met by a read set inflated with identical-binding re-executions — on a DIFFERENT transition. One deliberate difference from that row's pattern: it pins `expected 5` literally because that constant IS its defect, whereas here the expectation TRACKS THE DRAWN STATE (7, 11 and 17 all observed), so this pattern anchors on the structure and the `tolerance 5 … watches=0` tail and never on the counts — pinning a number here would mis-classify the next draw as novel. The redundancy itself is the task-#15 roster, not a new defect. | **PRE-EXISTING at `904c2563` — A/B by full-keystone run, 2026-08-30, quarantine-triage workspace.** BASE (`crates/holon-filesystem/src/file_sync_controller.rs` replaced by a `jj file show -r @-` extraction, asserted `PathIntent` hits = 0 in the run's own log header): reds with `16 dedup (raw 21, 5 redundant re-executions) … expected 7 + tolerance 5 = 12 (watches=0, docs=4)` ×15 including the `Test failed:` line, plus an `18 dedup (raw 49, 31 redundant) … expected 11 … = 16` variant (`scratchpad/ks_base_full.log`). LANE TREE (alias + contested-path change present): the BYTE-IDENTICAL `16 dedup … expected 7 … = 12` signature ×21, same `Test failed:` line (`scratchpad/ks_mine2_full.log`). The land gate at the rebased tip saw the same family at a larger draw — `23 dedup (raw 103, 80 redundant) … expected 17 + tolerance 5 = 22` (`/tmp/alias-gate-keystone.17166.log`). Magnitudes track the draw; invariant, transition and shape do not. **REFUTED as caused by the contested-path refusal**, which is what it was first attributed to: `refuse_contested_path` issues ZERO SQL (a single in-memory `doc_home` scan), and `AMBIGUOUS PAGE-FILE PATH` occurs 0 times in ALL THREE logs — it never executed in any failing run. **Shrink hazard, checked rather than assumed:** the `bulk-add-sibling-order-under-journals` row warns that sweeps shrink INTO an `inv-sql-budget` TypeChars violation owned by another family; for the land-gate capture the FIRST panic (line 296) IS this signature, so it is genuine there. On any re-run read the FIRST panic and hunt with `PROPTEST_MAX_SHRINK_ITERS=0`. | #15 | `inv-sql-budget` / redundant-read roster (task #15) |
| `drawer-open-matches-ref` | known-red | `drawer block:default-[a-z]+-sidebar rendered open=true but reference says open=false` | `inv-drawer-open-matches-ref` as the SOLE violation: a sidebar drawer the SUT paints OPEN while the reference model has it CLOSED, e.g. `[inv-drawer-open-matches-ref] drawer `block:default-right-sidebar` rendered open=true but reference says open=false`. Both sidebars have been seen (`left` and `right`), so the pattern takes the region as a character class and never pins one — the region is the drawn state, not the defect. (A `|` alternation is forbidden here: it is the registry table's own column separator.) The direction is always SUT-open/ref-closed; the reverse (`open=false` vs `open=true`) is NOT covered and must classify as novel until triaged. The run also self-reports `lowest MEASURED failing layer: viewmodel (inv-drawer-open-matches-ref) — NOT proven first-divergent`, so do not read it as a viewmodel defect without re-measuring the layers below. | **PRE-EXISTING at `6eec7606` — A/B by population, 2026-08-30, breadcrumb-root workspace.** `just keystone-smoke` ×10 per tree, serialized under one lock, `PROPTEST_MAX_SHRINK_ITERS=0`, separate `CARGO_TARGET_DIR`; base tree extracted with `git -C <primary repo> archive 6eec7606` (sentinel-checked non-empty, confirmed to lack the lane's `region_view_root`). BASE 1/10 failures (run7, `block:default-left-sidebar`); LANE `c38afa06` 0/10. The signature therefore reproduces with the lane's changes ABSENT and the lane elevates nothing. Second independent sighting: the hunt lane's 64-case run, 2026-08-30. Rate is low enough that a single smoke run is not evidence either way — characterise by population, never by one run. | — | unowned — sidebar drawer open-state vs ref |
| `loro-frontier-height` | known-red | `frontier change present` | Panic at `loro_backend.rs` `doc_lamport_height`: `doc.get_change(id).expect("frontier change present")` — a frontier id whose change is not retrievable (suspect: shallow-snapshot history trimming). | Fired 1× in a 64-case run at main-based tree 2026-07-31. | #78 | Loro backend (task #78) |

## `cargo test -p holon-integration-tests --lib --features pbt` known reds

Separate suite from the composed nightly above (this is the crate's own unit
tests, not `general_e2e_composed_pbt`), tracked here for the same
pass-with-note discipline. `scripts/keystone-known-reds.sh` only classifies
composed-nightly logs, so it does not consume these rows automatically — they
are a manually-maintained baseline for judging this suite's local runs.

**Baseline established 2026-08-06** from three consecutive runs at
main=6ec42f1a (`/opt/homebrew/opt/parallel/bin/parallel --semaphore --id
holon-build -j4 --fg -- cargo test -p holon-integration-tests --lib --features
pbt`):

| Run | Wall time | Passed | Failed |
| --- | --- | --- | --- |
| 1 | 89.09s | 291 | 10 |
| 2 | 192.23s | 270 | 31 |
| 3 | 67.39s | 290 | 11 |

Run 2's wall time is >2x run 1/3's — a symptom of concurrent machine
contention (other lanes sharing the `holon-build` semaphore slot), not a
change in the suite. Its 21 extra failure names beyond run 1's set are NOT
registered below: they never recurred in run 3 (a clean, non-contended run),
so a single contended sample is insufficient evidence they are real
intermittent reds rather than contention artifacts (timeouts, resource
starvation) — see the flagged list at the end of this section. Both run 1 and
run 3 are close in wall time to each other and neither shows machine
contention symptoms, so their intersection is treated as the stable baseline.

**Stable family — right-sidebar pin/focus-roots (9 tests, identical across
all 3 runs)**, attributed to the SAME region-literal typo documented at
`docs/Testing/BugFunnel.md:575` (OPEN/ESCALATED, uncorrected): the seed GQL
`default-right-sidebar::src::0` filters `fr.region = 'right'` while
production `focus_pin` writes `navigation_history.region = 'right_sidebar'`
(`Region::RightSidebar.as_str()`), so the literal never matches and the right
sidebar never shows a pin, in prod and in every SUT/oracle path that exercises
it:

| Key | Status | Match pattern | Signature | Evidence | Task | Owner |
| --- | --- | --- | --- | --- | --- | --- |
| `lib-nav-history-shift-action-region` | known-red | `the block bullet's shift_action pins into the right sidebar only` | `components.rs:2756` `pbt::frontend_slice::components::tests::headless_nav_history_ops_dispatch`: `assertion `left == right` failed: the block bullet's shift_action pins into the right sidebar only` / `left: Main` / `right: RightSidebar`. | Present verbatim in run1 (`taska_lib_run1.log:379-382`), run2, and run3 (`taska_lib_run3.log`) of the 2026-08-06 baseline. | BugFunnel.md:575 | region-literal fix (unowned, escalated) |
| `lib-pin-block-right-sidebar-probe` | known-red | `focus_pin\(right_sidebar, .*\) must populate focus_roots\(right_sidebar\)` | `components.rs:5268` `pbt::frontend_slice::components::tests::headless_pin_block_right_sidebar_probe`: `[pin-probe] headless focus_pin(right_sidebar, block:ref-block-0) must populate focus_roots(right_sidebar) — the matview did not update without a window; got []`. | Present in all 3 runs of the 2026-08-06 baseline (e.g. `taska_lib_run1.log:412-415`). | BugFunnel.md:575 | region-literal fix (unowned, escalated) |
| `lib-unpin-block-probe` | known-red | `no right-sidebar pin row for ref-block-0 in navigation_history` | `components.rs:5397` `pbt::frontend_slice::components::tests::headless_unpin_block_probe`: `[unpin-probe] no right-sidebar pin row for ref-block-0 in navigation_history`. | Present in all 3 runs of the 2026-08-06 baseline (e.g. `taska_lib_run1.log:421-424`). | BugFunnel.md:575 | region-literal fix (unowned, escalated) |
| `lib-focus-roots-mismatch-right-sidebar` | known-red | `region 'right_sidebar' focus_roots mismatch .* the matview faithfully reflects the BASE navigation_history table` | `navigation_pbt.rs:650` `pin_block_lockstep_stays_green` and `navigation_pbt.rs` (`harness.rs:855`) `frontend_navigation_pbt`: `lockstep PinBlock should be green: [("inv-focus-roots", "[inv-focus-roots] region 'right_sidebar' focus_roots mismatch — the matview faithfully reflects the BASE navigation_history table, which itself disagrees with the reference. …")]` with `expected: {"block:ref-block-0"}` / `mirror/matview/base: {}`. | Present in all 3 runs of the 2026-08-06 baseline (e.g. `taska_lib_run1.log:432-435`). Covers 2 test names — both emit this exact `inv-focus-roots` payload. | BugFunnel.md:575 | region-literal fix (unowned, escalated) |
| `lib-sut-only-pin-not-caught` | known-red | `SUT-only PinBlock must trip inv-focus-roots with a Fail; failures were \[\]` | `navigation_pbt.rs:685` `pbt::frontend_slice::navigation_pbt::teeth::sut_only_pin_block_is_caught_by_focus_roots`: `SUT-only PinBlock must trip inv-focus-roots with a Fail; failures were [], ran=[...]` — the invariant that should catch a SUT-only pin never fires (because the base row is never written into the region the invariant checks). | Present in all 3 runs of the 2026-08-06 baseline. | BugFunnel.md:575 | region-literal fix (unowned, escalated) |
| `lib-right-sidebar-renders-pins` | known-red | `both pinned blocks must render in the right sidebar \(apple=None, zebra=None\)` | `structural_pbt.rs:4321` and `structural_pbt.rs:4341` (`right_sidebar_renders_pins`, `right_sidebar_renders_pins_in_declared_added_ts_order`): `both pinned blocks must render in the right sidebar (apple=None, zebra=None); rendered right-sidebar entity order = ["block:default-right-sidebar"]` — the first also names the cause verbatim in-message (`the seed region filter drifted off Region::RightSidebar.as_str() ('right_sidebar')`). | Present in all 3 runs of the 2026-08-06 baseline. Covers 2 test names sharing this message prefix. | BugFunnel.md:575 | region-literal fix (unowned, escalated) |
| `lib-sidebar-pages-declared-order` | no-longer-fires | `both seeded sidebar pages must render \(apple=None, zebra=None\)` | `structural_pbt.rs:4175` `pbt::frontend_slice::structural_pbt::teeth::sidebar_renders_pages_in_declared_content_order`: `both seeded sidebar pages must render (apple=None, zebra=None); rendered sidebar entity order = ["block:default-left-sidebar"]`. Co-occurs with the right-sidebar family in all 3 runs and shares its `apple`/`zebra` pin-fixture naming, but the causal link to the `'right'`/`'right_sidebar'` literal typo has NOT been independently traced here (only correlated) — flag for attribution review before assuming it is the same root cause. | Present in all 3 runs of the 2026-08-06 baseline. | BugFunnel.md:575 (correlated, unverified) | region-literal fix (unowned, escalated) **NO LONGER FIRES as of main 1417a5f2** — measured passing on the landed base (lane collapsed-bug, 2026-08-22); row kept for the history of the 2026-08-06 baseline, not as a current expectation. |

**Separate stable red — unrelated to the above family:**

| Key | Status | Match pattern | Signature | Evidence | Task | Owner |
| --- | --- | --- | --- | --- | --- | --- |
| `lib-seed-wide-drift` | no-longer-fires | `scripts/seed_wide/index.org body drifted from assets/default/index.org` | `live_mcp.rs:1223` `pbt::composed::live_mcp::tests::seed_wide_stays_aligned`: `assertion `left == right` failed: scripts/seed_wide/index.org body drifted from assets/default/index.org (the DEFAULT_INDEX_ORG the iOS app boots)`. Decoded diff: `assets/default/index.org` carries `:WIDGET_ONLY: t` on `default-left-sidebar`, `default-main-panel`, and `default-right-sidebar`; `scripts/seed_wide/index.org` does not — the two files have diverged. | Present in all 3 runs of the 2026-08-06 baseline. | — | UNOWNED — needs a triage owner **NO LONGER FIRES as of main 1417a5f2** — measured passing on the landed base (lane collapsed-bug, 2026-08-22); row kept for the history of the 2026-08-06 baseline, not as a current expectation. |

**Separate stable red — registered 2026-08-24 from the F2 lane's A/B, which
found it while proving its own change innocent:**

| Key | Status | Match pattern | Signature | Evidence | Task | Owner |
| --- | --- | --- | --- | --- | --- | --- |
| `lib-type-chars-home-profile-derived` | known-red | `block\(s\) carry a profile their home does not imply` | `structural_pbt.rs:2577` `pbt::frontend_slice::structural_pbt::teeth::editor_type_chars_lockstep_stays_green`: `lockstep TypeChars must stay green over the composed editor CapMap: [("inv-home-profile-matches-derived", "[inv-home-profile-matches-derived] 9 block(s) carry a profile their home does not imply: [\"block `block:c1`: home resolves to `org` but the draw homes it in `holon-native`\", …]")]`. The nine are EVERY seeded block of the wide tree (`c1`, `c2`, `parent`, `structural-page`, and the five `journals::*`), not a subset — so this is the derived-home rule disagreeing with the draw wholesale, not a per-block drift. The test name is already registered for its OWN signature elsewhere in this doc; this is a SECOND, distinct signature under the same test, and a fix for one is not evidence for the other. | A/B at base `843c5ce0`, sha-verified revert of `crates/holon-frontend/src/headless_editor_mirror.rs`: fires identically with the F2 fix in the tree (`.lane-tmp/gate-nextest-it-lib2.log:404`, mirror sha `aa63a3d3…`) and with `main`'s file restored (`.lane-tmp/probe-baseline-typechars.log:81`, mirror sha `5b09b3ce…`). Pre-existing at `843c5ce0`; the `inv-home-profile-matches-derived` invariant and the `RehomeEntity` model landed in that commit and this red came with them unregistered. | 2026-08-24-split-block-indent-no-previous-sibling (found by, not caused by) | home-profile derivation (unowned, escalated) |

**DETERMINISTIC, mechanism located, ATTRIBUTION OPEN pending a base replay.
Registered 2026-08-24 from the wave gate. NOT permission to ignore it:**

| Key | Status | Match pattern | Signature | Evidence | Task | Owner |
| --- | --- | --- | --- | --- | --- | --- |
| `bulk-add-sibling-order-under-journals` | known-red | `sibling order diverges under parent block:journals` | Four arms co-fire on one `BulkExternalAdd` — `inv-live-children-match-ref`, `inv-blocks-match-ref/org`, `inv-loro-children-match-ref` — all reporting the SUT placing the freshly bulk-added blocks BEFORE the page's pre-existing children: ref `[block:journals::auto-create, block:61133fe7-…, block:bulk-1-0, block:bulk-1-1, block:bulk-1-3]` vs sut `[block:bulk-1-0, block:bulk-1-1, block:bulk-1-3, block:journals::auto-create, block:61133fe7-…]`. It reaches the store (`/loro` and `/org` both), so it is an ordering disagreement on the external-add seam the `BulkExternalAdd` generator's own comment names — org-parser-assigned sort keys against the Loro tree's fractional indices — not a projection artifact. | **DETERMINISTIC — 3 transitions.** The keystone log carries no transition trace, but the `[inv-sql-budget] <Transition>:` telemetry lines name every transition as it runs; counting them up to the panic at `.lane-logs/wavegate/keystone-full.log:151` gives the whole case: `SplitBlock` → `BulkExternalAdd` → `UndoLastMutation`. The second occurrence (`:262`) is the same pair. Replayed as a hand-authored case (`SplitBlock{block:parent, 2}` → `BulkExternalAdd{doc_uri: block:journals, three text blocks}` → `UndoLastMutation`, default wiring) it reproduces with the ref/sut orders and the block ids BYTE-IDENTICAL to the sweep, first try: `.lane-logs/wavegate/f2-probe2.log`. **Do NOT read the sweep's `minimal failing input` as this family's** — it shrank to an `inv-sql-budget` TypeChars violation, the cross-family shrink hazard documented at the head of this file; that is why the sequence had to come from the telemetry instead. An earlier bare-`BulkExternalAdd` probe with no undo PASSES (`f2-probe.log`), so the undo is the load-bearing ingredient. **Mechanism (hypothesis, matches the code):** `bulk_add_blocks` marks the bulk blocks ingest-origin (`ref_caps/layout.rs:287`) and canonically re-sequences the whole tree; `rematerialize_file_ingested`'s CONTENT-block loop then restores them VERBATIM from `pre_restore`, carrying sequences from the post-canonicalisation space into a snapshot whose other children hold the older one, and nothing reconciles the two. The doc-ROOT loop immediately below it already handles exactly this concern for journals (`next_seq`, "Match the SUT's created-last sibling order"); the content-block loop has no equivalent. That places this in the same `rematerialize_file_ingested` / ref-undo area as `org-blocks-ref-diverge` cause A rather than a new one. **PRE-EXISTING at `843c5ce0` — settled by the base replay:** the 3-transition case FAILS at pure base with the identical `inv-live-children-match-ref` signature (`d7c-binding/.lane-logs/basegate/f2-base-replay3.log`, 2026-08-24); the wave's alphabet relaxation newly DREW it, nothing in the wave caused it. Supporting code-path argument: every file on this path is byte-unchanged across `843c5ce0..204fd77f` (`ref_caps/layout.rs` and the whole `holon-filesystem` crate are absent from the wave diff; `mutation_driver.rs` changed only `rehome_entity`'s return type), and the case reproduces identically with the 2026-08-24 undo focus-clear fix DISABLED (`f2-probe2-nofix.log`), so that fix is not the cause either. (The base sweep's zero occurrences was one random-seeded run; the deterministic replay supersedes it.) | — | **UNOWNED** — deterministic case in hand and pre-existence proven; fix = give the content-block loop what the doc-root loop already has (`next_seq` reconciliation); org-blocks-ref-diverge family, cause-D candidate |

**Intermittent — reproduced in 2 of 3 runs (present in a CLEAN run, so not
purely a contention artifact; absent from run 1):**

| Key | Status | Match pattern | Signature | Evidence | Task | Owner |
| --- | --- | --- | --- | --- | --- | --- |
| `lib-displayed-text-nested-content-skipped` | known-red | `must reach Ok over the grafted nested content .* got Some\(Skipped\("\[inv-displayed-text/viewmodel\] no block-bound text widgets` | `integration_tests.rs:179` `pbt::frontend_slice::integration_tests::frontend_slice_displayed_text_viewmodel_bites_on_nested_content`: `headless /viewmodel must reach Ok over the grafted nested content (NOT Skipped — that would mean the recursive snapshot didn't resolve the Main-panel content), got Some(Skipped("[inv-displayed-text/viewmodel] no block-bound text widgets in the VM tree yet"))`. | Absent from run 1 (89s, clean); present in run 2 (192s, contended) AND run 3 (67s, clean) of the 2026-08-06 baseline with byte-identical panic text — reproduced in a non-contended run, so not registered as contention-only. Observed 2 of 3 runs; do not read this as a 67% rate, it is 2 samples. | — | UNOWNED — needs a triage owner |

**Observed once, in the contended run only — NOT registered, needs separate
triage before either fixing or adding to this file** (all 21 names below
appeared ONLY in run 2 of the 2026-08-06 baseline and did not recur in the
clean run 3; treat as likely contention-induced — e.g. a driver poll timeout
under CPU starvation — not evidenced as genuine intermittent reds from a
single contended sample):
`headless_type_chars_commits_to_block_raw`,
`booted_widget_tree_has_no_pending_placeholders`,
`editor_sut_only_type_chars_is_caught`,
`editor_type_chars_lockstep_stays_green`,
`journal_feed_expanded_newest_first_with_divider`,
`org_ingest_link_marks_survive_full_catalog`,
`shadow_mesh_predicts_concurrent_primary_peer_merge`,
`wide_create_document_lockstep_stays_green`,
`wide_frontend_setup_watch_lockstep_stays_green`,
`wide_frontend_sut_only_navigate_is_caught`,
`wide_frontend_sut_only_toggle_state_is_caught`,
`wide_frontend_sut_only_watch_rows_is_caught`,
`wide_frontend_toggle_state_lockstep_stays_green`,
`wide_indent_outdent_roundtrip_lockstep`,
`wide_indent_then_split_parent_lockstep`,
`wide_pin_block_lockstep_stays_green`,
`wide_simulate_restart_lockstep_stays_green`,
`wide_split_then_type_lockstep_stays_green`,
`wide_sut_only_create_document_is_caught`,
`wide_sut_only_pin_block_is_caught`.
None of these match the three names from an earlier session's unverified
"rotating 10th" premise (`editor_type_chars_lockstep_stays_green`,
`wide_create_document_lockstep_stays_green`,
`turso_draw_reaches_the_feed_driven_writeback_path`) as a stable rotating
set — `editor_type_chars_lockstep_stays_green` and
`wide_create_document_lockstep_stays_green` DO appear in this contended-only
list, but `turso_draw_reaches_the_feed_driven_writeback_path` never failed in
any of the 3 runs, and neither of the two that did appear recurred in the
clean run 3. That premise is not re-confirmed by this baseline.

## Windowed suite (`harness = false` binaries) known reds

The `harness = false` windowed/TUI binaries enrolled in the `nextest --list`
collection protocol (`crates/holon-integration-tests/src/libtest_list.rs`) are
suite members of a plain `cargo nextest run`, so a flake inside one of them
aborts a windowed suite run. Rows here carry the same pass-with-note
discipline as the sections above.
`scripts/keystone-known-reds.sh` classifies composed-nightly logs only, so it
does not consume these rows automatically — they are a manually-maintained
baseline for judging windowed-suite runs.

| Key | Status | Match pattern | Signature | Evidence | Task | Owner |
| --- | --- | --- | --- | --- | --- | --- |
| `tui-focus-propagate` | known-red | `\[ClickBlock\] focus did not propagate` | `frontends/tui/tests/tui_ui_pbt.rs` (via `common::pbt_main::run`) panics mid-scenario with `[ClickBlock] focus did not propagate` — a click seats no focus in the TUI frontend, so the following step asserts on unfocused state. | **PRE-REGISTERED, NOT YET OBSERVED IN A GATED RUN** (Martin's ruling, 2026-08-08). The signature is quoted from the enrollment commit `b3be6e9c`'s disclosed residual: `tui_ui_pbt` became a suite member carrying this focus-propagation flake, probed 2/2 green at the time. Registered ahead of its first firing so the first occurrence is a WARN pass-with-note instead of an aborted suite run. Because no payload has been decoded yet, the Signature column above is a PREDICTION — decode and correct it on the first real firing. | #5 | TUI windowed PBT (task #5) |
| `movecursor-unopened-editor` | known-red | `byte_position .* not a char boundary of ""` | `[apply_move_cursor] byte_position 1 not a char boundary of ""` — the keystone asserts a REF-generated cursor position against an editor the SUT never opened. `apply_move_cursor` (`crates/holon-integration-tests/src/pbt/frontend_slice/components.rs:2273`) falls back `editor_live_text` → `editable_text` (an `Err` in a Turso-only drawn wiring: no Loro cell) → `unwrap_or_default()` = `""`, then asserts against the empty string; the file's own comment warns about the first miss but not the `Err` after it. ENVIRONMENT primary (harness diverges from prod: a MoveCursor gesture presupposes a mounted editor, the generator admits it for any FOCUSED block), ORACLE secondary (the ref's active-editor model has no SUT counterpart at that point — incomparable, not disagreeing). Unconditional for the draw shape (the assertion runs BEFORE the only call that can create an editor VM), so NOT a race. | RATIFIED by Martin 2026-08-11 (D1a). Discovered by the task #78 arm-(d) lane in a 1-of-5 smoke red and classified DISCOVERED-NOT-CAUSED two ways: a DETERMINISTIC hand-authored replay (`[Indent(block:c1), MoveCursor(1)]`, Turso-only — the keystone sets `failure_persistence: None`, so the repro had to be authored; reds every run, `lane-logs/task78/r8-replay-*.txt`) and an ordering A/B eliminating the lane's own suspect (`r8-ab-ordering-*.txt`, sha256-restored). Full triage: BugFunnel 2026-08-11 rows (increment log + ledger) — the repro JSON line is recorded there for one-paste re-arming and is deliberately NOT in `keystone.jsonl` (it would make `just hand-authored` permanently red). Frequency ~1/5 smoke runs, draw-dependent (needs a Turso-only wiring + MoveCursor without a prior editor-opening transition). | #101 | keystone harness/generator — MoveCursor precondition (task #101) |
| `task-state-storage-coherence` | known-red | `diverged from the oracle: .*"inv-task-state-storage-coherence"` | `[inv-task-state-storage-coherence] N block(s) have task_state diverging from the reference across block_raw SQL / Loro projection`, e.g. `block:54bf066f-…: ref=Some("TODO") sql=Some("") loro=Some("")` — the reference carries a task keyword the BOTH stores dropped. Reachable under the ARMED interleaving axis (`HOLON_PBT_SCHED_KINDS=TypeChars`) with typing-dominant weights; it lands ~2 masked transitions into the first case, so it is early-and-reliable rather than deep. | Registered 2026-08-14. Observed as the FIRST panic in 3/3 armed runs with `HOLON_PBT_WEIGHTS=TypeChars:400 HOLON_PBT_FORCE_FULL=1` (`lane-logs/pump-redesign/inc2-burst-RED.log`, `inc2-burst-repeat.log`, and `AB-inc0-armed-burst.log`). **Dated by A/B:** it reproduces on the pump-redesign rev that contains NO schedule-point code (`AB-inc0-armed-burst.log`, tree materialised at that rev), so it PREDATES the completion-driven pump. Not observed in unarmed runs, and the in-lane gate (`just pbt general 1`) is 5/5 green, so it is armed-configuration-only on current evidence. Per the shrink-phase caveat at the head of this file, all three attributions are taken from the FIRST panic, not from `Test failed:`. | — | **UNOWNED** |

## Evidence corpus & the pattern-drift guard

Every row's Evidence column must name a **decoded payload**, not a recollection.
Two rows were found on 2026-08-04 to describe something no captured payload ever
showed (`org-render-echo-loop` blamed empty-title headings; `org-blocks-ref-diverge`
blamed the journals projection), and both would have mis-steered a fix. So the
raw logs those verdicts came from are committed, zstd-compressed, at
`crates/holon-integration-tests/hand-authored-regressions/fixture-logs-2026-07-31/`
— four red runs and the four green runs of the same nights (the 4/8 = 50%
base-rate record). They are evidence, not scratch: `/tmp` does not survive a
reboot.

`just known-reds-fixture` replays the four red logs through
`scripts/keystone-known-reds.sh` and asserts the per-key hit counts are exactly
what the corpus contains. A diff there means a `Match pattern` above no longer
matches the text it was written for. Re-bless with
`scripts/keystone-known-reds-fixture.sh --bless` **only** after confirming the
pattern change is intended.

The same check then pins what the classifier says about a log BEFORE any pattern
matching — whether it reads the run as failed, passed, or as saying nothing
either way. That half is not blessable, and the four GREEN corpus logs are its
fixtures, because until 2026-08-25 the classifier had no outcome detection at
all: it assumed every log handed to it came from a non-zero exit, so a green
log's absence of panics read as a missing signature and every passing run
classified as `NOVEL: run failed but no panic signature was extracted`. A green
run now exits 0 with nothing to classify; a run that genuinely failed with no
extractable panic is still NOVEL at exit 1; and a log that admits no verdict is
loud at exit 3 — `INDETERMINATE` (empty or truncated) and `UNREADABLE` (no such
file) are worded differently on purpose, so a fixture case can pin each without
the other standing in for it.

Shapes no corpus run happens to have live in `synthetic/` beside the corpus, and
are committed `.log.zst` for the same reason the corpus is: `.gitignore`'s
`*.log` would otherwise drop them from every clean checkout and silently take
the cases that depend on them along.

**It cannot detect assertion-message drift, and must not be trusted to.** The
corpus is ARCHIVED logs, so it pins each pattern against the wording that
existed when the log was captured — never against what the code emits today.
Reword an assertion and the fixture stays green while the live pattern is dead,
exactly the `sidebar-focus-bind` failure (risk R10) that this section was
written to prevent. Demonstrated again 2026-08-06: arming the read budget
changed `sql_reads` to lead with the dedup count, which killed
`pinblock-unrendered-target`'s pattern against live output while the fixture
passed. The only real guard is to re-read the pattern against the CURRENT
format string whenever you touch a quoted message.

## `holon` integration-test binaries known reds

The `holon` crate's own `--test` binaries (`sync_suite`, `api_pbt`) run under
`cargo nextest run --no-fail-fast -p holon -p holon-app`. `-p holon` ALONE does
not compile its tests: its `test-helpers` feature is unified only when
`holon-app` is in the same invocation, so always run both packages together.

**This command is a PER-LAND gate** (D64.a, 2026-09-02), run in parallel with
the existing landing battery — `just landing-gate` alongside
`cargo nextest run -p holon-app` (D43.a). Its failures are CLASSIFIED, never
judged by exit code: the five registered `e2e_backend_engine_test` matview reds
are pass-with-note, and today's sanctioned load-sensitive flakes are
`concurrent_keystrokes_keep_every_undo_step` (OPEN ORACLE entry) and
`test_multi_peer_sync_iroh` (measured 2026-09-02: 5/5 PASS isolated at
111–179s; its single red coincided with a load average above 200 and was a QUIC
transport timeout). Anything else blocks the land. The full signature list is
`docs/Testing/HolonCrateReds-2026-09-01.md`.
chain.

The vault-scale latency guard runs in the nextest test-group
`vault-scale-latency` with `max-threads = 1`, because it is load-sensitive:
853ms isolated against a 5s budget, 7.5s contended.

**A/B baseline for a lane**: run the identical command on a main-base tree and
on the lane tip, then compare the two failure-name sets with `comm -23`. Only a
name that appears on the lane tip alone is the lane's own regression.

**Adding a row here ARMS the nightly classifier.**
`scripts/keystone-known-reds.sh:50-55` parses this file with a section-blind
`awk -F'|' '/^\| *`/'` and takes EVERY row whose Status is `known-red`,
whatever section it sits in. A row therefore auto-demotes its signature to a
WARN pass-with-note in composed-nightly logs — it is not a passive
manually-maintained baseline. Register a signature here only when
pass-with-note is what you want for it; a suspected real defect belongs in the
bug funnel as an OPEN entry instead, where it stays visible.

**Baseline established 2026-09-01** at base `ed38a4dae833`, 10 isolated runs
per test (each run its own `cargo nextest run … -E 'test(<name>)'` invocation,
serialized through the `holon-build` semaphore; logs `lane-logs/flakes/`).

| Key | Status | Match pattern | Signature | Evidence | Task | Owner |
| --- | --- | --- | --- | --- | --- | --- |
| `loro-backend-change-count` | known-red | `Reference and SUT should receive same number of change` | `loro_backend_pbt.rs:534` `test_loro_backend_state_machine`: `assertion \`left == right\` failed: Reference and SUT should receive same number of change notifications.` / `Reference: N changes` / `SUT: M changes` — the watcher notification counts diverge between the reference model and `LoroBackend`. | FLAKES ONLY UNDER SUITE LOAD. 2026-09-01, 10 isolated runs: **10 passed, 0 failed** — `lane-logs/flakes/loro-1.log` .. `loro-10.log`, each `1 test run: 1 passed (1 slow), 5 skipped`, wall time 43s–66s. The signature above is therefore quoted from the assertion's own format string, NOT decoded from a captured payload: correct the Signature column on the first firing that is actually logged. Registered so that first firing inside a loaded suite run is a WARN pass-with-note rather than an aborted run. | — | **UNOWNED** |

`subtree_share_round_trip_pbt`'s `P-NO-TMP-LEFTOVER` failure is deliberately
NOT registered here, and no longer needs to be: it was root-caused on 2026-09-02
to the harness sweeping `shares/` while a publish armed by rehydration's
detached kick-sync was still running, and the test now waits for a real settle
point instead of a fixed sleep. Bug-funnel entry
`2026-09-01-subtree-share-tmp-leftover-race`, status FIXED.

## Where it runs — local, not GitHub Actions

The tier is a LOCAL nightly (Martin's machine or an orchestrator session). No
scheduled workflow was added, because CI cannot currently execute the composed
keystone at all:

- `.github/workflows/ci.yml`'s `rust-checks` job runs
  `cargo test --workspace --exclude rust_lib_holon` on `ubuntu-latest`, and
  `pbt` IS a default feature of `holon-integration-tests`, so in principle the
  keystone is in that job's scope.
- In practice it never gets there. The last 200 CI runs are 200 failures; the
  step spends ~14min compiling and then dies inside the `holon` crate's own
  suite (`create_page_from_link`), before any `holon-integration-tests` binary
  starts. `general_e2e_composed_pbt` appears in ZERO CI logs.
- Full depth is hours of wall clock on top of that compile, on a 2-core runner.

A scheduled job today would be a gate that never ran the keystone. Re-evaluate
once CI is green and the runner budget is measured; until then
`just keystone-nightly` IS the tier.

## Running the tier

```
just keystone-nightly            # 2 serialized full-depth runs, judged against this file
just keystone-nightly 1 8        # 1 run at 8 cases — for exercising the plumbing, not a gate
```

Keystone runs must be serialized against every other keystone lane:

```
/opt/homebrew/opt/parallel/bin/parallel --semaphore --id holon-keystone -j1 --fg -- just keystone-nightly
```
