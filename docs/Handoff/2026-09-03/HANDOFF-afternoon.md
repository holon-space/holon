# Handoff — 2026-09-03 afternoon (orchestrator session 1d3fdfe9, cut at ~16:05 by loss of network)

Resume from THIS file plus `followup-queue.md` (same directory; the running log, newest at the bottom). Do not reread session transcripts.
Verify every load-bearing claim with the check command given before acting on it.

## 0. Where the code is

| Thing | Where | Check |
|---|---|---|
| main | `4e2ee368` (= `main@origin`, wave 8) | `jj log -r main@origin -T commit_id` |
| integration chain (wave 9 candidates) | `main..integration` = search-fix `2521d550` → lowcode-inc2a `5f9ef621` → lowcode-inc4 `rtyttoyzkuux` (see §2) | `jj log -r 'main@origin..integration' -T 'if(conflict,"CONFLICT ","ok ") ++ change_id.short() ++ " " ++ description.first_line() ++ "\n"'` |
| lane workspaces | `.claude/worktrees/<lane>/` (jj workspaces; uncommitted work on each `@`) | `jj workspace list` |
| lane snapshots on origin | `wip/<lane>` bookmarks (pushed at cut time; see §5) | `git ls-remote origin 'wip/*'` |
| session scratchpad copies of every report/verdict | `docs/Handoff/2026-09-03/reports/<lane>/` (this bundle) | `ls reports` |
| weave gate script (per-lane cases) | `docs/Handoff/2026-09-03/weave-gate.sh` (copy of the scratchpad script) | — |
| Fable vision review | `vision-review.md` (+ artifact https://claude.ai/code/artifact/7558fb71-ed4d-4698-94f0-35f11a88988f) | — |

## 1. Rulings today (all applied or dispatched; memory `rulings-2026-09-03-d85-d92`)

D85.c target-gc + gate normalisation · D86.a route iroh imports through `admit` (QUEUED lane, security-executor) · D87.a visual-accent lane (QUEUED) · D88.a expiry closes enrollment only (confirmed) · D89.b UTCP PR-1 OPENED: utcp-specification #64, python-utcp #99 (commit author display name is still the placeholder; fix = amend + force-push, not done; PR-2 not opened) · D90.a Inc 3 delete cook.rs + incremental content-hash scan (QUEUED after inc4 lands) · D91.a ADVANCED_UNITS off in the cooklang guest (fold into Inc 3) · D92.a accept the jaq 10k cost (done inside inc4 rev 2: ADR 0034 restated, entry NOTED).

OPEN for Martin: **D93** (pairing conflict-copy UI; rec a = badge via a profile variant) · **D94** (unsatisfiable re-import at boot: stop vs boot-degraded banner; rec a) · **V1–V12** in the vision review (answer by id). Decision-inbox artifact: https://claude.ai/code/artifact/de1c7a1a-ba44-4e87-a75f-a4a824a6d2ab (cards D93/D94 only).

## 2. Lane states at cut time (newest verdicts in `reports/`)

| Lane | WS | State | Next step |
|---|---|---|---|
| search-fix | `.claude/worktrees/search-fix` (edits chain commit `2521d550` IN PLACE; `@` = empty child) | rev 2 = GLOB `[lu]` classes fixed the P0 overflow (CONFIRMED). rev 2 REFUTED partial on folding/length/budget. **rev 3 CHECKPOINT on disk**: defect 1 FIXED+tested (`FOLD_CLASSES` LazyLock over all Unicode scalars, BMP sweep test green, `lane-logs/r4-foldclass2.log`); defect 2 FIXED+tested (`MAX_GLOB_PATTERN_BYTES`, typed `SearchQueryTooLong`, `SearchMatch::new` → Result); defect 3 on disk, not gated (vault-scale test now asserts the 6 s JOIN-regression bound only). REMAINING: run hand-authored case `search-many-to-one-case-folds-reach-every-spelling` (80/80) + `just hand-authored` + `keystone-smoke` + vault-scale under the new bound (r4-*.log), flip entry `search-does-not-fold-case-for-non-ascii-letters` honestly, fix two stale "1500 ms" mentions (report §1a + entry `…costs-a-second-at-vault-scale.md`), bugfunnel check. Corrected commit message for `2521d550` = bottom of `lane-report-search-fix.md` "Rev 3" (drop its 'fold entry FIXED' clause unless the remaining work is done). | Finish rev 3 (full-fold equivalence classes from a generated reverse map + BMP-wide oracle≡GLOB probe; typed length refusal; honest budget test), re-verify (same checks as `search-fix-verify.md`), amend the commit message of `2521d550` (it still names LikeOperand/ESCAPE), then the live cmd-K dogfood re-run (holon-feature gate) before landing. Check: `rg -n "GLOB" crates/holon/src/api/query_engine.rs` |
| lowcode-inc2a | on chain `5f9ef621` | gated green, woven | lands with wave 9 |
| lowcode-inc4 | `.claude/worktrees/lowcode-inc4`; chain commit `rtyttoyzkuux` (woven, **1 conflict** `crates/holon-rows/src/lib.rs`, inc2a JSON-Lines vs inc4 RowMapper) | rev 2 CONFIRMED (445/445) + cleanups done. **Conflict RESOLVED and squashed at cut** (chain tip `b4c154e2`, no CONFLICT in `main@origin..integration`); the post-resolution FULL weave gate at the tip has NOT been run yet. | If `jj log -r 'main@origin..integration'` still shows CONFLICT: resolve as the semantic UNION in the lane WS (`jj new rtyttoyzkuux` if `@` is not already its child; edit markers; gate `cargo check -p holon-rows -p holon-plugin-host -p holon-mcp-client -p holon-kitchen -p holon-app` + `nextest -p holon-rows -p holon-plugin-host`; `jj squash`). Then rerun the full weave gate at the tip: `bash weave-gate.sh lowcode-inc4` from `_sw_integ` (after `jj workspace update-stale` there). |
| pair-reimport | `.claude/worktrees/pair-reimport` (uncommitted, `wip/pair-reimport` on origin) | rev 2 crash-safe pairing **CONFIRMED** by security verifier (`reports/pair-reimport/pair-reimport-verify-r2.md`). 2 LOW residuals (pairing.json single-file guard; no re-entrancy guard). | WEAVE next (weave-gate case `pair-reimport` exists: sentinel `crates/holon-loro/src/pairing_swap.rs`): `sw weave pair-reimport -m "<msg from lane report Rev 2>" --check "bash weave-gate.sh pair-reimport"`. Rebase-verify because inc4/inc2a touch holon-kitchen only — expect clean. |
| readonly-edits | `.claude/worktrees/readonly-edits` (uncommitted, `wip/readonly-edits` stale on origin — re-push) | rev 2 (cell takes the dispatcher's WriteTierAuthority; `live_field_any` vs `editable_field_any` seam; `ReadOnlyTextCellBacking`; 1358/1358). **CONFIRMED** (`readonly-edits-verify-r2.md`): seam audit over 1201 hits found no production-reachable bypass; teeth held. Two LATENT gaps (not reachable today): direct `live_field` for `content` still mints an ungated write; `editable_field_any` special-cases only `content` (`completed`/`block_type` unguarded via that entry). Verifier saw `keystone-smoke` fail on `inv-sql-budget` in a CRDT-disabled run — looks environmental, not bisected. | verify → weave (gate case `readonly-edits`: crates holon-loro holon-frontend holon-core holon-filesystem holon-app; add to weave-gate.sh if missing). Still open after: read-only RENDERING (caret enters a recipe row; `InputState::disabled` is pub(crate) in the gpui-component fork). |
| sql-loss | `.claude/worktrees/sql-loss` (uncommitted, `wip/sql-loss` stale — re-push) | rev 1 REFUTED (assertion flagged a cascade-swept row as loss). rev 2: lane REFUSED the prescribed subtraction WITH MEASUREMENT (pin went red — the Err→reseed is load-bearing because the owner's projection emits `update:block:c2` with NO parent_id while Loro holds c2 elsewhere) and shipped a split diagnosis (`PreparedOp.removed_ids`; two Err texts). 720 run / 714 pass (5 matview + 1 seed flake). rev 2 **CONFIRMED** (`sql-loss-verify.md` "Rev 2"): pin-red under the subtraction reproduced 3/5; the verifier's counterexample shape is provably indistinguishable from real loss at the assertion → Err is correct, only its old text was false; split diagnosis is diagnostic only, recall unchanged. | WEAVE (gate case `sql-loss`: add to weave-gate.sh: sentinel `batch_delete_cascade_updates.rs`, crates holon holon-app holon-loro, EXTRA loro). UPSTREAM lane to queue: `crates/holon-loro/src/loro_sync_controller.rs:2193-2196` `block_diff_params` emits `parent_id` only when old≠new in ITS diff snapshot — carry parent_id on that update so the reseed becomes a no-op. |
| target-gc (D85.c) | `.claude/worktrees/target-gc` (on main; uncommitted) | **DONE, unverified** (`reports/target-gc/lane-report-target-gc.md`): `scripts/target-gc.py` (keep newest 2 per crate/target, busy-guard exit 3, `--self-test`), `just target-gc` = landing-gate step 11 (non-fatal); Part B: 9 shapes → one `CANON`/`CANON_FEATURES` justfile variable on every host-profile gate except `test`; churn proof: one off-shape run added 711 dirs; freed ~40.9 GB in its own target. Lane disclosed one rule breach: it pkill-ed its OWN test compile. | Fresh verifier (self-test teeth, busy-guard, that `just` recipes still run the SAME tests — diff `--list` output before/after — and that CANON respects D64.a), then weave (gate case needed: sentinel `scripts/target-gc.py`, crates holon-app, plus `just --list`). Add to sw.sh new: `(cd "$REPO/.claude/worktrees/$INTEGWT" && /usr/bin/python3 scripts/target-gc.py target --apply >/dev/null 2>&1) || true`. |
| docs-truth (vision L2+L4) | `.claude/worktrees/docs-truth` (on main; uncommitted) | IN PROGRESS (two agents lost to connectivity; no report — `lane-report-docs-truth.md` is an empty skeleton). ON DISK (9 files, +182/−44, pushed as `wip/docs-truth`): `archlint/smells/identity_minting.toml` (NEW, 84 lines; lane-logs/archlint_teeth_full_scan.log + archlint_final_clean.log suggest teeth proven and the tree clean — RE-RUN to confirm), Model.md (invariant 13?), Sync.md, Schema.md, Operations.md, Replication.md, Integrations.md, FeatureMap.md, featuremap.yaml. | Spawn a mech-executor into the WS: `jj diff` first, then finish the L2 list + CrateMap, re-run the lint (pass on tree, red on a scratch mint), write the report, then verify + weave (gate: archlint + `cargo check` only). |
| dogfood-search | `.claude/worktrees/dogfood-search` (`wip/dogfood-search`, **conflict** state, NOT pushed) | 12 docs files (bugfunnel entries from the search dogfood). Its base was rewritten in place (search-fix rev 2/3) → divergent/conflicted `@`. | `jj -R .claude/worktrees/dogfood-search log` → pick the commit whose tree holds the 12 entries, `jj rebase -r <it> -d 2521d550` (by COMMIT id), resolve (entries only), weave with gate case `dogfood-search`. |
| vision-review (Fable) | scratchpad → `vision-review.md` here | DONE; questions V1–V12 open. | Rule V1–V12; then dispatch its §7 lanes L1, L3, L5, L6, L7, L8, L9 (L2/L4 = docs-truth; L10 = readonly verifier). |
| Vault rewrite | `/Users/martin/Workspaces/pkm/holon-pkm/Projects/Holon/*.org` (20 files, on disk, NOT committed — Martin's repo) | DONE by 4 Fable agents: holon-handoff style; every open task has TODO + `[#X]` + `:gate:` + `:effort:` + `:REQUIRES:` + `:agent:`/`:human-only:` (the Now.org query contract, text in README.org §Conventions); feature trees not removals; Flutter removed. Gate values G1–G6 were ASSIGNED BY THE AGENTS; explicit lowercase `:priority:`/`:effort:` keys sit beside cookies. | **Unverified: live ingest** — boot the app on the vault and check the Now.org query returns rows from the new files; fix property names if the parser maps cookies itself. |

QUEUED lanes (not started): admit-routing (D86.a, security-executor; `holon-sharing/src/acceptor.rs:145 admit`, `iroh_advertiser.rs:171-177 acceptor_enroll`) · visual-accent (D87.a; GPUI windowed PBT pins, two widths) · lowcode Inc 3 (D90.a + D91.a; after inc4 lands) · pinblock-render (scout: journal day pages fall to the collapsed `embedded_page` variant because the render path lacks the `expand_default` column — `assets/default/types/block_profile.yaml:89-119`, `journal_feed_matview.sql:38`, `holon-frontend/src/lib.rs:118-123`; WS `.claude/worktrees/pinblock-render` empty) · driver-titlebar (WS empty) · rules-unparsed (WS empty) · dogfood re-run of live cmd-K search (holon-feature gate for wave 9) · D88.a device list follow-up · upstream sql-loss parent_id fix · dogfood-explorer rotation (Martin's standing order: settings → rules → newest feature → visual).

## 3. Landing wave 9 (when the chain is green)

1. Chain must show no CONFLICT; every chain commit verified (search-fix needs rev 3 verify + dogfood re-run; inc4 needs the post-resolution full gate).
2. Land gate = `land-gate.sh` (bundle copy) run from `_sw_integ` on a quiet machine (`land-when-quiet.sh`: load<10 && rustc<3); classify reds against the 5 registered matview reds.
3. `sw land --push --check "<gate>"`; confirm `main@origin` moved; `jj new main` in every landed lane WS and the session repo; purge landed lanes' `target/`; TaskStop landed agents.

## 4. Process facts learned today (also in `~/.claude/skills/orchestrator/IMPROVEMENTS.md`)

- Network outage kills EVERY agent + build at once ("Agent stalled: no progress for 600s" / ENOTFOUND). On-disk edits survive. Recovery: one state sweep, then `SendMessage` to each dead agentId ("process is DEAD, re-issue gates with NEW log names") — resumes from transcript, no respawn. Over a weak hotspot resume 2–3 at a time (7 cold rebuilds = load 370 swap-thrash).
- A lane may refuse a prescribed fix with a measurement (sql-loss did; it was right). Ask for the stronger experiment, never override.
- Doc-comment rot in the lane's OWN rewrite is the recurring verifier catch (twice today in inc4).
- `parallel … sh -c` shapes and `~/.parallel/semaphores` paths trip the env-dump guard in Bash — sweep the semaphore with a `$SEM` variable and no `sh -c`.

## 5. What was pushed at cut time

`wip/handoff-2026-09-03` (this bundle), `integration`, `sw/search-fix`, `sw/lowcode-inc2a`, `wip/lowcode-inc4`, `wip/pair-reimport`, `wip/readonly-edits`, `wip/sql-loss`, `wip/target-gc`, `wip/docs-truth` — see the last lines of `followup-queue.md` for the exact commit ids and anything that failed to push. NOT pushed: dogfood-search (conflict), the vault (Martin's repo).
