# nav-latency — adversarial verify. pwd /Users/martin/Workspaces/pkm/holon/.claude/worktrees/nav-latency, @- = 830d794f878f

C1 CONFIRMED. Base registers both subscribe_root inside start_gated (base loro_sync_controller.rs:446,480,495); its doc-comment line 442 claims "still registered up front" — false. Base fast-path gate line 883 `seeded && armed`. Base log measure-d120-63326.log shows mode="full" reason="coldboot" with blocks growing 193->1067->1435->1849->2792 = the accumulating walk. New ownership: LoroProjection.subscriptions (loro_sync_controller.rs:682), installed at loro_module.rs:228 immediately after from_storage at :215 and BEFORE Shared::new(projection) at :231 — the only production construction site (all others are tests). Idempotence guard (`if !installed.is_empty()`) is safe: doc slots assigned once at loro_document_store.rs:207 under a double-check, never replaced.

C2 CONFIRMED semantics / GAP on coverage. Branch loro_sync_controller.rs:1047 `!armed && deletes>0` -> FullReason::UnarmedDelete (:1056) -> full walk. `deletes` is counted after `changed.extend(layout_changed)` (:955), so layout deletes cannot bypass it. NO TEST: grep for UnarmedDelete/unarmed_delete returns zero hits outside loro_sync_controller.rs. The nearest test, loro_projection_withheld_delete.rs:46, calls `projection.arm()` at :61 — it pins the ARMED case. Mutating :1047 to `false` is invisible to every gate the lane ran.

C3 CONFIRMED. Before-numbers script-emitted (lane-logs/parsed-d120.txt: full 121, incremental 20, snapshot p50 258.0, full-pass p50 510.0, boot_ingest_total 110233). I re-ran the repo's own scripts/measure_latency.py on the post-fix logs: measure-fix2-d120-2302.log yields exactly snapshot p50 8.0, full pass 201.0, boot 58768ms, full=1 — the report's after-column. I ran the test myself via with-build-slot.sh: default scale GREEN "12 total, 1 full", boot 4.26s; HOLON_VAULT_SCALE_DOCS=120 GREEN "120 total, 1 full", boot 79.16s. TEETH (evidence): MAX_FULL_PASSES=2; base at 120 files = 121 full (parsed-d120.txt) and the lane's RED-teeth-d40 log = 41 full for 40 files -> the assertion fails on revert at both scales.

C4 CONFIRMED. just keystone-smoke GREEN 4 passed/0 failed. just hand-authored GREEN 9 passed/0 failed (992s). cargo nextest --no-fail-fast -p holon -p holon-app: EXACTLY "725 tests run: 719 passed, 6 failed, 10 skipped" — the 5 e2e_backend_engine_test (registered known reds, docs/Testing/KeystoneKnownReds.md:314) + shopping_pull_mock a_local_deletion (hardcoded 2026-09-01 dates at shopping_pull_mock.rs:104,532 = the calendar time-bomb). No other failure.

C5 CONFIRMED. No /Users/martin or holon-pkm string in the diff. Fixture is synthetic ("Scale Doc N"/"Node d-i", vault_scale_interaction_latency.rs:118-145); real vault only via HOLON_LATENCY_VAULT_DIR, which no gate sets. lane-logs/ (measure-realvault3 does contain vault text) sits under .claude/worktrees/, gitignored — cannot land.

C6 CONFIRMED. Both entries status: OPEN with the measured improvement recorded; new entry 2026-09-08-the-boot-scan-... status: FIXED, names vault_scale_interaction_latency.rs as the covering test. bugfunnel.py check: "656 entries, 0 problems".

OVERALL: CONFIRMED, with gaps.

GAPS / DEFECTS (evidence only, no fixes)
1. Untested branch: FullReason::UnarmedDelete (loro_sync_controller.rs:1047) has no covering test.
2. Oracle blind spot: projection_stats::record() sits under `if !ops.is_empty()` (loro_sync_controller.rs ~1346), so a full walk emitting zero ops increments neither full_passes nor the holon_latency log, yet still pays the O(N) snapshot. MAX_FULL_PASSES cannot see that regime.
3. Range understated in the durable artifact: the bugfunnel entry says boot 43.3-58.8s. My own 120-doc run measured 79.2s and the lane's own final-growth-d120-97626.log shows 151.4s. The report discloses the 42-151s spread in section 6; the entry does not.
4. Gate not run: loro-suite (land-gate member covering LoroProjection incl. Restart/OfflineMerge and withheld deletes) was not run, although this diff moves subscription ownership off LoroSyncControllerHandle. Related stale comment now false: crates/holon-integration-tests/src/pbt/loro_sync/stub_sut.rs:126-128 still says "Dropping the handle dispatches its owned Subscription, which cancels the subscribe_root callback" — subscriptions now live for the projection's lifetime.
5. Nit: vault_scale_interaction_latency.rs:338 names a real vault file ("Advice Dogfood.org") in a committed comment.

## Rev 2 — delta re-verify. pwd /Users/martin/Workspaces/pkm/holon/.claude/worktrees/nav-latency, @ = 8403e5161f60, @- = 830d794f878f (unchanged)

VERDICT: CONFIRMED. All four gaps closed; nothing I ran refutes rev 2.

Withhold semantics (the report's correction to my C2 framing) — CONFIRMED against the code. `withheld_deletes_are_owed(armed) -> armed` (loro_sync_controller.rs:287) with the reasoning in its doc-comment at :273-286: an UNARMED projection withholds by design because the sink legitimately holds raw-inserted seed-layout rows Loro does not have yet, and the all-clear is `arm()`, not another pass. So an unarmed full walk withholds the delete, reports Converged, and the row STAYS. The report's correction is right and my rev-1 "delete semantics preserved" is consistent with it: pre- and post-fix an unarmed delete takes the same full walk and is withheld identically.

G1 CONFIRMED. I ran the three tests myself via a script file under the build-slot wrapper (driver /private/tmp/.../verify-navlat/rev2-gates.sh, logs v2-unarmed.log / v2-lorosuite.log): all 3 PASS. They are not vacuous — assertions are a `MemorySink::read_calls` delta to observe the route (only the full walk reads the sink), `row_ids()` unchanged after the delete to pin the withhold, `full_passes`, and the fast-path case driven through `flush()`, the org scan's own entry point (loro_projection_unarmed_delete.rs:72-99,113-131,141-161). The four cp-aside probe logs are red with four DISTINCT, mutation-specific messages (probeA "the pass read the sink, so it took the full walk" 1 vs 2; probeB "an unarmed CREATE must not pay a full-document walk" 2 vs 1; probeC "a layout-only delete is still an unarmed delete" 1 vs 2; probeD "the zero-op full walk counts" 1 vs 2). Restore verified independently: `shasum -a 256 crates/holon-loro/src/loro_sync_controller.rs` = cb50c514784f329456dc5a6dd85caa73594bfcdc870eca1e216a5c0208356c63, matching the report exactly. (Limit stated honestly: the probe REDS are lane-produced; I confirmed the greens and the assertion substance myself, not the mutations.)

G2 CONFIRMED. The guard inverted: `if !ops.is_empty()` became `if ops.is_empty() { projection_stats::record(0, .., full) } else { record(..) }`. The op-gated `holon_latency` tracing event is deliberately unchanged, and the report discloses why (it would move what measure_latency.py takes p50s over). Scale test expectation unchanged, as claimed.

G3 CONFIRMED by reproduction. I ran `/usr/bin/python3 scripts/boot_distribution.py lane-logs` myself; at 120 files / 2793 blocks it prints exactly the report's figures: fixed n=6 min=41.9 median=59.5 max=151.4 (samples 41.9, 43.3, 58.4, 60.6, 90.8, 151.4); defect n=2 min=74.5 median=93.5 max=112.5. It also independently reproduces the misnamed-log finding — measure-fix-d120-33174.log classifies full=121, i.e. defect regime. The overlap conclusion holds: fixed max 151.4s > defect max 112.5s, so wall-clock boot cannot separate the regimes and only the ratio can gate. My rev-1 gap 3 is closed.

G4 CONFIRMED. `just loro-suite` run by me: "Summary 16 tests run: 16 passed, 1 skipped" — the 3 new unarmed tests are in it (positions 1, 2, 4). The moved subscription ownership breaks nothing there. stub_sut.rs:126-130 comment corrected. bugfunnel.py check: 656 entries, 0 problems.

Shutdown-contract gap: NOTED as flagged and routed to the `session-shutdown` lane, not counted against this lane. Dropping `LoroSyncControllerHandle` no longer stops the projector, because `_task` is a bare JoinHandle and the run loop previously went quiet only as a side effect of its subscription dying with the handle.

REMAINING NITS (neither blocks landing)
1. The corrected stub_sut.rs:126-130 comment is still not quite right in the same direction as the flagged defect: it says facts "keep queuing; nothing drains them until a controller is started again" and the task "becomes idle", whereas the report's own G4 flag says the OLD projection keeps receiving wakes and keeps projecting into the same sink. Whoever takes the session-shutdown lane should correct the comment along with the contract.
2. loro_projection_unarmed_delete.rs asserts ABSOLUTE `projection_stats::snapshot().full_passes` values (:98, :161) against a process-global static. That is safe only because nextest gives each test its own process; under plain `cargo test` these three would share a process and contend. Fine for the gate as configured; worth knowing before anyone runs the suite with `cargo test`.
