# op_button param-popup lane — adversarial verification

**VERDICT: CONFIRMED** (one comment-accuracy finding, non-blocking)

pwd for every command: `/Users/martin/Workspaces/pkm/holon/.claude/worktrees/opbtn-popup`
Tree-identity assert passed (`param_collection` present in lib.rs; no WRONG-TREE). Base main 0b93ac7d, rev 6ed1299c.

## Per-check evidence

1. **Windowed PBTs green.** Both run with `--test-threads=1` (mandatory: HeadlessAppContext not parallel-safe).
   - new: `settings_integrations_setfield_popup_windowed` → `test result: ok. 1 passed` (`/tmp/opbtn-newpbt-17309.log`)
   - sibling: `settings_integrations_ops_windowed` → `test result: ok. 1 passed` (`/tmp/opbtn-ops-23962.log`) — no regression to single-param begin_oauth flow.

2. **TEETH (red-for-the-right-reason, in-place, one tree).** cp op_button.rs → /tmp/op_button.rs.orig (sha 4ab5995f…). Neutered `open_or_dispatch` to call `services.present_op` unconditionally on the missing-params branch.
   - RED: `test result: FAILED. 0 passed; 1 failed` (`/tmp/opbtn-teeth-red-38256.log`). Panic = `present_op(integration.set_field): 2 required param(s) unresolved and this call site has no param-collection popup`, surfaced by the test as "must open a param-collection popup, not panic." Correct reason.
   - Restored from /tmp/op_button.rs.orig → sha 4ab5995f… byte-identical.
   - GREEN: `test result: ok. 1 passed` (`/tmp/opbtn-teeth-green-46297.log`).

3. **Unit tests.** `cargo test -p holon-frontend --lib` → `test result: ok. 487 passed; 0 failed` (`/tmp/opbtn-unit2-31275.log`). Includes 5 `param_collection::tests::*` (all ok) and 12 `command_provider::*` named tests. Non-zero counts. (`-p holon-frontend param_collection command_provider` fails — cargo takes one positional TESTNAME; ran full lib instead.)

4. **cargo check.** `cargo check -p holon-gpui -p holon-frontend --features holon-gpui/pbt` → exit 0 (`/tmp/opbtn-check-13308.log`). The 2 warnings (`unused_variable` editor_view.rs:1345, `unused_must_use` reactive_shell.rs:493) are in pre-existing files NOT in this diff.

5. **Dispatch parity — CONFIRMED at code level.** The popup's terminal pick calls `services.dispatch_intent(intent)` (op_button.rs `choice_row`). `ReactiveEngine::present_op` (reactive.rs:3757), when params are satisfied, itself calls `self.dispatch_intent(...)` (reactive.rs:3763). Same method → same journal record (`journal.record`, reactive.rs:3626), same `latency_e2e::interaction_dispatched` (reactive.rs:3700), same `surface_op_failure` on the Err arm (reactive.rs:3739). Identical path.

6. **Fail-loud unsupported — CONFIRMED.** `ParamCollector::current()` returns `CollectStep::Unsupported{reason}` for kinds with no pointer affordance (choices_for → None for String/Number/EntityId/Object/Expr/Collection). op_button `build_param_popup` renders that reason in a `t.danger`-colored row (visible), never a silent close. Unit test `an_uncollectable_kind_fails_loud_not_silent` covers the EntityId case (passing). A `CollectStep::Ready` reached during render also renders a danger "internal error" row.

7. **present_op panic guard — CONFIRMED, comment current-state-only.** Guard retained at reactive.rs:3776. New comment (reactive.rs:3770-3775) describes only current state; panic message no longer says "follow-up to mobile-bar PR". No history/PR references.

8. **Refactor signal — PRESENT.** op_button.rs:169-174 names trigger ("When a THIRD driver appears, or a flow needs a non-anchored / app-level popup") and destination ("one frontend-session-level popup surface observed from present_op (Option B in ~/.claude/plans/holon-opbtn-param-popup-2026-08-20.md)"). Plan file exists (16359 bytes, /Users/martin/.claude/plans/…).

9. **Bugfunnel — PASSES.** `python3 scripts/bugfunnel.py check` → `488 entries, 0 problems`, exit 0. Frontmatter shape (id/date/gap/secondary/status/summary) matches siblings; `secondary: null` valid for single-gap. COVERAGE classification defensible: the failing path paints in the windowed harness (not ENVIRONMENT), a panic trips any rung (not ORACLE), the multi-param op_button click was simply never generated by any test.

10. **Scope — CLEAN.** `jj diff -r 6ed1299c --stat` = exactly the 6 claimed files (lib.rs +1, param_collection.rs +345, reactive.rs ±14, bugfunnel entry +116, op_button.rs +243, new PBT +333). No fmt churn, no asset/default changes. Working copy restored to sha 4ab5995f after TEETH.

## Finding (non-blocking, comment accuracy)

op_button.rs:169-170 states the shared `ParamCollector` "now has two thin drivers — the editor slash menu (`command_provider` via `EditorView`) and this op_button overlay." **This is inaccurate.** `grep -rln "param_collection::"` shows op_button.rs is the ONLY importer of `param_collection::`; `command_provider.rs` does not use `ParamCollector` at all (it has its own separate async EntityId-search collection; the sole hit there is an unrelated test name `param_collection_select_executes`). There is currently exactly ONE driver, not two. The refactor-signal TRIGGER ("third driver") is still meaningful, but its premise ("now has two") misdescribes current state — a comment-rot hazard per the commenting guideline. Does not affect the feature; noted for the fix router.

---

# Delta round 625891c5

**VERDICT: REFUTED (narrow, reproducible).** F2 latency closure, F1 Escape dismissal, no-dispatch-on-cancel, scope, and all build gates hold. The refutation is specific: the test `opening_a_second_rows_op_button_closes_the_first` does NOT pin the `on_mouse_down_out` outside-click wiring — it passes with that handler dead — so it is a false witness for the claimed outside-click dismissal, and "single-open emergent from outside-click" is misattributed. Single-open is actually delivered by the ephemeral-cache wipe on structural rebuild.

pwd: `/Users/martin/Workspaces/pkm/holon/.claude/worktrees/opbtn-popup`. Frozen rev wyxpzqtv / 625891c5 (delta from f0caaf91). Tree fully restored after probes: op_button.rs sha 2567962e, integration_projection.rs sha 4c0d7634; `jj diff --from 625891c5 --to @` = 0 files.

## The counterexample (reproducible)

cp op_button.rs aside (sha 2567962e), guard ONLY the `on_mouse_down_out` body with `if false` (Escape left fully live), rebuild, run the 3-test file:
- Result: `test result: ok. 3 passed` (`/tmp/opbtn-d2-mouseout-67361.log`). `opening_a_second_rows_op_button_closes_the_first` PASSES with the outside-click handler dead.
- Earlier, neutering BOTH handlers: `escape_closes_...` FAILED ("Escape must close the param-collection popup", PBT:473) but `opening_a_second_...` still PASSED (`/tmp/opbtn-d2-f1red-55557.log`).
- Restored byte-identical (sha 2567962e) both times.

Mechanism: `OpParamPopup` is cached under `CacheKey::Ephemeral(...)`, which is NOT state-bearing (`entity_view_registry.rs:71`). `wipe_ephemeral` (`entity_view_registry.rs:250`) drops every non-state-bearing ephemeral entry on each structural rebuild of the ReactiveShell; opening a second row's popup is a structural rebuild, which wipes the first row's popup entity, resetting its collector to `None`. That — not `on_mouse_down_out` — is what closes gcal's popup when todoist's opens. The op_button.rs top comment (lines 14-17) even states the entity "is wiped on a structural rebuild."

Consequences:
- The claim "new test opening_a_second_rows_op_button_closes_the_first [proves the outside-click dismissal]" is refuted; that test would pass even if `on_mouse_down_out` were deleted.
- The mechanism attribution "single-open emergent from outside-click" is wrong in BOTH the code comment (op_button.rs:133-134) and the F1 bugfunnel entry (`2026-08-20-opbutton-popup-has-no-cancel-path.md`, "Single-open: emergent from outside-click").
- The pure outside-click-on-empty-space dismissal is UNTESTED: no test in the file fails when `on_mouse_down_out` is dead. I did not prove it broken (empty-space click is never exercised), only that it is unverified and unnecessary for the multi-button case.

## Checks that HELD

1. **Scope (delta f0caaf91→625891c5)**: exactly integration_projection.rs (+27), op_button.rs (+60), the windowed PBT (+197), and the 2 new bugfunnel entries (cancel-path, e2e-never-closes). lib.rs is NOT in this delta (it landed in the prior rev) — no unexpected files. `opbtn-verify.md` is my report.
2. **Windowed PBT (3 tests) + begin_oauth sibling**: `ok. 3 passed` (`/tmp/opbtn-d2-pbt-6893.log`) and `ok. 1 passed` (`/tmp/opbtn-d2-sib-21802.log`).
3. **F2 teeth**: guard `rows_delivered` with `if false` (sha 4c0d7634 → probe → restore) → `clicking_multi_param...` FAILED with e2e-expiry semantics ("must CLOSE once the projection lands ... expires as `e2e_expired`", `/tmp/opbtn-d2-f2red-10675.log`); the other 2 tests still passed. Restored byte-identical → `ok. 3 passed` (`/tmp/opbtn-d2-f2green-41911.log`). F2's `interaction_still_pending == false` assertion lives in the main test (PBT:342-346) via `latency_e2e::pending_targets()`.
4. **F1 Escape teeth**: neutering Escape → `escape_closes_the_param_popup_without_dispatching` RED for the right reason (popup never closes). Both cancel paths (`on_key_down` escape, `on_mouse_down_out`) only clear `collector`/`focus` + `window.refresh()`; neither reaches `dispatch_intent`/`present_op` — code-read confirmed (op_button.rs:151-167). Both dismissal tests also assert `mirror_enabled == initial` (no dispatch).
5. **F2 report call is on the projection-visible moment**, not a test hook: `latency_e2e::rows_delivered("integration_state", ...)` sits inside `IntegrationStateProjector::project()` after the mirror upsert/DELETE (integration_projection.rs:199), over the projected ids.
6. **F1 bugfunnel entry root cause** = missing dismissal wiring; it explicitly records that the earlier "impossible under per-row cached-view design" claim was REFUTED. No impossibility claim remains. (But it carries the misattributed single-open mechanism — see above.)
7. **Gates**: `cargo test -p holon-app integration` → 8 passed, 0 failed (`/tmp/opbtn-d2-app-79915.log`). `cargo test -p holon-frontend --lib` → `487 passed` (`/tmp/opbtn-d2-frontend-8854.log`). `cargo check -p holon-gpui --features pbt` → exit 0 (`/tmp/opbtn-d2-gpuichk-13870.log`). `cargo fmt -p holon-app -p holon-frontend -p holon-gpui --check` → exit 0, no diff. `python3 scripts/bugfunnel.py check` → 490 entries, 0 problems.

## Recommendation for the fix router (not applied — verifier does not fix)

Either add a test that pins `on_mouse_down_out` in isolation (e.g. click on empty Settings space with a single popup open, assert it closes — a case ephemeral-wipe does NOT cover, since no structural rebuild occurs), or correct the mechanism attribution in the comment and the F1 entry to name the ephemeral-cache wipe as the single-open mechanism. As written, deleting `on_mouse_down_out` would regress empty-space dismissal with zero test coverage catching it.

---

# Delta round 5f190d37

**VERDICT: CONFIRMED.** The lane's response resolves my prior refutation. The new rung genuinely pins `on_mouse_down_out`, the attribution is corrected in both the comment and the F1 entry, and all gates hold. One minor doc nit (wrong wipe-function name), non-blocking.

pwd: `/Users/martin/Workspaces/pkm/holon/.claude/worktrees/opbtn-popup`. Frozen rev wyxpzqtv / 5f190d37 (delta from 625891c5). Scope of delta = F1 bugfunnel entry, op_button.rs comment (cosmetic), the windowed PBT (+65: new rung + env-caveat header + two-row re-label), and opbtn-verify.md (my report) — nothing else. Tree fully restored after probe: op_button.rs sha 4a7d4682; `jj diff --from 5f190d37 --to @` = 0 files.

## (a) 4-rung file + sibling
`test result: ok. 4 passed` — clicking_multi_param, escape_closes, opening_a_second_rows, outside_click_on_inert_space (`/tmp/opbtn-d3-pbt-4797.log`). begin_oauth sibling `ok. 1 passed` (`/tmp/opbtn-d3-sib-4797.log`).

## (b) Counterexample REDONE against the new rung — now closes
cp op_button.rs aside (sha 4a7d4682), guard ONLY `on_mouse_down_out` with `if false`, rebuild, run all 4 rungs (`/tmp/opbtn-d3-teethred-42035.log`):
```
clicking_multi_param_set_field_opens_param_popup_then_dispatches ... ok
escape_closes_the_param_popup_without_dispatching ... ok
opening_a_second_rows_op_button_closes_the_first ... ok
outside_click_on_inert_space_closes_the_popup_without_dispatching ... FAILED
  assertion failed: ... must close it via on_mouse_down_out
test result: FAILED. 3 passed; 1 failed.
```
Exactly the inert-space rung goes RED; base + escape + two-row stay GREEN. Restored byte-identical (sha 4a7d4682) → `ok. 4 passed` (`/tmp/opbtn-d3-teethgreen-70566.log`). The rung now IS teeth for the handler — my prior refutation is answered.

## (d) Inert-space click genuinely avoids structural rebuild — CONFIRMED
The teeth in (b) is itself the proof: with `on_mouse_down_out` dead the popup STAYS OPEN (painted_count left=1, right=0), so the click at `(btn.x-150, btn.y+60)` triggered NO structural rebuild (else `wipe_ephemeral` would have closed it → green) and hit NO op_button/toggle (else dispatch/rebuild). Only the handler closes it. Not a false witness. The rung also asserts gcal's mirror unchanged (no dispatch).

## (c) Attribution accuracy — corrected, no overclaim
- op_button.rs comment (lines 130-138): now says opening another row's button "is itself such an outside click, so this is the single-open mechanism the code relies on" — production-facing and defensible (dogfood observed live stacking without the handler).
- F1 entry MECHANISM ATTRIBUTION paragraph correctly states the two-row rung is NOT teeth for `on_mouse_down_out` (closed by the ephemeral wipe on structural rebuild in the windowed env; stays green with the handler `if false`), names the inert-space rung as the one that pins the handler, and cites the live pre-fix stacking as evidence the wipe does not fire in prod. This matches my §Delta-round-625891c5 findings exactly.
- Two-row rung re-labeled in-code as an end-to-end "never two stacked" guard. PBT header carries the environment caveat.

## (e) Gates
`cargo fmt -p holon-gpui -p holon-frontend -p holon-app --check` → exit 0. `python3 scripts/bugfunnel.py check` → 490 entries, 0 problems.

## Minor nit (non-blocking, for the router)
The F1 entry names the wipe function `evict_ephemeral_with_prefix`. The function that actually fires on a ReactiveShell structural rebuild is `wipe_ephemeral` (`frontends/gpui/src/views/reactive_shell.rs:254`, retains only state-bearing keys); `evict_ephemeral_with_prefix` is the EditorView-defocus path (`render_entity_view.rs:192`, prefix-scoped). The mechanism described (ephemeral wipe on structural rebuild) is correct — only the cited symbol is wrong. Worth a one-word fix in the entry; does not affect the code or the fix.
