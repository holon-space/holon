---
date: "2026-04-29 09:21"
session: "43559f70-5053-480b-b12b-ba6784ddaa78"
project: "holon"
---

## Code Graph MCP Connection Troubleshooting

We were attempting to use the code-graph-mcp server to find similar code patterns, but encountered a connection failure during the session.

**What happened:**
- User asked about parameters for `find_similar_code` tool
- Clarified that the actual available tools are `semantic_code_search`, `get_call_graph`, `get_ast_node`, and `trace_http_chain` within the code-graph-mcp server
- Attempted to reconnect to the code-graph-mcp plugin via `/mcp` command
- Connection failed with "Failed to reconnect to plugin:code-graph-mcp:code-graph" error

**Challenges:**
- The code-graph-mcp plugin became unavailable mid-session, preventing tool exploration and semantic code search capability
- The specific tool `find_similar_code` doesn't exist in this MCP implementation — the correct approach uses `semantic_code_search` for finding similar patterns

**Status:** Unable to proceed with code similarity analysis due to MCP plugin connection loss. Next step would be to either reconnect the code-graph-mcp server or clarify the specific code/function to search for before attempting again.
