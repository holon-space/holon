---
date: "2026-07-30 21:45"
session: "2324b6e3-f9b4-43f1-9541-bccefa996cfc"
project: "holon"
---

## Integrated Opus-5 context-engineering guidance into orchestrator skill

The orchestrator skill needed updates based on Anthropic's new best practices for prompting Opus 5/Fable 5 generation models, which fundamentally inverted several older patterns by removing 80% of system prompts with no loss in coding capability.

- Fetched the full HTML of Anthropic's blog post (WebFetch summary was too lossy) and extracted the core inversions: worked examples now *constrain* rather than clarify, rule pile-ups burn reasoning tokens, repetition has no recency-bias benefit in Opus-5-gen, specs-as-code beat prose, and progressive disclosure replaces front-loaded context.
- Added a new `## Prompting Opus-5-gen lanes` section to `orchestrator/SKILL.md:184` with actionable points: encode expectations in typed interfaces/enums rather than examples, deduplicate conflicting instructions, state target idioms instead of prohibition lists, remove say-it-at-top-and-bottom patterns, and point lanes at files to load at the step needing them.
- Carved out existing hard rules (no jj writes, pipefail, blocking gates) as deliberate exceptions to the "don't overconstrain" principle, so new and old guidance don't contradict.
- Logged the source in SKILL.md's header notes; deliberately excluded points about auto-memory and CLAUDE.md-as-gotchas-only since those concern repo config, not orchestration.
- Simultaneously refactored IMPROVEMENTS.md: pruned 597→47 lines by eliminating all entries already folded into SKILL.md and moving adopted rules into their proper sections (verifier protocol, gate-integrity rules, weave hygiene, build-economics liveness, boilerplate conduct).
- Updated Non-negotiable #4 to enforce deletion on adoption (not marking-in-place), making the policy self-enforcing; preserved pre-slim IMPROVEMENTS.md as `IMPROVEMENTS-ARCHIVE.md` (unloaded, zero runtime cost).

**Decisions**: Preserved archive rather than hard-delete for incident evidence since `.claude` isn't version-controlled. Left the question of splitting SKILL.md (now 383 lines) into referenced side files for a future ruling, per the article's own progressive-disclosure advice.

**Status**: Orchestrator skill now embeds non-obvious Opus-5 prompting patterns; IMPROVEMENTS.md contains only unimplemented proposals (two awaiting ruling, eight tooling fixes).
