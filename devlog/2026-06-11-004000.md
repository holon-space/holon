# 2026-06-11 — caret/indent reds fixed + Phase 2 main-panel virtualization

## Handoff reds (both fixed)

1. **Indent sibling-order (prod)**: `BlockOperations::indent` anchored at
   `get_children().last()` — an unordered `get_all()` filter — so the
   indented block landed mid-group (creation order). Anchor now resolves
   through the wired `BlockOrdering` (`ordering().children(..).pop()`),
   Loro live tree in Loro mode, sort_key order in SqlOnly.
2. **Caret-after-Indent (harness)**: headless chord dispatch never modeled
   its implicit click. New `HeadlessEditorMirror::seed_for_click` seeds
   end-of-text — skipped when the entity is already the focused editor
   (no click happens; mirrors `model_chord_click_focus`'s early-return).
3. **Gherkin text@caret-0 (prod GPUI)**: `grab_focus_and_seed_caret` had
   lost its end-of-text default. Restored as `default_caret_to_end`:
   true on the sync first-mount grab, false on the async focus binding.
   Safe under blur-on-focus-leave (pre-mount keys are dropped + retried,
   never consumed at a stale caret).

Gates: Full e2e PASS 58s (pinned multibyte seed green), sql_only PASS 51s,
gpui_gherkin_replay clean, holon-core 57/57, frontend+gpui units 304/304.
`loro_content_drop_pbt_fixtures` red is PRE-EXISTING (repros on bare main
082a385d): ApplyMutation "waiting for 12 blocks, got 3".

## Phase 2 — main-panel virtualization

- `reactive_shell.rs` block-mode render: eager collection branch now gated
  behind `HOLON_EAGER_PANEL_RENDER=1` (one-release rollback); default falls
  through to `builders::render`'s collection arm → cached `ReactiveShell`
  + `gpui::list` (viewport-only row materialization).
- `ReactiveShell::scroll_to_reveal_raw_index`: maps a raw
  `children_snapshot()` index through `visible_indices` before
  `scroll_to_reveal_item` — `scroll_entity_into_view` passed the raw index
  (latent off-by-collapse bug, load-bearing once the main panel
  virtualizes). Collapse-hidden rows return false (disclosed, not wedged).
- List-path row-gap floor aligned to the eager branch (max 2.0) so the
  main panel renders identically through either path.
- Stale "block-mode panels don't virtualize" comments updated
  (lib.rs, gpui user_driver.rs, pbt sut.rs).

## Phase 3 — per-frame/per-event churn (partial, same change)

- `RenderContext.data_rows` → `Arc<[Arc<DataRow>]>`: ctx is cloned twice
  per row in the collection pipeline; the Vec clone made that O(N²).
- ProviderCache fingerprint: per-lookup Debug-format String → structural
  u64 hash (Value/RenderExpr walkers, key-sorted Objects, f64::to_bits).
- rendered_text: dropped the per-frame displayed-content clone.
- image: positive cache for the per-frame `Path::exists()` stat.

Remaining for next session: live_query streaming through
`ReactiveView::new_collection` (behavior-sensitive), ElementInfo
`Arc<str>` conversion (wide), Phase 4 (matview GC env-gated, LiveData
Arc dedup, navigation/undo retention caps), dhat + RSS measurement
(deferred — concurrent session building in this tree all session).

## Phase 4 early item — watcher leak on removal diffs

`ReactiveShell.props_watchers` is now `Vec<[Task; 2]>` index-aligned with
`items`: RemoveAt/Pop/Move/Clear drop or move the row's watcher pair.
Previously removal diffs left the dead tasks (and the removed row's
signal + data Arc they pin) alive until the next full Replace.
