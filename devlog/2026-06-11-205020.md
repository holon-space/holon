---
date: "2026-06-11 20:50"
session: "9c3c2c38-6c41-4760-9907-b122d2722c47"
project: "holon"
---

## Replication.md cleanup and Phase 1 sequencing plan

Cleaned up Replication.md's inline comments by integrating them into the document structure, establishing consistency around three key concepts: 3-way merge as intent extraction, architectural optionality of the org backing, and the structural/text split in consolidator capabilities.

- **Integrated intent-extraction framing** across §1, §3, and §4: 3-way merge recovers intent from out-of-band edits; LoroText applies this uniformly for text fields regardless of consolidator mode
- **Clarified stability vs. architecture** in durable-base section: org default is human-recoverability, not architectural requirement; Loro becomes default once stable
- **Established structural/text capability split**: structural merge (tree CRDT parent/order/move) degrades LWW-only with Turso; text merge never degrades — LoroText handles it in all modes
- **Removed degenerate text-diff fallback**: no mode requires it; text intent is either LoroText ops or non-ops (LWW whole-value, git 3-way)
- **Resequenced Phase 1 to 5→3→1→2→4**: Item 5 (intent carries `after_sibling`) first as wire-type prerequisite; items 3+1 together (fi projection + one-line delete bug fix); Item 2 follows intent path; Item 4 descoped to need-driven implementation

**Challenges**: The `sole_block_writer` end-state framing exposed a deeper design question — whether a single writer implementation parameterized by consolidator (rather than writer-specific exclusions) is Phase 1 or Phase 5 work. Decided to land exclusion-based version first, note convergence as target. Stale-vault unseeded trees identified as hard prerequisite (re-seed adoption pass) rather than nice-to-have.

**Status**: Phase 1 spec rewritten with corrected sequencing and precondition clarification. Ready for implementation planning.
