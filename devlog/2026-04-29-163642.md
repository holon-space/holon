---
date: "2026-04-29 16:36"
session: "30d309ba-3514-446f-b0da-a2cb765f0d6e"
project: "holon"
---

## Senior Dev Code Review: Plan Critique & Refinement

**Context:** A plan to migrate the editor's mutable text from SQL CDC to Loro CRDT was submitted. Rather than approve it, we performed a ruthless senior review, cross-checking claims against the live codebase and surfacing implicit assumptions.

**What happened:**

- **Read and validated 18 explicit hypotheses** against `Cargo.toml`, `loro-internal-1.11.1/src/event.rs`, `gpui-component/state.rs`, and the plan's own architecture docs
- **Falsified the core API claim:** plan repeated "filter by `event.local == false`" 4× — Loro 1.11.1 has no `local` boolean. Real API is `triggered_by: EventTriggerKind` + `origin: &str` (verified at `loro-1.11.1/src/event.rs:32-37`)
- **Found a latent race condition:** plan's `applying_remote: AtomicBool` reinvents functionality that already exists as `silent_replace_text` in `InputState` (verified `gpui-component/state.rs:2060`). The existing impl is load-bearing; the new one is racy
- **Audited Phase 0's sync-closure claim and found it false:** `loro_module.rs:191–192` holds a read guard across `.await` on `rehydrate_shared_trees`, contradicting the plan's "none hold the lock across await" assertion
- **Identified 6 additional severity-1 gaps:** marks regression risk, utf16↔codepoint index mismatch, container-ID divergence across peers, missing SQL→Loro backfill, IME composition handling, cursor sticky-bias semantics
- **Rewrote the plan** incorporating all fixes: origin-string tagging convention, per-container subscriptions instead of root, provider-side `(BlockId, FieldName)` cache, explicit backfill phase, utf16 conversion boundaries, IME guard, reframed phase boundaries, and 8 follow-up validation tasks

**Challenges:**

- **Tautological invariant:** `inv-mutable-text-convergence` checked two reads from the same `Arc<LoroDoc>` — always equal, no real validation. Replaced with `inv-cross-peer-text-convergence`.
- **Overstated phase independence:** Phase 3's invariants depend on Phase 2 (focus-gate removal) being landed. Corrected the phase boundary.
- **Missing migration story:** 35+ blocks with pre-Loro content would render blank. Added explicit backfill requirement.

**Decisions:**

- **Keep dual-write paths per keystroke** (Loro + SQL intent) rather than removing intent dispatch; break the echo loop via origin-string filtering — incremental, lower risk than (b) removing intent dispatch entirely
- **Reuse existing `LoroSyncReference` pattern** for PBT reference state instead of orphaning `LoroText` handles without a backing `LoroDoc`
- **Gate Phase 0.5 on a preliminary audit** of the 35+ closures in the 6 missing files (`loro_module.rs`, `matview_manager.rs`, etc.) to catch hold-across-await cases before refactoring

**Status:** Plan rejected pending fixes; refined version with origin-string API corrections, 8 gated follow-up tasks, and phase
