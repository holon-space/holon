---
date: "2026-05-09 21:32"
session: "7135c9e0-2ce1-448a-94bd-3a8b1fd9a3e3"
project: "holon"
---

## Phase 2 Cleanup Pass: Architecture Documentation & Audit Gates

Authority flip implementation is landed; now solidifying it with audit gates, architecture documentation, and verification that all Phase 1–2 deprecated symbols are truly gone.

### What happened

- **Audit grep** for deleted symbols (`BlockContentResolver`, `live_content`, `EditableTextProvider`, `MutableText` struct, `with_content_resolver`, etc.): confirmed 0 source hits. The `_expected_*` watermark strings only appear in regression-gate tests that assert they stay gone.
- **Architecture smell gate** added to `archlint/smells/words.toml` (`deleted_cell_symbol` rule) covering all the above identifiers plus watermark literals. Phase 2 files all pass `archlint check`.
- **MEMORY.md** updated with new entry `cells_and_loro_authority.md` (LANDED Phase 1+2); predecessor entries ("MutableText does NOT synchronously commit", "Production split_block silently discards pending edits") marked SUPERSEDED.
- **ARCHITECTURE.md** extended with "Cells & Authority/Projection Model" subsection between `CollaborativeDoc` and `LoroBackend`, covering cell primitives, the dispatcher table, the authority diagram, `_expected_*` removal rationale, and the arch-test gate.
- **May-9 handoff devlogs** both carry an explicit "Phase 2 LANDED" addendum pointing to closing devlog and MEMORY entry.

### Decisions

- Skipped `cells_only_constructed_in_registry_layer` and `cells_are_sole_block_writer` arch-test gates: they would block legitimate test fixtures and don't match the pragmatic Phase 2 shape (some fields intentionally SQL-first for now).
- Kept inbound runtime SQL→Loro path active (not arch-gated) for fields without clean Loro encoding (`marks`, `sort_key`, `content_type`, etc.).

**Status**: Cleanup pass complete. Phase 1+2 are documented and gated; PBT TypeChars divergence (carried from Phase 1) deferred to next session.
