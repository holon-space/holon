---
date: "2026-03-24 09:53"
session: "da5eb94c-63d5-4652-83ec-a21709fe0c35"
project: "turso"
---

## Squashed IVM PR into single commit based on main@upstream

**Context:** With 51 revisions between `main@upstream` and `holon`, we needed to extract the Incremental View Maintenance feature into a clean, reviewable PR for upstream while keeping the two standalone bug fixes (`WAL read lock leak` and `CDC constant-false WHERE`) as separate PRs.

**What happened:**
- Identified that `fix-wal-read-lock-leak` and `fix-cdc-dml-constant-false` were already well-positioned as separate branches on `main@upstream` with proper test-then-fix structure — left them untouched
- Extracted all ~40 remaining IVM-related commits into a single clean commit
- Removed local-only artifacts: handoff docs (18 files), workspace-hack additions, nightly toolchain config, `.cargo/`, `.claude/`, `.pre-commit-config.yaml`, `sql-replay` tool, `jj-rebase.sh`, TODO/investigation docs, and Cargo.lock noise
- Restored `core/Cargo.toml` to upstream state (retaining upstream dependencies: `fallible-iterator`, `regex-syntax`, `aes`, `num-traits`) and added only the necessary `matview_benchmark` bench entry
- Verified file contents: all 121 files in `ivm-pr` are byte-identical to `holon` (except intentionally excluded `core/Cargo.toml`)

**Status:** `ivm-pr` bookmark created — a single clean commit on `main@upstream` with 122 files changed (~23.5k insertions), ready for upstream PR submission. Original `holon` branch unchanged and still contains all 51 revisions; merge ancestry preserved via the three parent bookmarks (`fix-wal-read-lock-leak`, `fix-cdc-dml-constant-false`, `ivm-pr`).
