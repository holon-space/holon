---
date: "2026-06-11 07:49"
session: "72dc4a3f-3acc-4d0b-9f8f-6f5655254afc"
project: "memory-reduction"
---

## Memory-reduction devlog: handoff reds fixed, Phase 2–3 landed, pre-existing reds surfaced

**Context:** Continuing from a prior session's handoff on a memory-reduction initiative. Two failing test gates (indent sibling ordering, caret divergences) needed root-cause fixes; Phase 2 (main-panel virtualization) and Phase 3 (churn reduction) were ready to land.

**What happened:**

- **Indent sibling-order bug:** `indent()` anchored "last child" via unordered `get_children()` filter, landing blocks mid-sibling-group. Fixed by routing through `BlockOrdering` authority (Loro live tree / sort_key). Concurrent session absorbed this fix into main mid-flight.
- **Caret divergences:** established 3-layer chord-click convention (click non-focused → editor at end-of-text; chord already-focused → no-op). Headless got `seed_for_click` guard; GPUI got safe `default_caret_to_end` back (true on mount, false on async focus binding). Fixed gherkin "HiFirstBlock" red and GPUI harness reds.
- **Phase 2 main-panel virtualization:** landed eager-every-frame branch behind rollback flag; main panel now renders through `gpui::list`. Fixed raw-vs-visible index bug in `scroll_entity_into_view` via `ReactiveShell::visible_index_of` (concurrent session's superior design), ported Phase-2 regression tests.
- **Phase 3 churn batch (partial):** `RenderContext.data_rows` → `Arc<[Arc<DataRow>]>` (kills O(N²) clones), ProviderCache fingerprinting via u64 hash, dropped per-frame content clone, fixed `props_watchers` index alignment (now actually drops watcher tasks on row removal).
- **Merge conflict resolution:** main's `visible_index_of` direct-shell-query design supersedes my raw-index mapping; removed redundant `scroll_to_reveal_raw_index`, ported tests, all gates green.
- **Pre-existing reds surfaced:** `gpui_capture_replay` (typed "fm4" → "4", TursoProjection keystroke drop) and `loro_content_drop_pbt_fixtures` (ApplyMutation timeout) — both proven on bare main, not from this work.

**Challenges:**

- **Concurrent-session collision:** four main rewrites mid-flight (divergence fix, same-bug-area scroll redesign, rebase conflict, absorbed edits). Resolved by taking their superior design, merging doc comments, removing redundant code.
- **Virtualization index mapping:** raw snapshots vs. visible (collapse-filtered) indices — solved via querying the panel cache's live `ReactiveShell`s directly instead of walking fresh engine state.

**Decisions:**

- **Measurement deferred:** repo forbids measuring under concurrent builds; deferring dhat/RSS baseline until Phase 3 is fully landed and the workspace is clean.
- **Rollback flag:** `HOLON_EAGER_PANEL_RENDER` left in place for safe rollback; cleanup happens post-soak.

**Status:** Change `oxzuoovm` (20 files, +457/−137) on main with both handoff reds fixed, Phase 2–3 landed, all gates green (306
