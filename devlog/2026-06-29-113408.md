---
date: "2026-06-29 11:34"
session: "d901649c-32e0-4611-b55e-d92c24ac4a09"
project: "block-event-storm"
---

## BUG B Detection: Red Proof Blocker Discovered

The red proof completed green (exit 0, 287s), exposing a **structural flaw in the plan**: the transition that writes `requires` and the invariant that checks it live in two different harnesses that never overlap.

### What happened

- Added `requires` generator arm to `ApplyMutation::weighted_generator`, gated on `enable_loro` + ≥2 blocks, emitting UI Updates via production `LoroBackend::set_block_requires`
- Added SUT routing in `apply_apply_mutation_to_sut` to dispatch `requires` Updates through the real backend (fail-loud if missing)
- Added `inv-blocks-match-ref/matview` to `WIDE_REQUIRED_INVARIANTS` as the catching mechanism
- Ran reduced-case PBT (`PROPTEST_CASES=12`) expecting an `inv-blocks-match-ref/matview` divergence — **got green instead**

### Root cause: capability split excludes the write path

Investigated why the catch never fired. Found two independent facts:

1. **`ApplyMutation` auto-narrows out of composed PBT**: `full_headless` deliberately lacks `SutSeamMutate` (split from `SutMutate` on 2026-06-21 because `ApplyMutation` is harness glue with no single production operation). Builder asserts this (`builder.rs:939-942`), so the transition never runs in `general_e2e_composed_pbt`.

2. **`/matview` doesn't run in `E2ESut`**: The invariant lives in `NATIVE_ONLY_EXCLUDED` (`invariant_runner.rs:666`) because `SutBackend` was deleted off `E2ESut` in E3. It only runs in the composed catalog.

The write and the check are in two harnesses that never intersect — the plan is uncatchable as designed.

### Viable alternatives

**Option 1**: Add `SetRequires` transition to the existing `SutBlockTreeWrite` cap (which `full_headless` does host). New trait method (~6 implementors), caught by `/matview` in composed PBT. Grows scaffolding, overturns the 2026-06-29 decision.

**Option 2**: A dedicated projection-level PBT in `holon-loro` — parallel to how BUG A's catch is a focused `proptest` there, not the E2E PBT. Property-test `project()`/`blocks_differ` directly: seed a block, `set_block_requires`, re-project, assert `requires` reflects the change. No new caps, no E2E scaffolding. Feasibility hinges on `project()` being drivable with observable SQL in `holon-loro`'s scope.

### Status

Paused implementation. Tree contains dead code (`ApplyMutation` arm + SUT routing that never runs). Awaiting decision on which alternative fits your intent — north-star emphasis on "ONE composed PBT" suggests Option 1, but Option 2's symmetry with BUG A's accepted catch and avoidance of E2E scaffolding growth deserves consideration.
