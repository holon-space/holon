---
date: "2026-05-07 23:07"
session: "43cdd8f8-9cfd-4982-9da2-4a90d8e248bf"
project: "holon"
---

## Verified Turso IVM fix and clarified remaining loose ends

A critical Turso bug (IVM LEFT JOIN dropping null-padded rows on redundant UPDATEs) was pinned in `Cargo.lock` and needed verification. The session confirmed the fix works and clarified what's left to do.

- **Verified the fix**: Ran the minimal repro (`devlog/2026-05-07-turso-ivm-focus-roots-minimal.sql`) against pinned commit `05c3326752ff…`. All assertions pass; exit code 0; verdict: "No IVM inconsistencies detected."
- **Pre-fix state confirmed**: The same repro on the unfixed code reported `INCONSISTENCY in block: matview=0 fresh=1 missing=1` (exit 1), confirming the diagnosis was correct.
- **Identified two uncommitted artifacts**: `Cargo.lock` (the pin bump) and `devlog/2026-05-07-turso-ivm-focus-roots-stale-rows.md` (rewritten bug report with correct root cause).
- **Clarified band-aid option (#2)**: Adding `AND <col> != <new_value>` guards to `set_field` UPDATEs at `crates/holon/src/core/sql_operation_provider.rs:1051` would prevent redundant UPDATEs from reaching IVM. Skipping it because the upstream fix is already pinned and the PBT already alarms on regression.
- **Clarified trace-tagging option (#3)**: Current `trace_sql` doesn't emit `actor_tx_begin/commit` markers, so replayed traces lose original transaction boundaries. Only worth implementing if upstream review requests transaction-grouped reproductions.

**Status**: Bug fix verified and pinned. No code changes needed. Awaiting Martin to commit artifacts and decide whether to file upstream issue.
