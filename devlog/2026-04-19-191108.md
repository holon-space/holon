---
date: "2026-04-19 19:11"
session: "08f38821-5d62-4d36-9dbb-6b227b23f986"
project: "holon"
---

## Value Function PBT Invariants — Deferred Items Completion

**Context:** Following the initial handoff work on value function caching and invariant detection, three deferred tasks remained: production wiring of `navigation.focus` into `set_focus`, fixture probability tuning for the mobile action-bar, and sharpening the cache-reuse metric in inv12.

**What happened:**

- **Navigation focus mirroring**: Added `maybe_mirror_navigation_focus()` in `reactive.rs` to intercept `navigation.focus` intent dispatches and mirror them into `UiState::set_focus`. Also updated the PBT's `test_environment.rs` NavigateFocus handler to set focus on the SUT's lazily-created ReactiveEngine (resolving a two-engine synchronization bug).

- **Fixture probability rebalance**: Modified `generate_render_source_mutation` to emit the mobile action-bar fixture at ~50% parity with other expressions (up from 17%), matching the `last_idx.max(1)` replica logic.

- **inv12 cache-reuse metric sharpened**: Now reports `provider groups=N reuse_groups=M reuse_sites=K` with a hard panic if provider identity destabilizes within a group—catching real cache bugs earlier.

- **Verification and debugging**: Discovered a PBT assertion was too eager (predicting focus_chain rows from *any* render_expression mentioning focus_chain, even if not active at inv time). Narrowed the prediction to only check active roots. Confirmed `general_e2e_pbt_sql_only` passes (112–119 s) with stable cache identity across 90+ invariant calls.

**Status:** All deferred items complete and verified. `general_e2e_pbt_sql_only` passes cleanly; pre-existing custom-property persistence bug remains orthogonal. Branch `peaceful-strolling-muffin` ready for merge.
