---
date: "2026-05-16 23:31"
session: "2401ea87-6304-4ec9-9cef-1eda6420194d"
project: "turso"
---

## Planning MatchCounter Rewrite to Antijoin Implementation

The bug handoff document outlined a comprehensive rewrite of MatchCounter to use antijoin logic, with significant algebraic improvements and one notable regression around matview format versioning.

**What happened:**

- Reviewed the delta formula rewrite (R-term uses `L_pre`, L-term uses `c_post`) — this eliminates the double-counting bug in the original `δL·[c_post] + L_post·([c_post]−[c_pre])` formula and removes the phase-coordination hack that was masking the issue
- Identified H1 (validate the chained-matview bug is actually in MatchCounter) as a critical gate before rewriting, matching hypothesis-validation discipline
- Confirmed test-first approach: convert MatchCounter bugs to `.sqltest` format *before* writing Antijoin code, improving on the original plan ordering
- Noted the state-machine enumeration with explicit cursor-lifetime and NULL-key-skip invariants tied to past bugs — stronger than generic "mirror JoinOperator" approach
- Accepted 5–7 day estimate as more realistic than initial 3–4 days (no reusable JoinOperator test fixtures available)

**Challenges:**

- **Matview format-version regression:** The handoff plan dropped the explicit format-version bump and migration code in favor of "drop matviews after upgrade; loud crash on stale matview is acceptable." This trades defensive robustness for simplicity in single-user context — requires decision on whether to restore the bump
- **Commit granularity trade-off:** Step 4 combines compiler wire-in + MatchCounter deletion into one commit vs. keeping them split for diff reviewability

**Status:** Plan reviewed and validated; awaiting decision on matview format-version bump before entering implementation phase.
