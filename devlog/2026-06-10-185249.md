---
date: "2026-06-10 18:52"
session: "b07cade8-cda1-4c93-aa74-82a5ce5e791c"
project: "pbt-harness-improvements"
---

## Promoted org-mode content + task-state axes to defaults; Bug B (join_block authority) fixed; Phase 3 ~60% complete

## Context

Continuing from Phase 3 of the PBT plan: axes 1–2 (content normalization, nesting) needed promotion to defaults after their extended sweeps passed; Bug B (backspace-after-split corrupting Loro state) required root-cause analysis and a multi-part fix; and axis 4 (profiles/GQL) needed env-gated landing. The Full test slice had been red for ~18 min before the session.

## What happened

- **Debugged Bug A** (`:PROPERTIES:` headlines): re-verdicted as org tag syntax, not a parser bug. Added `split_headline_tags` parser-mirror + tag-split lens in holon-org-format; pinned behavior with 4 deterministic tests; arm un-quarantined.
- **Promoted axes 1+2 to defaults**, which exposed a fixed-point normalization bug: `[[ x]]` wasn't idempotent over two round-trips. Implemented `normalize_content_for_org_roundtrip` iteration; verified with `org_render_fixed_point` parity.
- **Root-caused Bug B**: backspace-at-0 after split dispatches `join_block`, which deleted the merged block **SQL-direct** (bypassing Loro's cell registry). Implemented three-part fix: `delete_entity` trait-lifted in `EntityCellRegistry`, `delete_block_via_cells` wired into `join_block`, plus two harness-fidelity improvements (headless mirror now adopts `peek_caret_seed`, ref's DeleteBackward models per-keystroke joins).
- **Full test slice recovered**: from timeout/1064s-failure → **59s green**. Extended sql_only and guards all green.
- **Axis 4 landed env-gated** (profiles, GQL watch queries); **axis 3 unblocked** by adding `extended_gen_pbt_full` twin.
- **Forward-fixed concurrent ADR-0011 absorb**: fluxdi `ErrorKind::ProviderAlreadyRegistered` match + `spawn_loro_org_sync` caller wiring with direct `holon-filesystem` dep.

## Challenges

- **Idempotent normalization**: trim+marks need to iterate to fixed-point, not single-pass. Solved by wrapping in a while-loop checking `old == new`.
- **Loro authority pattern leak**: The third instance of "write bypasses Loro" (earlier: set_field, create; now: delete). Required lifting the entire delete path through `*_via_cells` routing, mirroring split's pattern.
- **Mid-flight upstream churn**: ADR-0011 session's absorb broke the fluxdi match + spawn_loro_org_sync signature during the session. Forward-fixed both; noted for revert if a new fluxdi rev ships.

## Decisions

- **Re-verdict Bug A as org-canonical**: tag syntax moves content to `block.tags`; harness lens normalizes, not prod parser. Aligns with org spec.
- **Promote axes 1+2 immediately**: the extended sweeps were solid and blocking 3/4. Layout-override CI slice for axis 4 deferred (env-gate is stable).
- **Audit authority pattern systematically next
