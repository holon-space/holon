---
date: "2026-03-16 19:15"
session: "ca1028e1-0a3a-4500-9a0a-56e34fe8fe5a"
project: "turso"
---

## Array Literal in WHERE Clause Breaks Materialized View Creation

A materialized view using an array literal comparison in the WHERE clause (`WHERE name <> ['', 'index', '__default__']`) fails to create or execute properly.

**What happened**

- Attempted to reproduce the query by creating a minimal test case with `tursodb --experimental-views`
- Query structure: `CREATE MATERIALIZED VIEW ... AS SELECT *, 'document' AS entity_name FROM document WHERE name <> [...]`
- The array literal syntax in the WHERE clause (`<> ['', 'index', '__default__']`) appears to be the trigger — this is non-standard SQL that may not parse or compile correctly in the IVM compiler pipeline
- The bug report lacks error messages and symptom details (hang? crash? wrong results?), making reproduction difficult without running the actual command

**Challenges**

- **Incomplete bug report**: No schema for the `document` table, no error message or symptom description. Without knowing whether the query hangs, crashes, or silently returns wrong results, targeted reproduction is difficult.
- **Array literal syntax**: SQL array comparisons `<> ['a', 'b', 'c']` are not standard SQL (this looks like Python or similar). Need to verify whether Turso's parser even supports this syntax, or if the intended syntax is `NOT IN ('', 'index', '__default__')`.
- **First reproduction attempt blocked**: Cannot proceed with test writing until we can either trigger the bug or understand what the actual error is.

**Decisions**

- Need clarification from the reporter: (1) What is the exact error message or symptom? (2) Does `document` table exist and what are its column types? (3) Is the array literal syntax intentional, or should this be `NOT IN (...)`?
- If array literals are intentional, this may be a parser/translator issue rather than IVM-specific.

**Status**

Blocked waiting for clarification on the bug report. Ready to proceed with reproduction once error message and complete schema details are provided.
