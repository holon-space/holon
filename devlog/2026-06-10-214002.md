---
date: "2026-06-10 21:40"
session: "79b9230d-88d2-49c7-b234-8365a5440088"
project: "pbt-harness-improvements"
---

## Axis 3 and Axis 5: Peer Conflicts and Keyword Sets Complete

Completed two major phases of property-based testing infrastructure for the block editor: axis 5 (keyword set serialization) and axis 3 (peer conflict handling), with bonus fixture-replay refactoring and a peer-id determinism fix.

### What happened

- **Axis 5 landed & promoted**: `WriteOrgFile.keyword_set` now carries `TodoKeywordSet` with serde defaults for legacy captures; `ToggleState` converted to `CycleTarget` enum (parse-don't-validate); parity guard validates keyword sets survive serialize→parse round-trip (256 cases); fixed latent `ActiveEditor::delete_backward` byte-vs-char panic
- **Layout-override CI slice created** (`tests/layout_override_pbt.rs`): surfaced pre-existing axis-4 bug (custom PRQL layout → default "Holon Layout" leak); env-gated pending root-cause
- **Axis 3 refactored from unit test to fixture replay**: replaced generator-eligibility test with `tests/peer_conflict_pbt.rs` — hand-authored `concurrent-content-edit-merge.json` fixture replaying real peer edit → primary conflict → Loro merge E2E
- **Peer-id determinism bug fixed**: production was using random primary u64; wired in orphaned `set_loro_peer_id_if_unset` to both fixture runner and slice config, pinning `HOLON_LORO_PEER_ID=1` so primary < peers matches ref oracle's tie-break assumption
- **Disk hazard cleaned**: `cargo clean`ed two stale 118GB worktrees from handoff phase 2

### Challenges

- **Multi-session jj/absorb hazard**: main showed `(divergent)` after baseline replay; resolved by abandoning temp commits and verifying working copy integrity
- **Pre-existing SplitBlock Loro divergence surfaced**: `[[ Hbplihw7UF]]` seed → signature-identical flake on main and pre-handoff baseline; previous guards-12/12 was RNG luck; capture saved for root-cause session
- **Fixture replay exposed oracle infidelity**: ref predicted `"primary eer editdit"` but SUT produced `"peer rimary editdit"` — both valid CRDT interleavings depending on peer-id ordering

### Decisions

- **Fixture replay over unit test**: exercises real SUT E2E (peer edit → primary conflict → actual Loro merge) instead of just generator eligibility; serde-generated fixture JSON for shape correctness
- **Env-gate layout slice pending investigation**: slice proved immediately productive but red on pre-existing bug; gating with `HOLON_PBT_LAYOUT_OVERRIDE=1` keeps CI green while root-cause is separate session

**Status**: Axes 3 and 5 verified green; all gates passing (default `general_e2e_pbt` 56.0s); three env-gated findings documented in plan STATUS with replay captures; next: root-cause the layout watch leak, SplitBlock Loro flake, and axis-3 coverage depth before axes 6–7 and axis-4 promotion.
