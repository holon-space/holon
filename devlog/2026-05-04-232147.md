---
date: "2026-05-04 23:21"
session: "4e2ffd54-6e52-4e32-b7b7-07406d149afc"
project: "turso"
---

## Verified Turso IVM fixes and cleared path to Task #3

**Context:** The previous session identified three gaps in Turso's incremental materialized view (IVM) support blocking holon's block hydration task. Turso shipped fixes; this session verified them and assessed remaining work.

**What happened:**

- Confirmed `Cargo.lock` pinned to `2f61ca4eee2c97b485462658cd005db45244da43` with G0 (row-drop bug) and G1 (DISTINCT+FILTER) fixes merged
- Re-ran full reproducer (`bugs/holon_block_hydration_repro.sql`) against rebuilt `tursodb` — all dual-LEFT OUTER + dual `json_group_array` + GROUP BY cases now pass
- Verified CDC propagates correctly through mutations (INSERT/DELETE on junctions reflected in matview immediately)
- Confirmed G2 (correlated scalar subqueries) remains open but unnecessary — the dual-LEFT workaround is what holon uses anyway

**Challenges:**

- G0 / G1 fixes required rebuilding local `tursodb` to match `holon/Cargo.lock` commit; earlier test runs had caught false negatives from stale binaries
- Identified one critical preflight before Task #3: holon has 20+ existing matviews that read from `block` (e.g., `block_with_path`, `task_blocking_edges`, all `watch_view_*`). Promoting `block` itself to a matview creates matview-on-matview chains, which have hung in past sessions. Need to test chained CDC propagation before proceeding.

**Decisions:**

- Skip G2 — new error message guides users to the LEFT OUTER JOIN workaround, which is already holon's target shape
- Test chained-matview preflight on new Turso before Task #3: extend reproducer with a filtered matview on top of `case2b`, drive CDC, confirm propagation

**Status:** Turso fixes verified; Task #3 unblocked pending chained-matview CDC validation. Ready to rewind session and proceed with preflight test.
