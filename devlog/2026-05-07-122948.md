---
date: "2026-05-07 12:29"
session: "6717dcb1-3d95-4395-9983-45f46a506cdf"
project: "holon"
---

## Switched to Turso Upstream Investigation, Discovered Root Cause is Holon-Side

Investigated whether upstream Turso IVM drop on chained matviews (the documented "split_block CDC drop") was causing the focus_roots mismatch. Testing revealed it's not an upstream bug.

**What happened**

- Wrote `bigdata/turso/bugs/holon_chained_matview_update_2026-05-06.sql` reproducing the production pattern: 4× `(INSERT new block + UPDATE original.content)` through `block_mv` (dual-LEFT join + json_group_array + GROUP BY) → `focus_roots` matview
- Tested on Turso `@b17265d3` (current HEAD): all 4 splits preserved `block:1-4` in focus_roots — bug does not reproduce in isolation
- Compared holon's pinned Turso (`f682709866bb`) with `b17265d3`: diff is whitespace only (signature reformat); functionally identical
- Confirmed MatchCounter I/O re-entry fix from prior session's memory notes is already in holon's pin — no upstream fix pending

**Challenges**

- Initial hypothesis was that chained-matview UPDATE drops rows during CDC delivery, but isolated SQL testing on both Turso versions passed
- The two commits appeared distinct until diffing revealed they're functionally the same — the MatchCounter fix already landed in holon's pin

**Decisions**

- Narrowed the root cause scope back to holon-side concurrency/reactivity — likely culprits: reactive watchers spawning during split_block dispatch, LiveData mirror wiring races, or `EventKind::FieldsChanged` publish timing relative to `find_document_uri` reads
- Documented non-reproduction in upstream bug file `G4` entry so future sessions don't re-investigate
- Next investigation: capture actual SQL trace from a live PBT failure to see what operations are actually issued vs what the matview state reflects

**Status**: Root cause is confirmed holon-side, not upstream Turso. Recommitting to focus_roots invariant truth-check pattern (from #2 work) as the diagnostic foundation for the next phase.
