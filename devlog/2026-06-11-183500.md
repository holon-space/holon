---
date: "2026-06-11 18:35"
session: "1acbab27 (background, worktree arch-docs-update)"
project: "holon"
---

## Phase 2.5 (spec 0007): Turso/Loro/Org subsystem decoupling — C1–C7 complete

Implemented all of Phase 2.5 from `codev/specs/0007-architecture-improvement-plan.md`,
plus an unplanned prerequisite: main was compile-broken (10 errors in the holon lib,
plus holon-mcp-client/holon-filesystem/test-target collateral from the StorageEntity
`Arc<str>` migration × block-sync landing). Repaired all of it first — workspace
`cargo check --all-targets` is green again.

**What landed:**

- **C1/C2**: `LoroBlockReader`/`LoroDocumentManager`/`LoroBlockOrdering` + `spawn_loro_org_sync`
  moved from holon-orgmode to `crates/holon-app/src/loro_org_sync.rs`. holon-orgmode now has
  **zero** Loro mentions. Seam items publicized: `run_file_sync_controller`, `OrgRerender`,
  `LoroAliasRegistrar.doc_store`.
- **C3**: `LiveDocumentManager::new` takes `holon::storage::DbHandle` (it only ever called
  `backend.handle()`); `TursoBackendProvider` resolution dropped from holon-orgmode — zero
  Turso-concrete mentions left there.
- **C4**: `TursoSinkReader` → `crates/holon/src/storage/turso_sink_reader.rs`; the `SinkReader`
  trait stays in the sync layer, which no longer imports `DbHandle`.
- **C5**: `TursoCommandLog` + `CommandLog` trait **deleted** — dead code (never wired).
- **C6**: new `BlockLinkIndexer` trait (extraction stays in `LinkEventSubscriber`);
  `TursoBlockLinkIndexer` in the storage layer, wired in `event_infra_module`.
- **C7**: `LiveData::apply_changes` takes `Vec<Change<StorageEntity>>`; `subscribe` generic over
  `Stream<Item = BatchWithMetadata<C>> where C: Into<Change<StorageEntity>>`;
  `From<RowChange> for ChangeData` lives in holon-turso. Sync layer is `RowChange`-free.
- **Rename**: `OrgSyncController` → `FileSyncController` repo-wide (module file, fn names, docs).
- **archlint**: orgmode loro/turso smells now catch holon-mediated coupling
  (`loro_backend::|\bLoroBackend\b`, `\bTursoBackend`); zero findings. `holon::storage::DbHandle`
  deliberately allowed as the neutral handle seam.
- ~6 Loro/Turso-narrating comments in org code rewritten to "the consolidator" terms.

**Gates:** workspace check+all-targets green; archlint findings byte-identical to pre-change
baseline (19 pre-existing in untouched holon-frontend/holon-api files); `general_e2e_pbt_sql_only`
PASS (56.6s); `link_live_data_feed` 6/6, `block_live_data_catch_up` 6/6; orgmode/app/api nextest
green except known/unmasked reds below.

**Unmasked pre-existing reds (documented in spec 0007):**
- `sync_controller_mutation_pbt` 3× — (a) parser writes `parent_id = file:test.org` for top-level
  blocks after round-trip while renders root at the `block:<uuid>` doc id → header-only files;
  (b) 2× ordering_replay: block-sync's new "creates must appear in `ordering.children()`" wait
  vs static/empty stub `children()`. Both pre-date this session (binary was uncompilable).
- `holon-api test_state_display` (known, stage10).

**Hazard note:** mid-session, a concurrent jj operation folded this worktree's snapshot into the
`main` change (`smqlpyom`, now a4f7ca52+) — same absorb-rewrites-main hazard as 2026-06-10. Work
is preserved on main's change; no commits were made from this session.
