---
date: "2026-04-26 19:53"
session: "2e247798-8cf5-43f0-a74f-6def603a7015"
project: "holon"
---

## Documentation Refactor: ARCHITECTURE.md Split & Content Updates

**Problem**: ARCHITECTURE.md had grown to 2951 lines with 15 stale comment markers and outdated architecture descriptions (Flutter-first, old ViewModel patterns). The monolith needed splitting into maintainable pieces and fact-checking.

**What happened**:
- Identified all `<!-- -->` markers and verified current type usage (`WidgetSpec` → `DataRow`, `ViewModel` → `ReactiveViewModel`)
- Fixed 4 quick-fact errors: storage diagram labels, EventBus implementation status, platform support (added Android/WASM), GQL schema scope
- Removed 3 large historical sections: OLD/NEW migration diagrams, "WidgetSpec vs Old RenderSpec" comparison table, entire Flutter Frontend Architecture subsection
- Rewrote EntityProfile + MVVM sections to reflect current `ReactiveViewModel`/`ReactiveEngine` reality
- Split 2951 lines into hub + 7 focused sub-docs using systematic boundary analysis
- Reorganized to `docs/Architecture.md` (236-line hub) + `docs/Architecture/{Storage, RenderPipeline, Operations, Integrations, Schema, Engine, Sync}.md` (7 files, ~2600 lines total)
- Updated 16 files with new reference paths (`ARCHITECTURE.md` → `docs/Architecture.md`, relative backlinks adjusted)

**Challenges**:
- **Stale type references**: `WidgetSpec`/`ResolvedRow` had been replaced by `DataRow`/`DataRowAccumulator` in futures-signals layer; verified against current codebase before removing
- **MVVM rewrite scope**: Section mixed old ViewModel tree concepts with new ReactiveViewModel — solved by keeping only current abstractions and updating code examples
- **Backlink fragility**: Sub-docs initially had `../../ARCHITECTURE.md` references; adjusted to `../Architecture.md` after move

**Decisions**:
- **Hub-first approach**: Keep ARCHITECTURE.md as slim navigation layer (Overview, Principles, Crate Structure, Core Traits, links table) rather than deep reference — users navigate to focused sub-docs
- **CamelCase filenames**: Chose `docs/Architecture/RenderPipeline.md` format for consistency with domain-language naming over snake_case
- **Deletion over deprecation**: Removed Flutter subsection entirely (GPUI is primary) rather than marking stale — reduces maintenance burden

**Status**: Documentation is now split, current, and link-consistent across 8 files. All stale markers cleared.
