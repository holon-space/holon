<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="space.holon.gpui">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:label="Holon"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:hasCode="true"
        android:debuggable="true">

        <!-- ESCALATION PATH (do NOT implement here without on-device evidence):
             classes.dex now ships dev.gpui.mobile.GpuiTextInputView, which the
             GPUI runtime loads via find_app_class to host the platform IME on a
             plain NativeActivity. If on-device testing shows the class loads but
             the soft keyboard still never surfaces under NativeActivity, the next
             step is to launch the fork's dev.gpui.mobile.GpuiActivity (a
             NativeActivity subclass) instead of android.app.NativeActivity below.
             That pulls in androidx (splashscreen, media) + more Gpui* classes, so
             it requires vendoring those and an androidx-capable dex/resource build.
             Out of scope for this lane. -->
        <activity
            android:name="android.app.NativeActivity"
            android:exported="true"
            android:theme="@android:style/Theme.DeviceDefault.NoActionBar"
            android:windowSoftInputMode="adjustResize"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden">

            <meta-data
                android:name="android.app.lib_name"
                android:value="holon_gpui" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
