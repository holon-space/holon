// This file is automatically generated, so please do not edit it.
// @generated by `flutter_rust_bridge`@ 2.11.1.

// ignore_for_file: invalid_use_of_internal_member, unused_import, unnecessary_import

import '../../frb_generated.dart';
import 'package:flutter_rust_bridge/flutter_rust_bridge_for_generated.dart';
import 'package:freezed_annotation/freezed_annotation.dart' hide protected;
part 'types.freezed.dart';

// These types are ignored because they are neither used by any `pub` functions nor (for structs and enums) marked `#[frb(unignore)]`: `DependsOn`, `Tags`, `Timestamp`
// These function are ignored because they are on traits that is not defined in current crate (put an empty `#[frb]` on it to unignore): `as_ref`, `assert_receiver_is_total_eq`, `assert_receiver_is_total_eq`, `assert_receiver_is_total_eq`, `assert_receiver_is_total_eq`, `assert_receiver_is_total_eq`, `assert_receiver_is_total_eq`, `assert_receiver_is_total_eq`, `assert_receiver_is_total_eq`, `assert_receiver_is_total_eq`, `assert_receiver_is_total_eq`, `assert_receiver_is_total_eq`, `borrow`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `cmp`, `cmp`, `cmp`, `cmp`, `deserialize`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `from_str`, `from_str`, `from_str`, `from_str`, `from_str`, `from_str`, `from_str`, `from_str`, `from_str`, `from_str`, `from`, `from`, `from`, `from`, `from`, `from`, `from`, `from`, `from`, `from`, `from`, `hash`, `hash`, `hash`, `hash`, `hash`, `hash`, `hash`, `hash`, `hash`, `hash`, `partial_cmp`, `partial_cmp`, `partial_cmp`, `partial_cmp`, `serialize`, `try_from`, `try_from`, `try_from`, `try_from`, `try_from`
// These functions are ignored (category: IgnoreBecauseOwnerTyShouldIgnore): `as_slice`, `as_slice`, `as_str`, `contains`, `date`, `default`, `default`, `from_csv`, `from_csv`, `from_iter`, `is_empty`, `is_empty`, `iter`, `parse`, `push`, `to_csv`, `to_csv`, `to_org`, `to_set`
// These functions have error during generation (see debug logs or enable `stop_on_error: true` for more details): `active`, `done`, `new`, `new`

/// Content type — always "text" or "source", no other values exist.
///
/// flutter_rust_bridge:non_opaque
enum ContentType { text, source }

/// Typed entity name — the canonical name for a domain entity (e.g., "block", "document").
///
/// Simultaneously the SQL table name, the URI scheme component, and the
/// operation dispatch key. Always singular by convention.
///
/// Serializes as a plain string over FFI (Flutter sees `String`).
///
/// flutter_rust_bridge:non_opaque
class EntityName {
  final String field0;

  const EntityName({required this.field0});

  Future<void> asStr() =>
      RustLib.instance.api.holonApiTypesEntityNameAsStr(that: this);

  @override
  int get hashCode => field0.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is EntityName &&
          runtimeType == other.runtimeType &&
          field0 == other.field0;
}

/// Task priority — decoupled from org's A/B/C letter convention.
///
/// Stored as integer in SQL (High=3, Medium=2, Low=1).
/// Org serialization uses letters (A, B, C).
///
/// flutter_rust_bridge:non_opaque
enum Priority {
  low,
  medium,
  high;

  static Future<Priority> fromInt({required int n}) =>
      RustLib.instance.api.holonApiTypesPriorityFromInt(n: n);

  static Future<Priority> fromLetter({required String s}) =>
      RustLib.instance.api.holonApiTypesPriorityFromLetter(s: s);

  Future<int> toInt() =>
      RustLib.instance.api.holonApiTypesPriorityToInt(that: this);

  Future<void> toLetter() =>
      RustLib.instance.api.holonApiTypesPriorityToLetter(that: this);
}

/// Query languages with special dispatch in the engine.
///
/// flutter_rust_bridge:non_opaque
enum QueryLanguage {
  holonPrql,
  holonGql,
  holonSql;

  /// Returns a SQL `IN (...)` list of all query language string values, for use in SQL queries.
  static Future<String> sqlInList() =>
      RustLib.instance.api.holonApiTypesQueryLanguageSqlInList();
}

/// Navigation region — a named UI area that can hold focus state.
///
/// Currently: Main, LeftSidebar, RightSidebar. Closed enum — adding a new
/// region requires a code change (schema init, matview, CDC plumbing).
///
/// flutter_rust_bridge:non_opaque
enum Region {
  main,
  leftSidebar,
  rightSidebar;

  Future<void> asStr() =>
      RustLib.instance.api.holonApiTypesRegionAsStr(that: this);
}

@freezed
sealed class SourceLanguage with _$SourceLanguage {
  const SourceLanguage._();

  const factory SourceLanguage.query(QueryLanguage field0) =
      SourceLanguage_Query;
  const factory SourceLanguage.render() = SourceLanguage_Render;
  const factory SourceLanguage.other(String field0) = SourceLanguage_Other;

  Future<QueryLanguage?> asQuery() =>
      RustLib.instance.api.holonApiTypesSourceLanguageAsQuery(that: this);

  Future<bool> isPrql() =>
      RustLib.instance.api.holonApiTypesSourceLanguageIsPrql(that: this);
}

/// Whether a task keyword represents an active or done state.
///
/// flutter_rust_bridge:non_opaque
enum StateCategory { active, done }

/// Task lifecycle state: a keyword (e.g. "TODO", "DOING", "DONE") paired with
/// its category (Active or Done). The category is determined at the parse
/// boundary — from org `#+TODO:` config, provider mapping, or default lists.
///
/// flutter_rust_bridge:non_opaque
class TaskState {
  final String keyword;
  final StateCategory category;

  const TaskState({required this.keyword, required this.category});

  /// Categorize a keyword using the default done-keyword list.
  /// Unknown keywords default to Active.
  static Future<TaskState> fromKeyword({required String keyword}) =>
      RustLib.instance.api.holonApiTypesTaskStateFromKeyword(keyword: keyword);

  /// Categorize a keyword using a caller-supplied list of done keywords
  /// (from org `#+TODO:` config or provider mapping).
  static Future<TaskState> fromKeywordWithDoneList({
    required String keyword,
    required List<String> doneKeywords,
  }) => RustLib.instance.api.holonApiTypesTaskStateFromKeywordWithDoneList(
    keyword: keyword,
    doneKeywords: doneKeywords,
  );

  Future<bool> isActive() =>
      RustLib.instance.api.holonApiTypesTaskStateIsActive(that: this);

  Future<bool> isDoing() =>
      RustLib.instance.api.holonApiTypesTaskStateIsDoing(that: this);

  Future<bool> isDone() =>
      RustLib.instance.api.holonApiTypesTaskStateIsDone(that: this);

  @override
  int get hashCode => keyword.hashCode ^ category.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is TaskState &&
          runtimeType == other.runtimeType &&
          keyword == other.keyword &&
          category == other.category;
}

/// Frontend capability descriptor — tells the backend what the frontend can render.
///
/// Profiles are filtered against this: variants referencing widgets not in
/// `available_widgets` are dropped, so a TUI never receives `tree()` specs.
///
/// flutter_rust_bridge:non_opaque
class UiInfo {
  final Set<String> availableWidgets;
  final (int, int)? screenSize;

  const UiInfo({required this.availableWidgets, this.screenSize});

  static Future<UiInfo> default_() =>
      RustLib.instance.api.holonApiTypesUiInfoDefault();

  /// Returns true if this UiInfo performs no filtering (empty available_widgets = all allowed).
  Future<bool> isPermissive() =>
      RustLib.instance.api.holonApiTypesUiInfoIsPermissive(that: this);

  /// A UiInfo that accepts all widgets (no filtering).
  static Future<UiInfo> permissive() =>
      RustLib.instance.api.holonApiTypesUiInfoPermissive();

  /// Check whether all widget names in `names` are supported.
  /// An empty `available_widgets` set means "all widgets allowed".
  Future<bool> supportsAll({required Set<String> names}) => RustLib.instance.api
      .holonApiTypesUiInfoSupportsAll(that: this, names: names);

  @override
  int get hashCode => availableWidgets.hashCode ^ screenSize.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is UiInfo &&
          runtimeType == other.runtimeType &&
          availableWidgets == other.availableWidgets &&
          screenSize == other.screenSize;
}
