[package]
name = "example3-declarative"
version = "0.1.0"
edition = "2024"
description = "declarative_enum_dispatch: enum_dispatch! macro generates dispatch from trait + enum."

[workspace]

[dependencies]
declarative_enum_dispatch = "0.1"
anyhow = "1"
