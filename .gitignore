# Added by cargo

**/target/
/build

# Frontend
node_modules/
dist/
*.log
package-lock.json

*.db
*.db-wal
*.db-shm

# Auto Claude data directory
.auto-claude/

# Auto Claude generated files
.auto-claude-security.json
.auto-claude-status
.claude_settings.json
.worktrees/
.security-key
logs/security/
.observability/

# dora code context index
.dora/

# Claude Code — ignore local state but TRACK skills (they are repo deliverables;
# a bare `.claude/` silently dropped new SKILL.md files from commits)
.claude/*
!.claude/skills/
!.claude/logseq.json
.claude/worktrees/
.claude-flow/

# IDE
.idea/

# Flutter build artifacts
**/flutter/build/
**/.dart_tool/
**/.gradle/
.gstack/
frontends/dioxus-web/node_modules/

# Ralph spec state
specs/.current-spec
specs/.current-epic
**/.progress.md
.code-graph/

# cargo-crap coverage input (large, regenerated by `just analyze-crap`)
/lcov.info

# PBT capture-on-panic scratch output (hand-promote into tests/fixtures/<slice>/)
**/tests/.captures/

# archidoc IR scratch output
_context/

# Observability run records — emitted by every keystone/GPUI PBT run as a side
# effect; uploaded to the GitHub `observability` release by `just obs-push`.
.observability/
frontends/gpui/android/build-release/
frontends/gpui/android/build-release-aab/

# proptest auto-persisted failure seeds — disabled (failure_persistence: None)
# repo-wide; permanent regression guards live in
# crates/holon-integration-tests/hand-authored-regressions/keystone.jsonl instead
*.proptest-regressions
**/proptest-regressions/*.txt

# Bug-funnel index: derived from docs/Testing/bugfunnel/entries/ by
# `scripts/bugfunnel.py index`. Committing it would restore the single
# insertion point that per-entry files exist to remove.
docs/Testing/bugfunnel/INDEX.md

# Playwright run artifacts (frontends/dioxus-web)
test-results/
playwright-report/
